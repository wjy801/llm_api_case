# 框架测试用例编写指南重构同步变更记录

## 任务目标

同步整体重构后的 `FRAMEWORK_TEST_SPEC.md`，保证新增业务用例和框架离线测试遵循新的依赖边界，同时保持原有目录模板、断言、账单、重试、轮询和 Flaky 规范不变。

## 变更内容

- 更新用例到 P0/P1 的执行链，加入中性 Runtime Hooks、Noop 和 `quality.runtime_adapter`。
- 扩充分层职责，明确 `common/runtime_hooks/`、三个质量子目录和 `run_orchestration/` 的用途及禁止事项。
- 固定 `quality -> common.runtime_hooks` 单向依赖，禁止业务用例直接导入质量或编排内部实现。
- 在默认中间件能力中加入 `RuntimeObservationMiddleware`。
- 完善线程池 ContextVar 语义，明确传播 run/case、operation 和 Runtime Hooks。
- 将复合业务动作示例从 `quality.semantic_*` 改为 `common.runtime_hooks` 中性 API。
- 增加 common 解耦、质量目录和执行编排的离线边界测试要求。
- 明确 collect-only 不生成质量身份、产物，不调用 `pytest.main` 或真实接口。
- 同步 `596 passed` 和 Smoke collect-only `41` 项的当前验收基线。
- 更新新增用例提交检查清单，加入内部依赖和中性作用域检查。

## 验证

- 文档中的 Runtime Hooks 与 `submit_with_context` 示例均可公开导入。
- 当前 `module/` 不存在对 `quality` 或 `run_orchestration` 内部实现的直接导入。
- `git diff --check -- FRAMEWORK_TEST_SPEC.md` 通过。
- 未执行真实接口或付费测试。
