# 质量报告清理阶段 4：Jenkins 与报告入口收敛代码变更记录

## 目标

固定 Jenkins 与 Pipeline Summary 的最终入口白名单，确保人工入口只保留流水线执行摘要、JUnit、Allure 和构建产物。

## 代码改动

- 扩展 `test_quality_jenkinsfile.py` 的静态契约：
  - 固定 `reports/` 在每轮 Prepare 阶段整体删除后重建。
  - 固定摘要生成早于 Allure、归档和邮件通知。
  - 固定 fallback 不修改 `currentBuild.result` 或 `currentBuild.currentResult`。
  - 固定邮件链接仅包含 Pipeline Summary、Allure、JUnit 和构建产物。
- 阶段 2/3 已前移完成的 Jenkins 旧变量、P0/P1 链接及 renderer 详细证据删除保持不变。
- 未修改 Job 参数、凭据、SCM、SMTP、定时任务或 Jenkins `config.xml`。

## 验证结果

- Jenkins、Pipeline Reporting、删除边界定向测试：`25 passed`。
- 旧变量与旧链接扫描仅命中否定断言和阶段 0 删除前历史 fixture。
- `git diff --check` 通过。
- 未触发 Jenkins 构建。
- 未调用真实付费接口。
