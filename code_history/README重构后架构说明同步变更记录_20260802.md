# README 重构后架构说明同步变更记录

## 任务目标

同步框架整体重构后的 README，使使用者在不阅读开发方案的情况下能够确认稳定入口、依赖方向、模块职责和扩展落点。

## 变更内容

- 在当前状态中补充 common/quality 解耦及四个独立目录重构完成说明。
- 更新目录结构，加入 `common/runtime_hooks/`、`quality/runtime_adapter.py`、`quality/metrics/`、`quality/observation_report/`、`quality/flaky_store/` 和 `run_orchestration/`。
- 新增“重构后的架构边界”，明确 `quality -> common.runtime_hooks` 单向依赖和 Noop/Adapter 注入机制。
- 增加按变化类型选择修改目录的职责映射表。
- 补充线程池必须通过 `submit_with_context()` 传播 ContextVar 的要求。
- 修正默认请求中间件清单，加入 `RuntimeObservationMiddleware`。
- 说明根 `run_master.py` 是稳定兼容入口，并列出 pytest、Allure、Quality 环境和质量流水线的单一所有者。
- 明确 collect-only 无 Quality 和真实接口副作用，以及 P0 merge 与其他质量阶段的失败边界。
- 记录当前 `596 passed` 和 Smoke collect-only `41` 项的验收基线。

## 验证

- README 引用的目录和文件均存在。
- `common.submit_with_context` 可公开导入。
- `run_master.run` 与 `run_orchestration.run` 保持同一公开对象。
- `git diff --check -- README.md` 通过。
- 未执行真实接口或付费测试。
