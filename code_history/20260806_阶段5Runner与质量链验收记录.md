# 阶段5 Runner与质量链验收记录

> 执行日期：2026-08-06（Asia/Shanghai）
> 执行分支：`dev3`
> 执行提交：`c0954ba707dba258a08d7cfdd623e5628acf5ea8`
> Python：`3.14.6`
> 对应方案：`dev_plan/阶段5完成Runner、Quality、Metrics、Flaky与Jenkins验收详细开发方案_20260805.md`

## 1. 最终状态

```text
local_validation_status = PASS
jenkins_validation_status = PASS
stage5_status = PASS
release_status = PASS
overall_plan_status = PENDING_STAGE6
```

本地Runner、Quality、Semantic、Metrics、Fail-open、Flaky、Pipeline Summary、完整回归和Smoke分池门禁均通过。实际Jenkins Build #76按冻结参数完成并通过，因此阶段5可以关闭为完成；总方案仍需阶段6同步文档和发布基线。

## 2. 第一性原理与TOC约束

阶段5验证的是同一轮pytest事实能否被Runner、JUnit、Allure、Quality、Semantic、Metrics、Flaky和Pipeline Summary连续消费，不以单个命令退出码代替产物完整性。

本轮首要约束最初为实际Jenkins权限，取得用户明确提供的临时API认证后解除。首次Build #74又暴露GitHub出口暂时不可达；未修改Job、Jenkinsfile或SCM来源，等待出口恢复后重新执行。最终Build #76完成了真实Checkout、离线接口测试、质量链、报告和归档闭环。认证信息未写入仓库、日志或验收记录。

## 3. 执行前保护

- 工作树原有修改已记录并保持不变：两份计划文档、3个素材库文件和未跟踪的`module/test/`。
- `reports/execution-result.json`执行前SHA-256：`1C082BBB479003E04C838D5983940317FC0FFEAC3092CB65250BC47D58A186DC`。
- 所有可配置运行产物写入系统临时证据目录：`<system-temp>/api-case-stage5-execution-1785980217154/`。
- 未修改`common/`、`quality/`、`run_orchestration/`、`pipeline_reporting/`、离线业务模块、`tests/`或`Jenkinsfile`。

## 4. Runner与Quality关闭验收

### Collect-only

```text
total = 23
parallel = 23
serial = 0
exit_code = 0
```

黄金路径nodeid唯一，集合守恒；collect-only前后共享Runner摘要Hash一致。

### Quality关闭

```text
pytest = 23 passed
JUnit = 23 tests / 0 failures / 0 errors
Runner final_exit_code = 0
Quality files = 0
```

自定义JUnit和Allure目录生效，Quality关闭时没有创建run身份或质量机器产物。

## 5. 完整Quality、Semantic与Metrics验收

```text
pytest = 23 passed
run = finished / complete
P0 = complete / 23 invocations / 0 foreign records
Semantic = complete / 0 incomplete operations / 0 foreign records
Metrics = degraded
Metrics issue codes = [usage_incomplete]
workload operations = 21
workload request groups = 30
workload request events = 35
external URL templates = 0
```

P0、Semantic和Metrics共12项输出及来源Hash全部匹配。完整模块的Metrics降级原因只有职责单一用例缺少usage，未出现operation不完整、外来run ID或来源损坏。

## 6. 黄金路径精确验收

稳定nodeid：

```text
module/offline_framework_example/test_full_framework_flow.py::TestFullFrameworkFlow::test_offline_async_media_flow
```

结果：

```text
pytest = 1 passed
P0 = 1 invocation / 8 request events
Semantic = 4 operations / 7 request groups / 1 polling session
ASYNC_TASK = media_generation / workload / offline-media-model
ASYNC_TASK request groups = 4
control operations = 2 audit + 1 cleanup
Retry rescue = 503 -> 200
Polling = pending -> pending -> success
usage media_count = 1
Metrics = aggregated
```

## 7. Fail-open验收

- 3项既有Fail-open合同测试全部通过。
- 使用相对Flaky数据库路径执行黄金路径时，业务和Runner退出码仍为0。
- P0、Semantic和Metrics继续成功，Metrics为`aggregated`。
- `flaky-import.json`为`NO_DATA / invalid_flaky_history_configuration`。
- `flaky-evaluation.json`为`NO_DATA / flaky_history_import_not_ready`。
- 仓库内没有创建相对SQLite文件。

该结果证明“业务通过、Flaky观测按合同降级”，没有把观测失败伪造成观测成功。

## 8. Flaky多轮验收

```text
stable_min_samples = 3
rounds = 4
states = OBSERVING -> OBSERVING -> STABLE -> STABLE
transitioned_count = 1 -> 0 -> 1 -> 0
sample_size = 1 -> 2 -> 3 -> 4
```

每轮导入1个真实`pass`样本；4个run ID和observation ID均不同，`case_id`、`param_hash`、environment=`overseas`、execution_profile=`serial`、state epoch和flaky key保持稳定。最终history为4条，SQLite quick check为`ok`。

## 9. Pipeline Summary本地模拟

```text
conclusion = WARN
reason = 执行通过，但重试挽救了瞬时失败
smoke tests = 1 / 1 passed
request health = 7 / 8 success
retry health = 1 / 1 rescued
flaky available = true
flaky transition_count = 0
```

接口耗时Top非空；机器JSON、Markdown、邮件主题和邮件HTML四项产物均存在且非空。框架单测和收集阶段为`NOT_RUN`，接口测试与质量观测为`PASSED`，没有进入fallback摘要。

## 10. 完整回归与Smoke分池

```text
framework tests = 686 passed / 0 failed / 0 errors / 0 skipped
Smoke collect-only = 40 total / 15 parallel / 25 serial
```

完整回归零失败，Smoke集合和分池语义未回退。

## 11. Jenkins外部门禁

### 构建过程

- Controller可访问，Windows Agent在线。
- 阶段4并发分类和黄金路径文件已进入`c0954ba`，SCM可见性通过。
- Build #74在加载Jenkinsfile前因GitHub出口不可达失败，未进入业务或测试阶段，不属于代码失败。
- 用户手动启动的Build #75使用默认参数，目标仍为`module/smoke`；发现参数不满足合同后中止，不作为验收证据。
- Build #76使用冻结参数执行并以`SUCCESS`结束，Build URL为`http://localhost:8080/job/llm-api-case/76/`，耗时`103510 ms`。

### Build #76参数与SCM

```text
RUN_FRAMEWORK_TESTS=false
RUN_COLLECT_ONLY=false
RUN_REAL_SMOKE=true
GENERATE_PIPELINE_SUMMARY=true
ALWAYS_SEND_REPORT_EMAIL=false
USE_CHINA_ENVIRONMENT=FALSE
SMOKE_TARGET=module/offline_framework_example
TEST_PARALLEL_WORKERS=2
```

Checkout提交：`c0954ba707dba258a08d7cfdd623e5628acf5ea8`。

### Jenkins执行与报告事实

```text
Jenkins result = SUCCESS
JUnit = 23 passed / 0 failed / 0 skipped
Runner = 23 planned / final_exit_code 0
P0 = complete / 23 invocations / 0 foreign records
Semantic = complete / 0 incomplete operations / 0 foreign records
Metrics = degraded / only usage_incomplete
Flaky import = IMPORTED / 23 inserted
Flaky evaluation = EVALUATED / 23 affected / 23 transitioned
Pipeline Summary = WARN
request health = 44 / 50 success
retry rescue = 5 / 5
flaky transitions = 23
external business URL templates = 0
```

Pipeline Summary的`WARN`只来自Retry挽救瞬时失败，接口测试和质量观测阶段均为`PASSED`，框架单测和收集阶段按参数为`NOT_RUN`。

### 入口、归档与同源性

- Jenkins JUnit API返回HTTP 200，23项全部通过。
- Allure入口返回HTTP 200。
- Build归档包含581项文件，其中543项位于`allure-results/`。
- `pipeline-summary.md/json`、邮件主题、邮件HTML、Runner摘要、JUnit和Quality产物均已归档且非空。
- `run.json`、P0、Semantic、Metrics、Flaky Import和Flaky Evaluation使用同一run ID：`llm-api-case-76-20260806T020238Z-c901734c`。
- 9项关键输出和跨层来源Hash全部匹配。
- 邮件主题和HTML渲染成功；`ALWAYS_SEND_REPORT_EMAIL=false`，本轮不要求真实SMTP投递。

## 12. 现场恢复与范围

- `reports/execution-result.json`已恢复原字节，恢复后Hash与执行前一致。
- `git diff --check`通过。
- 未创建仓库内SQLite、JUnit、Allure、Quality或Pipeline临时产物。
- 用户执行前已有文件未被清理、还原、暂存或格式化。
- 本轮唯一新增仓库文件为本验收记录。

## 13. 结论

本地运行级事实链、完整回归和实际Jenkins离线构建均已通过。按照阶段5完成定义，结论为：

```text
本地门禁：PASS
Jenkins门禁：PASS
阶段5整体：PASS
阶段5发布门禁：PASS
总方案：等待阶段6文档与发布基线收敛
```

阶段5已经完成，但不提前把尚未执行的阶段6写成总任务完成。
