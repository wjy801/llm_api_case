# 阶段6文档与发布基线验收记录

> 执行日期：2026-08-06（Asia/Shanghai）
> 执行分支：`dev3`
> 执行提交：`c0954ba707dba258a08d7cfdd623e5628acf5ea8`
> Python：`3.14.6`
> 对应方案：`dev_plan/阶段6完成文档历史记录与发布基线详细开发方案_20260805.md`

## 1. 本轮目标与边界

本轮只收敛离线框架能力的学习文档、用例编写规范和发布证据，不增加协议、用例、报告类型或核心实现。计划修改范围为：

- `README.md`；
- `FRAMEWORK_TEST_SPEC.md`；
- 本验收记录。

未修改 `common/`、`quality/`、`run_orchestration/`、`pipeline_reporting/`、`module/offline_framework_example/*.py`、`tests/`、`.gitignore`、`Jenkinsfile`、`requirements.txt` 或 `pytest.ini`。

## 2. 第一性原理与TOC约束

文档的最小可信单位是“准确事实、可执行命令、可追溯证据、明确状态边界”。阶段5运行事实已经通过，本阶段首要约束不是继续扩展能力，而是消除以下不一致：

```text
代码与机器证据已经PASS
→ README仍记录历史阻塞
→ 用例规范仍记录7项和未实现能力
→ 学习者无法判断当前真实边界
```

因此本轮以阶段6实际执行、Build #76归档、阶段5记录和当前Git对象为证据，统一数量、nodeid、状态和运行入口。

## 3. 执行前工作树和SCM状态

执行前已有且受保护的工作树内容：

- 3份开发方案修改；
- 3个素材库文件修改；
- 未跟踪的阶段5 `code_history`；
- 未跟踪的 `module/test/`。

SCM复核结果：

```text
offline module files = 15
tracked required files = 15
test files = 7
test cases = 23
SCM visibility = PASS
HEAD = c0954ba707dba258a08d7cfdd623e5628acf5ea8
```

`test_concurrency_context.py`和`test_full_framework_flow.py`均存在于HEAD，且本机内容与HEAD无差异，因此Build #76证据仍适用于当前代码。

## 4. 实际新增、修改文件

本轮修改：

- `README.md`：更新当前状态、学习顺序、分类与黄金路径职责、Quality开关命令、产物位置、Flaky规则、Jenkins事实和验收快照；
- `FRAMEWORK_TEST_SPEC.md`：固化三层离线结构、分类职责、确定性服务、Quality边界、新能力归类算法和23项快照。

本轮新增：

- `code_history/20260806_阶段6文档与发布基线验收记录.md`。

阶段4、阶段5文件和执行前已有业务文件不归属于本轮新增。

## 5. 23条分类数量

| 文件 | 数量 | 职责 |
|---|---:|---|
| `test_request_pipeline.py` | 3 | Request、Middleware、payload与Header |
| `test_retry.py` | 4 | 重试资格、等待与挽救 |
| `test_polling.py` | 4 | success、failure、unknown与deadline |
| `test_context_cleanup.py` | 4 | 提取、转换、LIFO清理与错误归并 |
| `test_capture_assertions.py` | 4 | Capture、Schema与脱敏诊断 |
| `test_concurrency_context.py` | 3 | ContextVar、Session与Header隔离 |
| `test_full_framework_flow.py` | 1 | 稳定成功黄金路径 |
| **合计** | **23** | **6组分类加1条黄金路径** |

Runner collect-only结果为`23 total / 23 parallel / 0 serial`，最终计划与两池互斥并集一致。

## 6. 黄金路径覆盖和关系

稳定nodeid：

```text
module/offline_framework_example/test_full_framework_flow.py::TestFullFrameworkFlow::test_offline_async_media_flow
```

业务主链：

```text
创建异步媒体任务
→ 首次Polling请求503后Retry挽救
→ pending
→ pending
→ success
→ 输出Capture与Schema断言
→ 并发审计查询
→ cleanup
```

阶段5同版本精确证据为`4 operations / 7 request groups / 1 polling session / 8 request events`，`media_count=1`。阶段6再次直接执行该nodeid，结果为`1 passed`。

## 7. 文档命令实际结果

| 命令 | 结果 |
|---|---|
| `pytest tests/test_offline_service.py -q` | `18 passed` |
| `run_master.py module/offline_framework_example --collect-only -q` | `23/23/0`，黄金nodeid一次 |
| README Quality关闭代码块 | `23 passed` |
| README Quality开启代码块 | `23 passed` |
| 黄金nodeid直接pytest | `1 passed` |
| `run_master.py module/smoke --collect-only -q` | `40/15/25` |
| `pytest tests -q` | `686 passed` |

两个README Quality代码块均在独立PowerShell进程中原样执行成功。Runner并发池把自定义JUnit文件写为同目录下的`*-parallel.xml`，路径语义与文档一致。

## 8. Quality关闭和开启结果

Quality关闭：

```text
pytest = 23 passed
JUnit = 23 tests / 0 failures / 0 errors
Quality files = 0
Allure raw = non-empty
Runner result hash restored = true
```

Quality、Semantic和Metrics开启：

```text
pytest = 23 passed
run = finished / complete
P0 = complete / 23 invocations / 0 foreign records
Semantic = complete / 0 foreign / 0 conflict / 0 incomplete / 0 integrity issues
Semantic outputs = 36 operations / 45 request groups / 7 polling sessions
Metrics workload = 21 operations / 30 request groups / 35 request events
Metrics = degraded / only usage_incomplete
P0, Semantic and Metrics hashes = matched
external URL templates = 0
```

Quality运行使用唯一系统临时目录和run ID，未复用阶段5产物；结束后环境变量和`reports/execution-result.json`均已恢复。

## 9. Flaky多轮结果

阶段6动态读取当前规则：

```text
stable_min_samples = 3
required rounds = 4
```

本阶段不重复写入Flaky历史，复用阶段5同版本真实样本证据：

```text
OBSERVING → OBSERVING → STABLE → STABLE
sample_size = 1 → 2 → 3 → 4
```

四轮使用同一黄金nodeid和稳定case身份、独占绝对SQLite数据库、逐轮唯一run ID。

## 10. Jenkins参数、Build和归档

Jenkins Build #76在线复核结果：

```text
Job = llm-api-case
Build URL = http://localhost:8080/job/llm-api-case/76/
Result = SUCCESS
Duration = 103510 ms
Checkout = c0954ba707dba258a08d7cfdd623e5628acf5ea8
JUnit = 23 passed / 0 failed / 0 skipped
Allure entry = HTTP 200
Artifacts = 581
Allure raw artifacts = 543
```

冻结参数：

```text
RUN_FRAMEWORK_TESTS=false
RUN_COLLECT_ONLY=false
RUN_REAL_SMOKE=true
GENERATE_PIPELINE_SUMMARY=true
ALWAYS_SEND_REPORT_EMAIL=false
USE_CHINA_ENVIRONMENT=FALSE
SMOKE_TARGET=module/offline_framework_example
TEST_PARALLEL_WORKERS=2
```

Runner、JUnit、Pipeline Summary、P0、Semantic、Metrics、Flaky Import和Flaky Evaluation均为Build #76同轮归档。Metrics唯一降级原因是`usage_incomplete`；Flaky导入23项、评估23项、迁移23项；Pipeline Summary为Retry挽救导致的`WARN`；业务外部URL模板为0。

## 11. 完整回归和Smoke收集

```text
framework tests = 686 passed / 0 failed / 0 errors / 0 skipped
Smoke collect-only = 40 total / 15 parallel / 25 serial
offline collect-only = 23 total / 23 parallel / 0 serial
```

完整回归零失败，Smoke与离线模块集合和分池语义均未回退。

## 12. 四层状态结论

```text
local_validation_status = PASS
jenkins_validation_status = PASS
release_status = PASS
overall_plan_status = COMPLETE
```

阶段6文档、命令真实性、历史记录和一致性门禁全部通过。因此整体发布为`PASS`，离线框架能力分类用例与黄金路径总任务为`COMPLETE`。

## 13. 未纳入能力和原因

- SSE/流式响应不属于本轮黄金路径；
- 真实模型服务、真实账号和真实计费不属于离线范围；
- Jenkins依赖准备可能访问pip/npm镜像，因此只声明业务接口零外部请求，不声明整个构建物理断网；
- 不增加第二套Runner、Quality、Flaky或人工报告实现；
- 数量是2026-08-06快照，不是永久不变的用例总数合同。

## 14. 静态验证与现场恢复

- README和规范中的7项、历史4项失败和旧`BLOCKED`结论已清零；
- 当前23项、黄金nodeid、`4/7/1/8`、Build #76和`COMPLETE`结论一致；
- 两份文档为UTF-8无BOM、LF、末尾换行，Markdown代码围栏成对；
- `git diff --check`通过；
- `.gitignore`、两个阶段4核心测试和`reports/execution-result.json`的SHA-256与执行前一致；
- 临时验证脚本已删除；Quality、JUnit和自定义Allure证据均位于系统临时目录；
- 黄金nodeid直接pytest按现有统一生命周期刷新本地忽略的Allure目录，不产生tracked差异；
- 执行前已有素材库、开发方案和`module/test/`未被清理、还原、暂存或格式化。

## 15. 剩余债务和责任边界

当前没有阻塞离线学习与回归基线发布的剩余债务。后续约束为：

- 目标提交或关键离线文件变化后，Build #76证据失效，必须重新执行Jenkins门禁；
- 新增能力优先归入现有单一职责分类，只有形成独立稳定合同才新增分类文件；
- 黄金路径只保留稳定成功主链，互斥异常继续由分类用例承担；
- 完整回归出现任何失败时，整体发布立即回到`BLOCKED`；
- 文档数量、学习顺序和机器证据必须随能力变化同步更新。
