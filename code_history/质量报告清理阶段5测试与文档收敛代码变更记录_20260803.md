# 质量报告清理阶段 5：测试与文档收敛代码变更记录

## 目标

清除活动测试、配置示例和文档对旧 P0/P1 报告的正向描述，保留阶段 0 Pipeline Summary 基线、Metrics、Semantic、Flaky、JUnit 与 Allure 契约。

## 代码与测试改动

- 扩展删除边界测试：
  - 固定 `run_orchestration.quality_observation_stage` 不存在。
  - 固定 `pipeline_reporting/sources.py` 不读取旧报告产物或导入旧模块。
  - 固定 README、FRAMEWORK_TEST_SPEC、Jenkins 迁移模板和 `.env.example` 不推荐旧报告、旧命令或旧环境变量。
- Observation Refactor 专属 fixture 的跟踪文件已在阶段 2 删除；未删除阶段 0 的 `pipeline_report_cleanup` 历史基线。
- Metrics、Flaky Store/State 等价性 fixture 保持不变。

## 活动文档改动

- `.env.example`：只保留有效的 Metrics 与 Flaky 用户配置，并说明 Jenkins 内部设置的前置开关。
- `README.md`：将 `pipeline-summary.md` 固定为唯一人工质量入口；merged、semantic、metrics、flaky JSON/JSONL 明确为机器数据。
- `FRAMEWORK_TEST_SPEC.md`：删除旧报告编写、阅读与验收入口，保留事实采集、身份、Schema、Metrics、Flaky、JUnit 和 Allure 规范。
- `JENKINS_MIGRATION_TEMPLATE.md`：删除旧报告变量、链接、产物检查和插件使用说明，迁移验收统一为 Pipeline Summary、JUnit、Allure 和质量机器数据。
- 为三份已跟踪且容易误当现行方案的 `dev/` 旧设计文档增加历史/已替代标记；两份原有未跟踪设计资料保持不变，未纳入本任务。

## 验证结果

- 删除边界、Jenkins、Pipeline Reporting 定向测试：`26 passed`。
- `tests/quality`：`309 passed`。
- 全量离线测试新基线：`559 passed`。
- Smoke collect-only：`41 collected`，并发池 `15`，串行池 `26`。
- 活动文档旧报告推荐扫描：零命中。
- `git diff --check` 通过。
- 未调用真实付费接口。
