# P0 迭代三：归并、完整性与失败分类详细开发方案

## 1. 文档定位

本方案用于指导 P0 迭代三“归并、完整性与失败分类”的代码开发，输入依据按以下层级理解：

1. `dev/P0与P1质量工程后续开发方向.md`：控制 P0/P1 总体节奏、原始事实与派生结论的边界。
2. `dev/P0质量数据底座详细开发方案.md`：控制归并、JUnit 对账、完整性校验、失败分类和失败指纹目标。
3. `dev_plan/P0迭代一契约与身份详细开发方案.md`：冻结 `quality.v1` 模型、身份、脱敏、指纹和存储基础。
4. `dev_plan/P0迭代二双层采集与分片写入详细开发方案.md`：冻结 Case、Request、Integrity 分片及 `run_id`、`execution_id`、worker 语义。
5. 当前框架代码：控制 pytest、xdist、JUnit、并串混合执行和原退出码的真实行为。
6. 本文件：把迭代三细化为可直接开发的模块职责、数据流、唯一键、完整性状态机、分类规则、测试矩阵和实施顺序。

当总体方案中的早期示例与已经确认的迭代二语义冲突时，以已经落地的代码和本方案确认决策为准。例如：

- `execution_id` 使用 `parallel-pool`、`serial-pool`、`manual-pytest`，不增加 `-1` 序号。
- RequestMetric 不允许 orphan 或空 Case 身份；缺少上下文写 `missing_case_context`。
- 一个测试函数对应稳定 `case_id`，参数实例对应 `invocation_id`。

本文属于开发方案文档，不是代码变更。本次输出不写入 `code_history/`；后续按本方案完成代码开发后，再单独生成迭代三代码变更记录。

## 2. 已确认决策

### 2.1 契约与版本

- 保留已经冻结的 `quality.v1` 公共模型，不给 `FailureRecord` 增加 `rule_version` 字段。
- 新增 `merged/manifest.json`，统一记录本次 merge 使用的归并、分类和指纹算法版本。
- 一次 merge 只能使用一套分类规则和一套指纹算法，禁止在同一个 failures 文件中混用版本。
- manifest 是派生事实集的提交清单，不替代 `run.json`、CaseResult、RequestMetric、FailureRecord 或 IntegrityIssue。

建议第一版版本常量：

```text
manifest_version = quality.merge.v1
merge_version = p0-merge.v1
classifier_rule_version = p0-classifier.v1
fingerprint_version = p0-fingerprint.v1
```

### 2.2 失败 occurrence 与失败指纹

- 每一次真实失败 occurrence 都必须保留。
- FailureRecord 唯一候选键为 `failure_id + invocation_id + phase`。
- `failure_id` 是相同失败模式的聚合键，不是删除其他受影响用例的唯一键。
- 完全相同的 occurrence 可在重复扫描时幂等去重。
- 同一 occurrence 内容冲突时生成完整性问题，不能静默覆盖。

### 2.3 JUnit 证据

- `QUALITY_ENABLE=1` 时，`run_master.py` 默认保证正式执行阶段存在 `--junitxml`。
- 调用方已提供 JUnit 路径时继续使用原路径和现有阶段后缀规则。
- 调用方未提供时，自动使用质量输出目录下的自有 JUnit 路径。
- pytest 插件向 JUnit testcase 增加 `quality_case_id` 和 `quality_invocation_id` property。
- JUnit 提供异常类型、消息、断言位置和用例级对账证据，但不取代 CaseResult 的身份和 phase 事实。
- 不根据 testcase 的 `classname + name` 猜测 invocation 归属；缺少显式 property 时标记完整性降级。

### 2.4 分类原则

- 第一版只做确定性、可解释规则，不做概率模型或 AI 归因。
- 普通 `AssertionError` 无法区分产品缺陷和测试预期错误时，归入 `UNKNOWN`。
- 分类优先级固定为：`CONFIGURATION`、`FRAMEWORK_DEFECT`、`ENVIRONMENT`、`TEST_DEFECT`、`TRANSIENT`、`PRODUCT_DEFECT`、`UNKNOWN`。
- 任何失败都必须生成 FailureRecord；证据不足时使用 `UNKNOWN`，不能丢失失败。
- 分类失败本身不得替换测试失败，必须转化为 IntegrityIssue。

### 2.5 迭代边界

- 本迭代生成 merged 事实集、manifest、run.json、FailureRecord 和 IntegrityIssue。
- 本迭代不生成最终 `summary.json`、`gate-report.json`、`gate-report.md`。
- 本迭代不实现影子门禁、不修改 Jenkinsfile、不改变 CI 退出码。
- 本迭代可以计算对账所需的内部计数，但不提前输出迭代四的最终质量报告。

## 3. 需求复述

迭代二已经解决：

```text
pytest 与请求事实如何采集
-> 如何关联 Case 与 Request
-> 如何在多线程和 xdist 下安全分片
```

迭代三要解决：

```text
多个分片是否属于当前 run
-> 每条记录是否合法
-> 重复记录是重复采集还是真实多次发生
-> Case 数量是否与预收集和 JUnit 一致
-> 每个失败能否提取证据并形成稳定指纹
-> 每个失败能否进入明确责任类别或 UNKNOWN
-> 最终事实集是否完整、可复算、可追溯
```

最终目标不是“把几个 JSONL 拼起来”，而是生成一个满足以下条件的唯一事实集：

- 只包含目标 `run_id` 的记录。
- 记录通过 `quality.v1` 模型重新校验。
- 重复和冲突有确定性处理方式。
- Case invocation 可以和 JUnit 显式对账。
- 每个 failed/error phase 都有 FailureRecord。
- 每个 FailureRecord 都可以追溯分类规则、指纹算法和证据来源。
- 数据缺失或损坏不会被误认为通过。
- merge 失败不会改变 pytest 原结果和 `run_master.py` 原退出码。

## 4. 第一性原理与因果链

### 4.1 分片存在不等于事实可信

```text
磁盘上存在 JSONL
-> 可能混入旧 run、半行、损坏 JSON、错误 schema 或重复数据
-> 直接拼接会把存储现象当成业务事实
-> 统计、分类和门禁建立在污染数据上
-> 质量结论失信
```

因此 merge 必须同时承担扫描、过滤、模型校验、去重、冲突检测和来源记录，不能使用简单文件拼接。

### 4.2 去重必须区分“重复数据”和“重复问题”

```text
多个用例出现相同 failure_id
-> 表示同一失败模式影响多个 invocation
-> 如果只按 failure_id 保留一条
-> 影响范围和发生次数丢失
-> 后续报告无法回答哪些用例受影响
```

因此 occurrence 层保留每次真实失败，problem 层使用 `failure_id` 聚合同类问题；merge 只删除完全相同的重复 occurrence。

### 4.3 分类是可升级算法，不是原始事实

```text
原始异常和请求事实不变
-> 分类规则可能因误判修正而升级
-> 同一原始事实可能得到不同 category
-> 如果没有规则版本
-> 无法解释历史差异，也无法可靠回放
```

因此 manifest 必须记录分类和指纹版本，原始 Case、Request、JUnit 证据保持不变，允许未来重新计算派生结论。

### 4.4 JUnit 是证据源，不是身份源

```text
JUnit 默认只有 classname + name
-> 参数化、类方法、路径转换可能产生歧义
-> 通过字符串反推 nodeid 可能错误关联
-> 错误证据进入错误 invocation
-> 分类和指纹全部失真
```

因此由 pytest 插件显式写入 `quality_case_id`、`quality_invocation_id`。JUnit 负责提供用例级结果和异常证据，CaseResult 负责 phase 和质量身份。

### 4.5 质量归并不能成为新的测试故障源

```text
测试已经执行完成
-> merge 或分类代码抛异常
-> 如果覆盖 pytest exit code
-> 真实测试结果被质量模块替换
-> fail-open 原则被破坏
```

因此嵌入 `run_master.py` 的 merge 必须 fail-open：记录问题、尽最大努力生成 partial 产物，但最终仍返回或抛出原测试链路结果。

## 5. TOC 约束决策

当前约束已经从“事实如何采集”移动到“事实如何被证明为唯一且完整”。

| 决策点 | 选择 | TOC 原因 |
| --- | --- | --- |
| 首要约束 | 唯一归并与可对账 | 未归并的数据不能稳定进入分类、报告和门禁 |
| 身份策略 | 显式 ID，不猜测关联 | 错误关联比缺失关联更危险 |
| 损坏数据策略 | 尽量回收合法行，同时显式降级 | 全文件失败会丢失已知事实，静默跳过会掩盖风险 |
| 分类策略 | 高确定性优先，无法判断归 UNKNOWN | 降低把测试、环境问题误判为产品缺陷的成本 |
| occurrence 策略 | 全量保留，指纹只聚合 | 保持影响范围和真实发生次数 |
| 输出策略 | 原子覆盖、manifest 最后提交 | 防止消费者读取半完成事实集 |
| 集成策略 | run_master 自动 merge，退出码不变 | 建立稳定父级生命周期但不提前门禁 |
| 延后内容 | summary、Markdown、影子门禁、Jenkins | 避免同时建设事实层和决策展示层 |

优先级固定为：

```text
不改变原测试行为
> 不产生错误归属
> 不丢失真实 occurrence
> 明确暴露完整性问题
> 分类覆盖率
> 聚合丰富度
```

## 6. 当前代码基线

### 6.1 已完成能力

当前 `quality/` 已具备：

- `quality.v1` 的 RunRecord、CaseResult、RequestMetric、FailureRecord、QualitySummary、GateDecision、IntegrityIssue 模型。
- 稳定身份、failure fingerprint、脱敏、规范化和 canonical hash 工具。
- 原子 JSON 写入、append-only JSONL 和严格 JSONL 读取。
- pytest CaseResult、HTTP/SSE/polling/retry RequestMetric 和 IntegrityIssue worker 分片。
- xdist worker 文件所有权、ContextVar 跨线程传播和 fail-open 采集。
- `run_master.py` 父级 `run_id` 和语义化 `execution_id` 编排。

### 6.2 当前缺口

- 没有 aggregator、classifier、JUnit parser 和 merge CLI。
- 没有最终 `run.json` 生命周期和 merged JSONL 原子重写。
- 没有当前 run 过滤、逐行恢复、模型重校验、去重和冲突检测。
- CaseResult 没有异常类型、消息和断言位置，JUnit 也没有质量 identity property。
- 没有 JUnit 数量、身份和状态对账。
- 没有 FailureRecord 分类、failure_id 回填、完整性状态和 manifest。
- `run_master.py` 所有测试阶段结束后不会自动 merge。

### 6.3 必须保留的现有行为

- collect-only 不生成正式质量身份和质量产物。
- 并行阶段失败后仍继续执行串行池。
- `_run_pytest(args)`、`_run_serial_pool(args)` 继续返回 pytest exit code。
- 调用方提供的 JUnit 路径继续生成现有 `-parallel`、`-serial` 后缀。
- pytest、xdist、Allure、JUnit、线程池、重试、轮询和 SSE 原生语义不变。
- `QUALITY_ENABLE=0` 时不创建 run、默认 JUnit、merged 或 manifest 产物。

## 7. 迭代范围与非侵入边界

### 7.1 本迭代包含

- 父级 RunRecord 初始化和最终状态写入。
- 默认 JUnit 参数补全和精确路径跟踪。
- JUnit quality properties 注入、XML 解析和证据提取。
- Case、Request、Integrity 分片扫描、逐行恢复和模型校验。
- 当前 run 过滤、唯一键去重和冲突检测。
- invocation 级 Case 状态折叠和 JUnit 对账。
- failed/error phase 的确定性分类与指纹生成。
- FailureRecord 生成及 merged CaseResult.failure_id 回填。
- merged 事实集、manifest 和 run.json 原子输出。
- `quality.cli merge` 手工恢复和回放入口。
- `run_master.py` 正常、失败和异常路径的 fail-open merge。

### 7.2 本迭代不包含

- 不生成最终 QualitySummary 文件。
- 不计算或输出 P95/P99、接口排行榜、趋势和历史基线。
- 不实现 retry rescue rate；当前 RequestMetric 没有独立逻辑请求组 ID，不能猜测请求链。
- 不生成 GateDecision、Markdown 报告或影子门禁。
- 不修改 Jenkinsfile、邮件、artifact 或构建状态。
- 不实现用例级自动重跑、Flaky 状态机和跨构建查询。
- 不实现人工分类修正流程。
- 不解析 Allure HTML 或反向抓取 Allure 页面。
- 不修改业务测试的断言和请求调用形式。

## 8. 总体架构

### 8.1 运行级数据流

```text
run_master 启动正式运行
-> 创建 initial run.json(status=partial)
-> 预收集用例并记录 expected_case_count
-> 确保每个正式 pytest 阶段有 JUnit 路径
-> pytest/xdist 写 worker shards 和 JUnit
-> 所有计划阶段结束或父级异常进入 finally
-> aggregator 扫描当前 run 分片
-> JUnit parser 读取显式 invocation property
-> 去重、对账、完整性校验
-> classifier 生成 FailureRecord
-> 原子写 merged JSONL
-> 最后写 complete/failed manifest
-> 更新 final run.json
-> 返回原 pytest 退出码或重新抛出原异常
```

### 8.2 组件关系

```text
quality.pytest_plugin
  └─ JUnit properties

quality.junit
  └─ JUnitCaseEvidence

quality.aggregator
  ├─ shard scanner
  ├─ model validator
  ├─ deduplicator
  ├─ reconciler
  ├─ integrity evaluator
  └─ merged writer

quality.classifier
  ├─ error extractor
  ├─ deterministic rules
  ├─ owner/confidence mapping
  └─ FailureRecord builder

quality.cli
  └─ merge_quality_run()

run_master
  ├─ run lifecycle
  ├─ expected stage/case/JUnit inputs
  └─ fail-open final merge
```

### 8.3 职责边界

| 模块 | 负责 | 不负责 |
| --- | --- | --- |
| `pytest_plugin.py` | 写 Case 分片、注入 JUnit identity property | 最终分类和 merge |
| `junit.py` | XML 解析、状态和异常证据提取 | 生成质量分类 |
| `classifier.py` | 单个 failure occurrence 的分类与指纹 | 扫描文件和写最终产物 |
| `aggregator.py` | 扫描、校验、去重、对账、调用分类器、写 merged | 门禁和 Markdown 报告 |
| `cli.py` | 参数解析和手工 merge | 复制核心算法 |
| `run_master.py` | 父级生命周期和 merge 调度 | 直接实现分类规则 |

## 9. 目标文件结构

建议新增：

```text
quality/
  aggregator.py
  classifier.py
  junit.py
  cli.py

tests/quality/
  test_quality_aggregator.py
  test_quality_classifier.py
  test_quality_junit.py
  test_quality_cli.py
```

建议修改：

```text
quality/storage.py
quality/pytest_plugin.py
quality/__init__.py
run_master.py
tests/quality/test_quality_storage.py
tests/quality/test_quality_pytest_plugin.py
tests/quality/test_quality_run_master.py
tests/test_master_service_parallel_serial.py
```

除非开发中发现已冻结模型无法加载既有合法分片，否则不修改 `quality/models.py`。

## 10. 最终产物结构

```text
reports/quality/
  run.json
  junit/
    quality.xml
    quality-parallel.xml
    quality-serial.xml
  shards/
    cases-{execution_id}-{worker_id}.jsonl
    requests-{execution_id}-{worker_id}.jsonl
    integrity-{execution_id}-{worker_id}.jsonl
  merged/
    manifest.json
    case-results.jsonl
    request-metrics.jsonl
    failures.jsonl
    integrity-issues.jsonl
```

说明：

- `junit/` 只用于调用方未显式提供 `--junitxml` 时的默认位置。
- 调用方显式提供的 JUnit 可以位于 `reports/` 其他路径，manifest 必须记录真实路径。
- merged 文件始终是当前目标 `run_id` 的唯一事实集，不追加历史 run。
- 原始 shards 永不因 merge 被删除或改写。
- `manifest.json` 是当前 merged 快照的提交标记，必须最后完成。

## 11. Merge Manifest 设计

### 11.1 建议结构

```json
{
  manifest_version: quality.merge.v1,
  schema_version: quality.v1,
  run_id: run-id,
  status: merging|complete|failed,
  merge_version: p0-merge.v1,
  classifier_rule_version: p0-classifier.v1,
  fingerprint_version: p0-fingerprint.v1,
  created_at: 2026-07-30T16:30:00+08:00,
  expected_execution_ids: [parallel-pool, serial-pool],
  expected_case_count: 90,
  source_shards: [],
  junit_files: [],
  output_counts: {
    case_results: 270,
    invocations: 90,
    request_metrics: 180,
    failure_occurrences: 3,
    failure_fingerprints: 2,
    integrity_issues: 1
  },
  output_hashes: {}
}
```

### 11.2 source_shards

每个扫描文件至少记录：

- 相对或规范化路径。
- 类型：`cases`、`requests`、`integrity`。
- SHA-256 文件摘要。
- 物理非空行数。
- 当前 run 合法记录数。
- foreign run 记录数。
- invalid JSON、invalid schema 数。
- exact duplicate 和 conflict duplicate 数。

不得把损坏原始行、请求 body、header 或完整异常写入 manifest。

### 11.3 提交语义

- merge 开始时先原子写入 `status=merging` 的 manifest。
- merged 文件逐个原子替换。
- 所有输出完成并计算 hash 后，再原子写入 `status=complete` manifest。
- 无法完成核心输出时写 `status=failed`；若 failed manifest 也无法写，只输出脱敏 warning。
- 消费者只有在 manifest 为 `complete` 且输出 hash 匹配时，才把 merged 目录视为完整快照。

## 12. RunRecord 生命周期

### 12.1 initial run.json

正式质量运行开始时写：

```text
status = partial
integrity_status = degraded
end_time = null
integrity_issues = []
```

这样进程被中断或 merge 未执行时，不能残留一个看起来已经完成的旧 run.json。

### 12.2 元数据来源

| 字段 | 来源 |
| --- | --- |
| `run_id` | 迭代二现有父级 run_id |
| `job_name` | `JOB_NAME`，可空 |
| `build_number` | `BUILD_NUMBER`，可空 |
| `branch` | `BRANCH_NAME`，回退 `GIT_BRANCH` |
| `commit_sha` | `GIT_COMMIT` |
| `trigger` | Job 和 Build 都存在时为 `jenkins`，否则为 `local`；CLI 回放为 `manual` |
| `environment` | `USE_CHINA_ENVIRONMENT=TRUE` 为 `china`，FALSE 为 `overseas`，无法判断为 `unknown` |
| `start_time` | 父级正式质量运行开始时间，timezone-aware UTC |

不得为了读取环境名而强制初始化业务 Settings，避免缺少 API Key 时连 run.json 都无法生成。

### 12.3 final run.json

- 所有计划 pytest 阶段正常返回，即使测试失败，运行状态仍为 `finished`。
- KeyboardInterrupt、SystemExit 或父级执行被明确中断时为 `interrupted`。
- 收集或执行发生非正常父级异常、只完成部分计划阶段时为 `partial`。
- `integrity_status` 由 aggregator 状态机给出，不根据 pytest exit code 推断。
- `integrity_issues` 保存本次 merged 后的完整性问题，按确定性顺序输出。
- `end_time` 在最终写入时记录。

测试失败和数据完整性失败是两个正交维度：

```text
pytest failed + integrity complete
  = 测试确实失败，但质量事实可信

pytest passed + integrity failed
  = 测试看起来通过，但质量事实不足以形成可信结论
```

## 13. 默认 JUnit 参数设计

### 13.1 参数补全

新增内部 helper：

```text
_ensure_quality_junit_args(pytest_args, quality_config)
```

规则：

- quality 禁用时原样返回。
- 已存在 `--junitxml path` 或 `--junitxml=path` 时原样保留。
- 未提供时追加 `--junitxml={QUALITY_OUTPUT_DIR}/junit/quality.xml`。
- 随后继续复用 `_replace_junitxml_suffix()`：parallel 使用 `quality-parallel.xml`，serial 使用 `quality-serial.xml`，整体串行使用 `quality.xml`。

### 13.2 路径跟踪

- 每个实际执行阶段从最终 pytest args 中解析精确 JUnit 路径。
- 相对路径按项目根目录解析。
- run_master 把实际执行过的 JUnit 路径传给 aggregator。
- aggregator 不使用通配符扫描所有 `reports/*.xml`，避免读入旧构建文件。
- JUnit 文件修改时间早于本次 run start 时视为 stale，不参与证据关联。

### 13.3 行为兼容

- 不删除调用方原有 JUnit 文件。
- 不改变 pytest 的 JUnit family 和 testcase 表达。
- 只增加 property，不修改 failure/error/skipped 节点。
- 默认 JUnit 只在 `QUALITY_ENABLE=1` 时出现。
- JUnit 生成或解析失败不修改 pytest exit code。

## 14. JUnit Identity Property

### 14.1 注入时机

在 `pytest_runtest_protocol` 成功构建 `QualityCaseContext` 后，把以下安全属性加入 `item.user_properties`：

```text
quality_case_id = case_context.case_id
quality_invocation_id = case_context.invocation_id
```

必须在 setup report 生成前完成，保证 setup error 也能进入 JUnit property。

### 14.2 安全约束

- 不写原始 pytest 参数。
- 不写 Authorization、Cookie、API Key、请求体或响应体。
- 不新增 `quality_nodeid`；JUnit 自身已有 testcase name，额外复制可能放大参数 ID 泄露。
- 已存在同名同值 property 时不重复追加。
- 同名 property 值冲突时保留质量插件生成值，并记录 `junit_property_conflict`。

### 14.3 xdist

pytest-xdist 会把 worker TestReport 的 `user_properties` 传回 controller，JUnit 插件在 controller 侧输出 property。必须增加真实 xdist 测试，不能只用单进程 mock 推断。

## 15. JUnit Parser 设计

### 15.1 技术选择

- 使用 Python 标准库 `xml.etree.ElementTree`。
- 不新增 lxml 或其他 XML 依赖。
- 只解析本地 pytest 生成的 JUnit，不支持外部不受控 XML 扩展。
- 禁止解析外部实体；标准 ElementTree 默认不展开网络外部实体。

### 15.2 内部证据结构

`quality.junit` 使用内部 frozen dataclass：

```text
JUnitCaseEvidence
  source_path
  suite_name
  invocation_id
  case_id
  classname
  name
  status
  duration_ms
  error_type
  message
  traceback_text
  phase_hint
```

该结构是解析过程数据，不作为 `quality.v1` 公共产物直接落盘。

### 15.3 状态映射

| JUnit 节点 | JUnit 状态 |
| --- | --- |
| 无 failure/error/skipped | `passed` |
| `<failure>` | `failed` |
| `<error>` | `error` |
| `<skipped>` | `skipped` |

如果同一 testcase 同时出现多个 failure/error 子节点，全部作为证据读取，并生成 `junit_multiple_failures`；不能只取第一个后静默丢弃。

### 15.4 异常类型提取优先级

1. failure/error 节点 `type` 属性。
2. `message` 中标准 `ExceptionType: message` 异常前缀。
3. setup/teardown failure message 内部的异常类型。
4. traceback 最后一个稳定异常行。
5. 无法提取时使用 `UnknownError`。

保存时统一使用稳定短类名：

```text
requests.exceptions.ReadTimeout -> ReadTimeout
builtins.AssertionError -> AssertionError
```

### 15.5 phase_hint

- message 明确包含 `failed on setup` -> setup。
- message 明确包含 `failed on teardown` -> teardown。
- `<failure>` 默认提示 call，但最终 phase 仍以 CaseResult 失败 phase 为准。
- 无法判断时为 null，不通过 traceback 文本猜测 phase。

### 15.6 缺少 identity property

- testcase 仍计入 JUnit 总数。
- 不根据 classname/name 猜测 invocation_id。
- 记录 `junit_identity_missing`。
- 该 testcase 不能用于单 invocation 分类证据，只能用于总量对账。

## 16. 分片发现与扫描

### 16.1 扫描范围

只扫描 `QUALITY_OUTPUT_DIR/shards/` 下：

```text
cases-*.jsonl
requests-*.jsonl
integrity-*.jsonl
```

不扫描 `merged/`、JUnit、Allure、日志、未知前缀文件或不存在的 failure shard。

### 16.2 当前 run 过滤

- 每条解析成功的记录必须先完成 `quality.v1` 模型校验。
- `record.run_id == target_run_id` 才进入当前事实集。
- foreign run 记录不进入 merged 输出。
- 每个文件只生成一条 foreign run 汇总问题，记录数量，不逐行制造噪声。
- foreign run 通常为 `info`，因为固定分片名可能保留未参与本次执行的旧 worker 文件。

### 16.3 分片三件套

同一个 `{execution_id}-{worker_id}` 初始化时应同时存在 Case、Request 和 Integrity 三个文件。

- Case 分片存在但 Request 或 Integrity 同 suffix 缺失，记录 `shard_peer_missing`。
- Request 分片可以为空，因为并非每个测试都发请求。
- Integrity 分片可以为空，但文件应存在。
- expected execution 没有任何当前 run Case 分片，记录 `execution_shards_missing`。
- xdist controller 不要求 master 分片。

### 16.4 逐行容错

现有 `read_jsonl()` 保持严格语义。aggregator 新增私有或存储层容错扫描能力：

```text
文件按二进制逐行读取
-> 单行 UTF-8 解码
-> json.loads
-> 对应 Pydantic 模型校验
-> run_id 过滤
-> 收集合法记录和问题
```

单行损坏不能阻止同一文件后续合法行恢复。

### 16.5 错误信息安全

完整性问题只记录文件名、行号、异常类型和脱敏字段级摘要。禁止记录损坏原始行全文。

同一文件同一错误代码最多生成一条聚合问题，message 保存总数和前若干行号，避免 run.json 被大量错误淹没。

## 17. 去重与冲突处理

### 17.1 CaseResult

唯一候选键：`invocation_id + phase`。

- 同键且 canonical 内容完全相同：保留一条，manifest 增加 exact duplicate 计数。
- 同键但内容不同：记录 `case_duplicate_conflict`。
- 冲突时按 `start_time` 最早、再按 source path 和 line number 排序选择 canonical 记录。
- 不因 execution_id 或 worker_id 不同保留两个同 invocation phase；这表示重复执行或身份冲突。

### 17.2 RequestMetric

唯一候选键：`request_event_id`。

- 同 ID 同内容：幂等去重。
- 同 ID 不同内容：记录 `request_duplicate_conflict`。
- RequestMetric 没有开始时间，冲突时按 source path 和 line number 确定性选取，不猜测哪个更真实。

### 17.3 IntegrityIssue

IntegrityIssue 没有 issue_id，exact duplicate key 使用全部规范化字段：

```text
run_id + severity + source + code + message + related_id + created_at
```

只有全部字段相同才去重。相同 code 的多次真实问题不能合并丢失；aggregator 自己产生的批量问题应在生成时聚合。

### 17.4 FailureRecord

唯一候选键：`failure_id + invocation_id + phase`。

- 同 failure_id 不同 invocation：全部保留。
- 同 invocation 不同 phase：全部保留。
- 相同 occurrence 同内容：幂等去重。
- 相同 occurrence 内容冲突：记录 `failure_duplicate_conflict`。

### 17.5 输出顺序

- CaseResult：`case_id, invocation_id, phase_order`。
- RequestMetric：`invocation_id, interface_id, attempt_index, request_event_id`。
- FailureRecord：`failure_id, invocation_id, phase_order`。
- IntegrityIssue：`created_at, severity, source, code, related_id`。

排序只用于稳定序列化，不表示 RequestMetric 的真实时间顺序。

## 18. Invocation 折叠与 JUnit 对账

### 18.1 为什么按 invocation 对账

CaseResult 是 phase 级事实，一个正常用例通常产生 setup、call、teardown 三条记录；JUnit testcase 是 invocation 级事实。因此不能比较 CaseResult 行数和 JUnit tests 数。

对账单位固定为：

```text
unique invocation_id
```

### 18.2 invocation 最终状态

聚合器从 canonical phase 记录计算内部 invocation 状态，优先级为：

```text
setup/teardown error
> call failed
> strict xpass 形成的 failed
> xpassed
> passed
> xfailed
> skipped
```

具体规则：

- setup error：invocation 为 error；通常没有 call。
- call failed：invocation 为 failed；若 teardown 同时 error，最终对账状态为 error，但两个 failure occurrence 都保留。
- teardown error：即使 call passed，invocation 仍为 error。
- xfailed 与普通 skipped 对 JUnit 都映射为 skipped，但质量内部继续保留差异。
- 非严格 xpassed 对 JUnit 映射为 passed；严格 xpass 在 pytest report 中已经表现为 failed。

### 18.3 三方数量对账

比较三个数字：

1. `expected_case_count`：run_master 预收集数量。
2. `merged_invocation_count`：当前 run canonical invocation 数量。
3. `junit_testcase_count`：本次实际 JUnit testcase 数量。

规则：

- 三者一致：数量完整。
- merged 少于 expected：可能丢分片或 case 写入失败，属于关键完整性错误。
- merged 多于 expected：可能重复执行、身份算法错误或混入错误 run。
- JUnit 与 expected/merged 不一致：记录 `junit_count_mismatch`。
- 调用阶段因 collection 或父级异常未完成时，expected 数仍保留，但 RunStatus 为 partial。

### 18.4 identity 对账

- 每个带 `quality_invocation_id` 的 JUnit testcase 应能找到 merged invocation。
- 每个 merged invocation 应有且只有一个 JUnit testcase。
- 一个 invocation 出现在多个 JUnit 文件时记录 `junit_invocation_duplicate`。
- JUnit case_id 与 CaseResult case_id 不一致时记录 `junit_case_identity_mismatch`。
- identity 缺失时不能使用 classname/name 回退。

### 18.5 状态对账

| 质量 invocation 状态 | 期望 JUnit 状态 |
| --- | --- |
| passed、xpassed | passed |
| failed | failed |
| error | error |
| skipped、xfailed | skipped |

不一致时记录 `junit_status_mismatch`，并同时保留 Case 和 JUnit 两侧状态。CaseResult 不被 JUnit 覆盖，JUnit 也不被质量模块改写。

## 19. Failure Candidate 生成

### 19.1 生成范围

以下 canonical phase 生成 failure candidate：

- `raw_status=failed`。
- `raw_status=error`。
- `final_status=failed` 或 `final_status=error`，即使未来 raw/final 不同。

以下不生成 FailureRecord：

- passed。
- skipped。
- xfailed。
- 非严格 xpassed。
- collection failure；它没有合法 case/invocation，继续作为 IntegrityIssue。

### 19.2 多 phase 失败

同一 invocation 可能同时发生：

```text
call failed
+ teardown error
```

必须生成两条 FailureRecord，并使用不同 phase 参与 failure fingerprint。不能只保留最终 invocation 状态。

### 19.3 证据选择

对每个 candidate 收集：

- CaseResult phase 和状态。
- 同 invocation 的 JUnit failure/error 证据。
- 同 invocation 的 RequestMetric 列表。
- 同 invocation 或 case 相关 IntegrityIssue。
- JUnit 文件和 Case 分片的安全证据引用。

JUnit phase_hint 与 CaseResult phase 一致时优先关联；无法唯一选择 JUnit evidence 时不猜测，使用缺失证据回退并记录 `failure_evidence_ambiguous`。

### 19.4 缺失证据回退

即使没有可关联 JUnit evidence，也必须生成：

```text
error_type = UnknownError
normalized_message = failure evidence unavailable
category = UNKNOWN
owner_domain = unknown
confidence = low
```

同时记录 `failure_evidence_missing`。这样“所有失败可路由”和“证据缺失可见”可以同时满足。

## 20. 分类器实现约束

具体 FailureEvidence 输入、规则输出和七类分类证据表见第 38 节。

### 20.1 规则实现方式

- 分类规则使用有序、不可变的 rule 列表。
- 每条规则必须有稳定 `rule_id`，例如 `configuration.missing_required_env`。
- 第一条满足全部条件的规则获胜。
- 规则只读取结构化字段和规范化文本，不读取原始请求 body、header 或完整响应。
- 规则中的关键词集合集中定义，不散落在 if/else 中。
- 规则顺序和关键词变化必须升级 `CLASSIFIER_RULE_VERSION`。
- 每条规则至少有正例、反例和边界测试。

### 20.2 请求证据选择

- 只读取同 invocation 的 RequestMetric。
- 当且仅当存在唯一、明确相关的失败 interface 时，写入 fingerprint source 的 `interface_id`。
- 多个失败 interface 无法判断主因时，`interface_id=None`，不能根据文件顺序猜测。
- HTTP 2xx 普通请求仍保持 success；业务正文错误继续由 Case 断言表达。
- polling、retry、SSE 继续沿用迭代二的原始 business_status，不在 classifier 中重解释协议语义。

### 20.3 fail-open

单个 candidate 分类抛异常时：

1. 记录 `failure_classification_failed`。
2. 使用 UNKNOWN fallback 生成 FailureRecord。
3. 继续处理其他 failure occurrence。

禁止因为一条失败无法分类而放弃整个 merged 事实集。

## 21. 失败指纹与 Case 回填

### 21.1 规范化输入

继续复用迭代一现有：

- `normalize_failure_message()`。
- `build_failure_message_hash()`。
- `build_failure_fingerprint()`。

指纹输入固定为：

```text
phase
+ error_type
+ normalized_message
+ unambiguous interface_id
+ assert_location
```

### 21.2 assert_location

从 JUnit traceback 中提取最后一个稳定、仓库内 Python 位置：

```text
module/test_x.py:88
```

规则：

- 转换为相对项目路径并统一 `/`。
- 去除列号、临时目录前缀和随机段。
- 优先测试断言位置，不选择 pytest、site-packages 内部帧。
- 无法可靠提取时为 null。
- 不读取或落盘完整 traceback。

### 21.3 FailureRecord 构建

每个 occurrence 生成：

- `run_id`、`case_id`、`invocation_id`、`phase`。
- `failure_id`。
- `category`、`owner_domain`、`confidence`。
- `error_type`、`normalized_message`。
- `FailureFingerprintSource`。
- `manual_override=None`。

### 21.4 CaseResult.failure_id 回填

原始 shard 不改写。aggregator 在 merged CaseResult 中使用 `model_copy(update=...)`：

- failed/error phase 填入对应 `failure_id`。
- 非失败 phase 保持 null。
- 一个 phase 必须只对应一个 occurrence。
- 如果同一 phase 产生多个不可区分 evidence，仍只生成一个 UNKNOWN FailureRecord，并记录证据歧义问题。

### 21.5 聚合语义

`failure_id` 相同表示同一规范化失败模式，迭代四可以据此计算：

- occurrence 次数。
- 受影响 invocation 数。
- 受影响 case 数。
- 受影响 interface 数。

本迭代只保证数据可聚合，不提前生成问题排行榜或趋势报告。

## 22. 完整性问题设计

### 22.1 分片与模型

| code | severity | 状态影响 | 含义 |
| --- | --- | --- | --- |
| `shards_directory_missing` | error | failed | 没有可扫描分片目录 |
| `execution_shards_missing` | error | failed | 计划执行阶段没有当前 run Case 分片 |
| `shard_peer_missing` | warn/error | degraded | worker 三件套缺失成员 |
| `shard_read_failed` | error | degraded/failed | 文件无法读取 |
| `shard_utf8_invalid` | error | degraded/failed | 行无法 UTF-8 解码 |
| `shard_json_invalid` | error | degraded/failed | JSONL 行损坏 |
| `shard_schema_invalid` | error | degraded/failed | 记录不符合 `quality.v1` |
| `foreign_run_records_ignored` | info | 无 | 忽略其他 run 记录 |

是否 failed 取决于损坏是否导致关键 Case invocation 缺失；不能只根据 severity 机械映射。

### 22.2 去重与身份

| code | severity | 状态影响 | 含义 |
| --- | --- | --- | --- |
| `case_duplicate_conflict` | error | failed | 同 invocation phase 出现冲突事实 |
| `request_duplicate_conflict` | error | degraded | 同 request_event_id 内容冲突 |
| `failure_duplicate_conflict` | error | degraded | 同 failure occurrence 内容冲突 |
| `case_identity_inconsistent` | error | failed | invocation 对应多个 case_id/param 身份 |
| `request_case_missing` | error | degraded | RequestMetric 无法找到 merged Case invocation |

RequestMetric 模型本身要求 case/invocation 非空；这里校验的是引用对象是否存在。

### 22.3 JUnit 与对账

| code | severity | 状态影响 | 含义 |
| --- | --- | --- | --- |
| `junit_missing` | warn | degraded | 预期 JUnit 文件未生成 |
| `junit_stale` | warn | degraded | 文件早于本次运行 |
| `junit_parse_failed` | error | degraded | XML 无法解析 |
| `junit_identity_missing` | warn | degraded | testcase 缺少 quality invocation property |
| `junit_property_conflict` | error | degraded | 同名 quality property 值冲突 |
| `junit_invocation_duplicate` | error | failed | 同 invocation 出现多个 testcase |
| `junit_case_identity_mismatch` | error | failed | JUnit 和 Case 的 case_id 不一致 |
| `junit_count_mismatch` | error | failed | expected、merged、JUnit 数量不一致 |
| `junit_status_mismatch` | error | degraded | invocation 状态不一致 |
| `junit_multiple_failures` | warn | degraded | 单 testcase 存在多个失败证据 |

### 22.4 分类与输出

| code | severity | 状态影响 | 含义 |
| --- | --- | --- | --- |
| `failure_evidence_missing` | warn | degraded | 失败没有可关联 JUnit 证据 |
| `failure_evidence_ambiguous` | warn | degraded | 多份证据无法唯一选择 |
| `failure_classification_failed` | error | degraded | 分类器异常并使用 UNKNOWN fallback |
| `failure_record_missing` | error | failed | failed/error phase 没有 FailureRecord |
| `merged_output_write_failed` | error | failed | 核心 merged 文件写入失败 |
| `manifest_write_failed` | error | failed | manifest 无法提交 |
| `run_record_write_failed` | error | failed | run.json 无法初始化或完成 |
| `parent_collection_failed` | error | failed | run_master 预收集失败 |
| `parent_execution_interrupted` | error | degraded/failed | 父级执行被中断 |

### 22.5 message 和 related_id

- message 只写组件、计数、文件名、行号和脱敏错误摘要。
- 不写原始 JSONL 行、完整 traceback、XML failure body、请求 body 或 header。
- Case 问题优先 related_id=`invocation_id`。
- Request 问题优先 related_id=`request_event_id`。
- 文件级问题 related_id 使用规范化相对路径。
- run 级问题 related_id 使用 `run_id`。

## 23. IntegrityStatus 状态机

### 23.1 complete

同时满足：

- 所有 expected execution 有当前 run Case 分片。
- expected、merged invocation、JUnit testcase 数一致。
- 所有带 identity 的 JUnit testcase 一一对应 merged invocation。
- 所有 failed/error phase 有 FailureRecord，包括 UNKNOWN。
- Case 和 Request 唯一键无冲突。
- 核心 merged 文件和 complete manifest 写入成功。
- 没有影响可信度的 warn/error 完整性问题。

### 23.2 degraded

核心事实集仍可使用，但存在明确缺口，例如：

- JUnit 缺失、stale、部分 testcase 缺少 identity。
- 非关键 Request 分片损坏，但 Case 对账仍完整。
- failure evidence 缺失，已使用 UNKNOWN fallback。
- JUnit 状态与 Case 状态不一致，但 invocation 数量和身份仍可确认。
- classifier 单条规则异常但 FailureRecord 已回退生成。

degraded 不能被解释为 complete，迭代四报告必须展示问题。

### 23.3 failed

任一关键条件成立：

- expected case 大于 0，但没有可用 Case invocation。
- expected、merged invocation 数量不一致。
- 同 invocation phase 存在冲突 Case 事实。
- JUnit identity 指向错误 Case 或重复 invocation。
- failed/error phase 无法生成 FailureRecord。
- merged Case、Request、Failure、Integrity 任一核心输出无法提交。
- manifest 无法标记完整快照。

### 23.4 状态计算顺序

```text
先收集全部 IntegrityIssue
-> 根据 code 和实际影响判定 critical conditions
-> critical 存在则 failed
-> 否则存在影响可信度的 warn/error 则 degraded
-> 否则 complete
```

不能简单使用“存在 error 就 failed”，因为某条无效 Request 行可能不影响 Case 事实；也不能简单使用“merge 有输出就 complete”。

## 24. Aggregator API 设计

### 24.1 输入模型

建议使用内部 frozen dataclass：

```text
QualityMergeRequest
  run_id
  output_dir
  expected_execution_ids
  expected_case_count
  junit_paths
  run_start_time
  seed_integrity_issues
```

约束：

- `run_id` 必填。
- output_dir 必须显式传入，不读取业务 Settings。
- expected execution 只包含实际计划并启动的正式阶段。
- expected_case_count 可以为空；CLI 手工回放缺失时用 JUnit 和 merged 自校验，但完整性最高只能 degraded。
- JUnit paths 必须是 run_master 实际使用路径或 CLI 显式参数。

### 24.2 输出模型

```text
QualityMergeResult
  run_id
  integrity_status
  case_results
  request_metrics
  failures
  integrity_issues
  invocation_count
  failure_fingerprint_count
  output_paths
  manifest
```

返回模型用于 run_master 最终写 RunRecord 和单元测试，不作为新的 `quality.v1` 公共数据契约。

### 24.3 公共入口

```text
merge_quality_run(request: QualityMergeRequest) -> QualityMergeResult
```

执行顺序固定为：

1. 验证 merge request。
2. 写 `status=merging` manifest。
3. 发现分片并校验三件套。
4. 容错扫描并过滤 run_id。
5. Case、Request、Integrity 去重。
6. 折叠 invocation。
7. 解析并对账 JUnit。
8. 生成 failure candidates。
9. 分类、生成 FailureRecord、回填 merged CaseResult.failure_id。
10. 计算完整性状态。
11. 原子写四个 merged JSONL。
12. 计算输出 hash。
13. 最后写 complete/failed manifest。
14. 返回 merge result。

### 24.4 纯函数边界

以下逻辑应拆成可独立测试的纯函数或小组件：

- `deduplicate_cases()`。
- `deduplicate_requests()`。
- `collapse_invocations()`。
- `reconcile_junit()`。
- `build_failure_candidates()`。
- `evaluate_integrity_status()`。
- `build_manifest()`。

文件扫描和写入留在 orchestrator，避免测试必须依赖大规模真实目录。

## 25. 原子输出与幂等性

### 25.1 write_jsonl_atomic

在 `quality/storage.py` 新增：

```text
write_jsonl_atomic(path, records) -> Path
```

要求：

- 父目录按需创建。
- 临时文件与目标文件同目录。
- 每个 record 使用现有 `_to_jsonable()`。
- 单行 JSON、UTF-8、LF、`allow_nan=False`、稳定 key 排序。
- flush + fsync 后 `os.replace()`。
- 替换失败时保留旧目标并清理临时文件。
- 空记录写出合法空文件，不写 JSON 数组。

### 25.2 输出提交顺序

```text
manifest(status=merging)
-> case-results.jsonl
-> request-metrics.jsonl
-> failures.jsonl
-> integrity-issues.jsonl
-> 计算所有输出 SHA-256
-> manifest(status=complete|failed)
-> final run.json
```

manifest 必须最后完成，因为多个文件无法形成真正跨文件原子事务。

### 25.3 失败恢复

- 写某个 merged 文件失败时，记录 `merged_output_write_failed`。
- 尽最大努力写 remaining integrity 和 failed manifest。
- 不删除原始 shards。
- 下次 CLI merge 可以从原始 shards 重新生成全部 merged 文件。
- complete manifest 的 output hash 必须能发现“旧 manifest + 新部分文件”的混合状态。

### 25.4 幂等性

相同：

```text
run_id
+ source shard 内容
+ JUnit 内容
+ merge/classifier/fingerprint 版本
```

重复 merge 应产生相同排序、相同 FailureRecord、相同业务内容 hash。允许 `created_at` 等 merge 元数据变化，但不得改变事实记录。

## 26. run_master.py 接入

### 26.1 顶层流程重构

当前非并行分支会直接 return。迭代三需要统一 finally merge，因此重构为单一父级收口，但保持外部函数签名不变：

```text
解析 pytest_args
-> 如果 collect-only：沿用现有无质量副作用流程并返回
-> 解析父级 quality config
-> quality 启用时创建 initial run.json
-> 预收集 cases
-> 计算 planned execution ids 和 expected_case_count
-> 补全默认 JUnit 参数
-> 执行 parallel/serial stages
-> 保存原 exit codes 或原异常
-> finally 执行 merge/finalize
-> 返回 _merge_exit_codes(results) 或重新抛出原异常
```

### 26.2 collect-only

- 必须在生成 run_id 和 run.json 前识别 collect-only。
- 继续调用现有预收集并打印并串池数量。
- 不注入默认 JUnit。
- 不创建 quality output、shards、merged 或 manifest。

### 26.3 planned execution ids

- 未启用并行：`serial-pool`。
- 并行池非空：包含 `parallel-pool`。
- 串行池非空：包含 `serial-pool`。
- 空池不加入 planned execution。
- 如果 parallel 异常导致 planned serial 未启动，aggregator 仍能看到 serial 计划缺失并标记 partial/failed。

### 26.4 pytest 正常失败

- pytest 返回 1、2、3、4、5 等 exit code 时，先按现有逻辑保存结果。
- 正常返回非零不阻止 merge。
- 并行阶段普通失败继续执行串行池，保持当前行为。
- final RunStatus 为 finished，IntegrityStatus 独立计算。
- 最终返回值仍使用现有 `_merge_exit_codes(results)`。

### 26.5 父级异常和中断

- `_run_pytest()` 或编排代码抛 Exception 时，记录原异常并进入 finally merge。
- 当前代码不会在 parallel 抛异常后继续 serial，本迭代不改变该语义。
- KeyboardInterrupt/SystemExit 标记 RunStatus.interrupted，merge 后重新抛出。
- 其他父级异常标记 RunStatus.partial，merge 后重新抛出原对象。
- merge 异常不得替换已有原异常。

### 26.6 merge fail-open

嵌入 run_master 的 merge 使用：

```text
try:
    result = merge_quality_run(...)
except Exception as quality_error:
    输出脱敏 warning
    尽最大努力写 partial/failed run.json
```

不能捕获后吞掉 KeyboardInterrupt/SystemExit，也不能因为 merge integrity=failed 修改 pytest exit code。

### 26.7 no tests 与 collection failure

- 预收集返回 0 个用例：保持现有返回 1；quality 启用时生成 `no_tests_collected`/failed integrity 产物。
- 预收集抛异常：记录 `parent_collection_failed`，RunStatus.partial，尽最大努力 merge 空事实集，然后重新抛出原 RuntimeError。
- 错误文本进入质量产物前必须脱敏，不复制完整 stdout/stderr。

### 26.8 环境恢复

- 继续复用 `_quality_stage_environment()`。
- 每个阶段结束恢复调用前 `QUALITY_*`。
- merge 使用父级 QualityRuntimeConfig，不依赖阶段环境仍然存在。
- 外部调用者预设的 `QUALITY_RUN_ID`、`QUALITY_OUTPUT_DIR` 最终保持不变。

## 27. CLI 设计

### 27.1 调用形式

```text
python -m quality.cli merge \
  --run-id <run-id> \
  --output-dir reports/quality \
  --expected-execution parallel-pool \
  --expected-execution serial-pool \
  --expected-case-count 90 \
  --junit reports/smoke-tests-parallel.xml \
  --junit reports/smoke-tests-serial.xml
```

PowerShell 使用对应的反引号换行或单行命令。

### 27.2 参数规则

- `--run-id` 必填，不从多个 shard run_id 中猜测。
- `--output-dir` 默认 `reports/quality`。
- `--expected-execution` 可重复。
- `--expected-case-count` 可选且必须大于等于 0。
- `--junit` 可重复。
- `--run-start-time` 可选 ISO 时间；缺失时不执行 mtime stale 判断，并在 manifest 标记未知。

### 27.3 CLI 退出码

- 0：merge 流程完成并写出 complete 或 failed manifest；IntegrityStatus 只打印，不作为 P0 门禁退出码。
- 2：参数错误或无法生成任何有效 manifest/核心产物。
- KeyboardInterrupt 保持标准中断退出。

CLI 的退出码表达工具自身是否执行完成，不表达未来质量门禁结论。

### 27.4 输出

终端只打印简洁摘要：

```text
run_id
integrity_status
invocation_count
request_count
failure_occurrence_count
integrity_issue_count
manifest_path
```

不打印原始失败消息、traceback、请求 body 或敏感字段。

## 28. 文件级改动清单

### `quality/aggregator.py`

- 新增 merge request/result 内部模型。
- 实现 shard discovery、容错解析、run 过滤、去重、对账和完整性状态。
- 调用 JUnit parser 和 classifier。
- 生成 merged Case、Request、Failure、Integrity 事实集。
- 生成 manifest 和 output hash。

### `quality/classifier.py`

- 新增版本常量、FailureEvidence 和分类决策结构。
- 实现有序确定性规则、owner/confidence 映射和 UNKNOWN fallback。
- 复用现有 failure normalize/hash/fingerprint。

### `quality/junit.py`

- 使用 ElementTree 解析 pytest JUnit。
- 提取 quality properties、状态、异常类型、消息、traceback 和 phase hint。
- 不实现质量分类。

### `quality/cli.py`

- 实现 `merge` 子命令。
- 参数转为 QualityMergeRequest。
- 输出安全摘要和工具级退出码。

### `quality/storage.py`

- 新增 `write_jsonl_atomic()`。
- 保持 `append_jsonl()` 和严格 `read_jsonl()` 现有语义。

### `quality/pytest_plugin.py`

- 在 CaseContext 建立后注入两个 JUnit quality properties。
- 处理同名 property 的重复和冲突。
- 不在插件 sessionfinish 执行父级 merge。

### `quality/__init__.py`

- 导出稳定公共 merge 入口和 `write_jsonl_atomic()`。
- 不导出 JUnit parser 内部 dataclass、规则内部实现和文件扫描私有结构。

### `run_master.py`

- 默认补全 JUnit。
- 初始化/finalize RunRecord。
- 记录 expected cases、planned stages、JUnit paths。
- 在 finally 中 fail-open 调用 merge。
- 保持原测试执行和退出码语义。

### `tests/quality/` 与邻接测试

- 新增 aggregator、classifier、JUnit、CLI 测试。
- 扩展 storage、pytest plugin、run_master 测试。
- 更新并串执行测试以覆盖默认 JUnit，但 disabled 模式原参数不变。

### 不应出现的改动

- 不修改业务用例断言。
- 不修改 RequestMetric 必填身份。
- 不增加 orphan。
- 不改变 RetryExecutor、PollingPolicy、SSE 消费责任或 ThreadPoolExecutor。
- 不修改 Jenkinsfile、邮件和门禁。
- 不删除或清空用户原始 JUnit、Allure、shards。

## 29. 测试方案

### 29.1 storage 原子写入

覆盖：

- 多条模型写成一行一记录的 JSONL。
- 空 records 生成空文件。
- Unicode、datetime、Enum 和 Pydantic model 正确序列化。
- NaN、unsupported object 被拒绝。
- `os.replace()` 失败时旧文件保留，临时文件清理。
- 成功替换后文件以 LF 结尾且无半行。

### 29.2 JUnit parser

覆盖：

- passed、failure、error、skipped。
- 参数化 Unicode testcase name。
- setup error、teardown error、AssertionError。
- `type` 属性存在和不存在。
- message/traceback 异常类型 fallback。
- quality_case_id、quality_invocation_id property。
- property 缺失、重复和冲突。
- 一个 testcase 多个 failure/error 子节点。
- 无效 XML、空 XML、多个 testsuite。
- XML 中的敏感 message 不直接进入输出。

### 29.3 pytest plugin JUnit property

使用 pytester 子进程验证：

- 普通 passed testcase 带两个 quality property。
- setup error 仍带 invocation property。
- 参数化 invocation property 各不相同，case property 相同。
- xdist `-n 2` 后 properties 出现在 controller 生成的 JUnit。
- quality disabled 时不增加 property。
- collect-only 不创建 JUnit 或质量输出。

### 29.4 shard scanner

覆盖：

- 正常 Case、Request、Integrity 三件套。
- foreign run 行被忽略并汇总。
- invalid UTF-8、invalid JSON、invalid schema 后继续读取后续合法行。
- 空分片合法。
- 缺 peer 文件。
- expected execution 缺 Case 分片。
- 文件不可读。
- 错误 message 不包含损坏行敏感内容。

### 29.5 去重

覆盖：

- Case 同 invocation+phase 同内容去重。
- Case 同键不同状态产生 conflict。
- Request 同 event ID 同内容去重。
- Request 同 event ID 不同内容产生 conflict。
- Integrity exact duplicate 去重，真实多次 issue 保留。
- 同 failure_id 不同 invocation 全部保留。
- 同 failure occurrence exact duplicate 去重。
- 相同输入不同文件枚举顺序得到相同 canonical 输出。

### 29.6 invocation 折叠

覆盖：

- setup/call/teardown 全 passed。
- setup error + teardown passed。
- call failed + teardown passed。
- call passed + teardown error。
- call failed + teardown error 生成两个 failure occurrence，invocation 最终为 error。
- skip、xfail、xpass、strict xpass 映射。
- phase 缺失导致完整性问题。

### 29.7 JUnit 对账

覆盖：

- expected、merged、JUnit 三方数量一致。
- merged 少、merged 多、JUnit 少、JUnit 多。
- invocation property 一一对应。
- duplicate invocation、case identity mismatch、status mismatch。
- JUnit 缺失、stale、parse failure。
- 无 property testcase 只参与总量，不猜测关联。

### 29.8 classifier

每个确定性规则至少包含正例和反例：

- 缺环境变量 -> CONFIGURATION。
- 单独 401 无配置文本 -> UNKNOWN。
- quality aggregator 内部 TypeError -> FRAMEWORK_DEFECT。
- request ReadTimeout 不能因公共请求路径误判 framework。
- DNS/connection refused -> ENVIRONMENT。
- 明确 fixture/test data 本地错误 -> TEST_DEFECT。
- 429/retryable timeout -> TRANSIENT。
- 唯一失败接口 + 明确契约断言 -> PRODUCT_DEFECT。
- 普通 AssertionError -> UNKNOWN。
- 多个可能失败接口 -> UNKNOWN 或不带 interface_id。
- classifier 抛错 -> UNKNOWN fallback + integrity issue。

### 29.9 failure fingerprint

覆盖：

- UUID、时间戳、内存地址、query、临时路径变化后 fingerprint 稳定。
- HTTP 404 和 500 不被归并成同一指纹。
- 同消息不同 phase 得到不同 failure_id。
- 同消息不同唯一 interface 得到不同 failure_id。
- assert location 行号稳定保留，临时根目录被去除。
- 多 invocation 同 fingerprint 生成多条 FailureRecord。

### 29.10 aggregator 输出

覆盖：

- 生成四个 merged JSONL、manifest 和 final merge result。
- merged CaseResult 失败 phase 回填 failure_id。
- 所有 failures 可重新加载为 FailureRecord。
- manifest 记录版本、source hashes、counts、output hashes。
- manifest 在输出前为 merging，完成后为 complete。
- 核心写入失败时为 failed，原 shards 保留。
- 相同输入重复 merge 事实内容和 hash 稳定。
- complete、degraded、failed 三种 IntegrityStatus。

### 29.11 run_master

覆盖：

- quality enabled 且无 junit 参数时自动增加默认路径。
- 显式 junit 路径继续使用，并串阶段后缀不变。
- quality disabled 时 pytest args 与当前版本完全一致。
- 单串行、并串混合、空 parallel、空 serial 的 planned executions 正确。
- parallel 普通失败后 serial 继续，最终仍 merge。
- pytest 异常进入 finally merge 后原异常重新抛出。
- KeyboardInterrupt 不被吞掉。
- merge 异常不改变 pytest exit code。
- collect-only 不生成 run_id、JUnit、run.json 或 merged。
- no tests 保持返回 1 并生成 failed integrity 记录。
- 环境变量在阶段和父级结束后恢复。

### 29.12 CLI

覆盖：

- 完整参数 merge 成功。
- 缺 run-id、负 expected count 参数失败。
- 多 junit、多 execution 参数解析。
- integrity failed 但 manifest 成功时工具返回 0。
- 无法写任何产物时返回 2。
- stdout 不包含原始异常和敏感字段。

### 29.13 安全回归

扫描所有迭代三产物，确认不包含：

- Authorization、Cookie、API Key 原值。
- 完整请求体和响应体。
- URL query 敏感值。
- 原始损坏 JSONL 行。
- 完整 JUnit traceback 被复制到 FailureRecord 或 IntegrityIssue。

JUnit 原文件仍属于现有测试证据，迭代三不扩大其内容，只增加安全 identity property。

## 30. 开发实施顺序

### 阶段一：原子输出与 manifest 骨架

1. 实现 `write_jsonl_atomic()`。
2. 定义 merge/classifier/fingerprint 版本常量。
3. 定义 QualityMergeRequest、QualityMergeResult 和 manifest 构建结构。
4. 完成 storage 与 manifest 序列化测试。

退出条件：可以安全生成 merging/failed manifest 和空 merged 文件，不修改现有 collector。

### 阶段二：JUnit identity 与 parser

1. pytest plugin 注入两个 quality properties。
2. 验证普通、setup error、参数化和 xdist JUnit。
3. 实现 ElementTree parser、异常类型和 phase hint 提取。
4. 完成缺失、冲突、无效 XML 测试。

退出条件：JUnit testcase 可以通过 invocation_id 与 CaseResult 显式关联。

### 阶段三：分片扫描与去重

1. 实现文件发现和三件套检查。
2. 实现二进制逐行容错解析。
3. 完成模型重校验、run_id 过滤和 source 统计。
4. 实现 Case、Request、Integrity 去重与冲突问题。

退出条件：损坏行不阻塞合法事实恢复，同一输入得到稳定 canonical 集合。

### 阶段四：Invocation 折叠与 JUnit 对账

1. 实现 phase 状态折叠。
2. 完成 expected/merged/JUnit 三方数量对账。
3. 完成 identity 和状态对账。
4. 固化 complete/degraded/failed 的关键条件。

退出条件：并串混合和 xdist 分片能够证明“执行了多少 invocation，缺了什么”。

### 阶段五：分类与指纹

1. 实现 FailureEvidence 和有序规则。
2. 实现 owner/confidence 映射。
3. 实现 JUnit evidence 和 RequestMetric 的保守选择。
4. 生成 FailureRecord 和 UNKNOWN fallback。
5. 回填 merged CaseResult.failure_id。

退出条件：每个 failed/error phase 恰好有一个 FailureRecord，模糊断言不误判。

### 阶段六：Aggregator 完整输出

1. 串联扫描、去重、对账、分类和状态计算。
2. 原子写四个 merged JSONL。
3. 计算 source/output hash。
4. 提交 complete/failed manifest。
5. 验证重复 merge 幂等性。

退出条件：合成分片可以稳定生成唯一事实集和可信 manifest。

### 阶段七：CLI

1. 实现 `python -m quality.cli merge`。
2. 完成参数、输出和工具退出码。
3. 使用真实生成的隔离分片验证手工恢复。

退出条件：不依赖 run_master 也能对指定 run 重放 merge。

### 阶段八：run_master 父级生命周期

1. 调整 collect-only 和正式运行分流。
2. 初始化 RunRecord。
3. 默认补全和跟踪 JUnit。
4. 记录 expected cases、planned stages 和原执行结果。
5. finally 调用 merge、finalize run.json。
6. 保证原退出码、异常和环境恢复。

退出条件：正常、测试失败、父级异常三条路径都产生可解释质量产物且不改变原行为。

### 阶段九：全量回归与变更记录

1. 执行 focused tests。
2. 执行 `tests/quality`。
3. 执行全量框架测试。
4. 执行业务用例 collect-only。
5. 检查产物脱敏、hash 和幂等性。
6. 写入独立 `code_history/` 迭代三代码变更记录。

## 31. 验证命令

Windows PowerShell 建议先隔离全局第三方 pytest 插件：

```powershell
$env:PYTEST_DISABLE_PLUGIN_AUTOLOAD = '1'
```

storage、JUnit、classifier：

```powershell
python -m pytest -p allure_pytest -p pytester `
  tests/quality/test_quality_storage.py `
  tests/quality/test_quality_junit.py `
  tests/quality/test_quality_classifier.py -q
```

aggregator、CLI、run_master：

```powershell
python -m pytest -p allure_pytest -p pytester `
  tests/quality/test_quality_aggregator.py `
  tests/quality/test_quality_cli.py `
  tests/quality/test_quality_run_master.py `
  tests/test_master_service_parallel_serial.py -q
```

质量模块回归：

```powershell
python -m pytest -p allure_pytest -p pytester tests/quality -q
```

全量框架回归：

```powershell
python -m pytest -p allure_pytest -p pytester tests `
  --basetemp=reports/pytest-temp-iter3 -q
```

业务用例收集：

```powershell
python -m pytest -p allure_pytest module --collect-only -q
```

手工 CLI 回放：

```powershell
python -m quality.cli merge `
  --run-id <run-id> `
  --output-dir reports/quality `
  --expected-execution parallel-pool `
  --expected-execution serial-pool `
  --expected-case-count <count> `
  --junit reports/smoke-tests-parallel.xml `
  --junit reports/smoke-tests-serial.xml
```

最终还需执行：

- merged JSONL 全量重新加载。
- manifest source/output SHA-256 复算。
- 同一输入连续 merge 两次并比较事实文件 hash。
- 对质量目录执行敏感字段扫描。

## 32. 验收标准

### 32.1 归并

- 并行池、串行池和 xdist worker 分片只生成一套当前 run merged 事实集。
- foreign run 数据不进入 merged 文件。
- 单行损坏不会阻止同文件其他合法记录恢复。
- Case、Request、Integrity 唯一键和冲突策略符合本方案。
- 同一输入重复 merge 事实输出稳定。

### 32.2 JUnit 对账

- quality enabled 时 run_master 每个正式阶段都有明确 JUnit 路径。
- 每个正常 JUnit testcase 带 case_id 和 invocation_id property。
- xdist 下 property 不丢失。
- expected、merged invocation、JUnit testcase 数量可对账。
- JUnit 缺失、损坏、stale、identity/status 不一致均可见。
- 不通过 classname/name 猜测 invocation。

### 32.3 失败分类

- 每个 failed/error phase 有且只有一个 FailureRecord。
- 同 failure_id 的多个 invocation occurrence 全部保留。
- 普通模糊 AssertionError 进入 UNKNOWN。
- 七类规则有确定性正反例测试。
- category、owner_domain、confidence 一致且可解释。
- classifier 异常使用 UNKNOWN fallback，不丢失败。

### 32.4 完整性

- complete、degraded、failed 状态有明确可复现条件。
- 核心 Case 缺失、数量不一致、身份冲突和输出失败不能被标为 complete。
- degraded 仍保留可用事实和具体问题。
- run.json 与 merged integrity-issues.jsonl 一致。
- manifest 记录版本、来源、计数和 hash。

### 32.5 非侵入性

- pytest exit code 与迭代三开发前一致。
- 并行普通失败后串行继续执行。
- 父级原异常和 KeyboardInterrupt 不被 merge 替换。
- `QUALITY_ENABLE=0` 时不新增默认 JUnit、run.json 或 merged 行为。
- collect-only 仍无正式质量副作用。
- 原 Allure、JUnit 和 shards 不被删除。

### 32.6 安全

- FailureRecord 和 IntegrityIssue 不包含完整 traceback。
- manifest 不包含原始损坏行。
- merged 产物不包含 API Key、Authorization、Cookie 和完整敏感载荷。
- JUnit property 只包含稳定质量身份。

## 33. 风险与控制

| 风险 | 因果链 | 控制措施 |
| --- | --- | --- |
| JUnit property 在 xdist 丢失 | worker report 未正确回传 -> 无法关联 | 使用真实 xdist 子进程测试，不依赖 mock |
| 旧 JUnit 被误读 | 固定路径残留 -> 错误对账 | 只用实际 args 路径，并校验 mtime/run start |
| foreign shard 污染 | 固定分片名保留旧 worker -> 数量虚高 | 每条模型校验后按 run_id 过滤 |
| 简单拼接遇到半行 | 进程中断 -> 全文件解析失败 | 二进制逐行容错，合法行继续恢复 |
| 按 failure_id 去重丢 occurrence | 同类问题多次发生 -> 影响范围消失 | occurrence 使用 composite key，failure_id 只分组 |
| 分类过度 | 普通断言被判产品缺陷 -> 门禁误报 | 高确定性规则，模糊证据 UNKNOWN |
| 多请求错误关联 | 一个 invocation 多接口 -> 猜错主因 | 只有唯一明确接口才进入 fingerprint |
| merge 覆盖原异常 | finally 抛质量异常 -> 测试语义改变 | 保存原异常，merge 捕获 Exception 后重抛原异常 |
| 多文件非原子 | 部分新文件 + 旧 manifest -> 假完整 | merging/complete manifest + output hash |
| IntegrityIssue 数量爆炸 | 每坏一行一条 -> run.json 过大 | 同文件同 code 聚合，限制示例行号 |
| 默认 JUnit 改变禁用模式 | 参数无条件增加 -> 兼容性回归 | 只在 quality enabled 时补全 |
| 环境配置读取失败 | 初始化业务 Settings -> 缺 key 阻断 run | 只读安全环境变量，不初始化业务配置 |

## 34. 回退方案

### 34.1 运行时回退

设置：

```text
QUALITY_ENABLE=0
```

应恢复：

- 不初始化 RunRecord。
- 不补默认 JUnit。
- 不注入质量身份 property。
- 不扫描 shards、不 merge、不分类。
- run_master 使用原 pytest args 和原退出码。

### 34.2 代码回退顺序

若迭代三上线后需要局部回退：

1. 先关闭 run_master 自动 merge 和默认 JUnit。
2. 保留 aggregator/CLI 作为离线工具，不进入正式执行链。
3. 再移除 pytest JUnit properties。
4. 最后按需移除新增模块和测试。

`quality.v1` 未修改，因此迭代一、二分片仍可继续生成和读取。

### 34.3 不允许的回退方式

- 不删除原始 shards 掩盖 merge 问题。
- 不把 integrity failed 强制改成 complete。
- 不通过按 failure_id 只保留一条降低失败数量。
- 不捕获并吞掉原 pytest 异常。
- 不通过放宽 CaseResult、RequestMetric 必填身份解决关联问题。

## 35. 与迭代四的接口

迭代三必须为报告与影子门禁提供稳定输入：

```text
run.json
  运行状态、完整性状态、完整性问题

merged/manifest.json
  merge/classifier/fingerprint 版本
  输入来源、计数、hash、提交状态

merged/case-results.jsonl
  phase 事实和 failure_id

merged/request-metrics.jsonl
  真实 request attempt

merged/failures.jsonl
  每次 failure occurrence 和分类

merged/integrity-issues.jsonl
  完整性证据
```

迭代四统计必须遵守：

- case_total 使用 unique invocation，不使用 CaseResult 行数。
- 相同 failure_id 按问题聚合，但 occurrence、case、invocation 数分别统计。
- IntegrityStatus.failed 时门禁只能 NO_DATA，不得 PASS。
- IntegrityStatus.degraded 时报告必须展示缺失项。
- UNKNOWN 是合法分类结果，但应进入观察列表。
- 请求成功率与测试通过率分开。
- 当前没有逻辑 request group id，不能可靠计算重试挽救率。

迭代三不得提前实现：

- QualitySummary 最终输出。
- GateDecision。
- Markdown 报告。
- Jenkins artifact 和邮件入口。

## 36. 实施前决策关闭情况

| 问题 | 最终决策 |
| --- | --- |
| 是否修改 `quality.v1` FailureRecord | 不修改 |
| 分类规则版本如何追溯 | 写入 merged manifest |
| failure_id 是否删除重复问题 | 不删除 occurrence，只作为聚合键 |
| FailureRecord occurrence 唯一键 | failure_id + invocation_id + phase |
| JUnit 是否默认生成 | quality enabled 时 run_master 默认保证存在 |
| JUnit 如何关联 Case | 显式 quality_case_id / quality_invocation_id property |
| 是否用 classname/name 猜测 | 不猜测，缺失时降级 |
| 普通 AssertionError 如何分类 | 证据不足归 UNKNOWN |
| 是否保留所有失败 | failed/error phase 各生成一条 FailureRecord |
| 分类异常如何处理 | UNKNOWN fallback + IntegrityIssue |
| merge 是否改变测试退出码 | 不改变，run_master fail-open |
| 是否本迭代生成 summary/report/gate | 不生成，留给迭代四 |

当前方案不存在需要继续阻断开发的产品语义问题。若实际开发发现以下任一情况，必须停下来确认：

- 必须修改 `quality.v1` 必填字段才能继续。
- JUnit property 在当前 pytest/xdist 版本无法稳定传播。
- 一个父级 run 内确实需要重复执行同一 case+param，并且应被视为多个 invocation。
- 需要把普通 AssertionError 默认判为 PRODUCT_DEFECT 或 TEST_DEFECT。
- 需要按 HTTP 200 正文统一识别业务失败。
- 需要新增 transient 独立 OwnerDomain。
- 需要在本迭代提前实现 summary、门禁或 Jenkins 修改。
- 需要删除原始 shards 或改变 pytest exit code 才能完成归并。

## 37. 交付物

本迭代代码开发完成后应交付：

- 原子 JSONL merged writer。
- JUnit quality identity 注入和 parser。
- 分片扫描、run 过滤、去重和冲突检测。
- invocation 折叠和 JUnit 三方对账。
- 确定性失败分类与 UNKNOWN fallback。
- FailureRecord 和 CaseResult.failure_id 回填。
- complete/degraded/failed 完整性状态机。
- run.json 和 versioned merge manifest。
- `quality.cli merge`。
- run_master 自动 JUnit、自动 merge 和 fail-open finalization。
- focused、xdist、全量回归和安全扫描结果。
- 可人工检查的 complete、degraded、failed 三组隔离样例产物。
- 一份写入 `code_history/` 的迭代三代码变更记录。

最终完成标准不是“生成 merged 文件”，而是：

```text
当前 run 的事实被唯一归并
并且
Case 数量和身份可以与预收集、JUnit 对账
并且
每个真实失败 occurrence 都可分类或明确 UNKNOWN
并且
损坏、缺失、冲突不会被静默解释为成功
并且
质量归并不改变原测试结果、异常和退出码
```

## 38. 附录：确定性失败分类规则明细

### 38.1 分类输入

classifier 接收一个内部 `FailureEvidence`，至少包含：

```text
run_id
case_id
invocation_id
phase
error_type
normalized_message
assert_location
junit_status
request_metrics
related_integrity_codes
```

classifier 不读取文件、不直接写 JSONL，也不依赖全局 collector。

### 38.2 规则输出

每个规则返回：

```text
category
owner_domain
confidence
rule_id
reason_code
selected_interface_id
```

`rule_id` 和 `reason_code` 用于测试、日志和 manifest 统计，不写入已经冻结的 FailureRecord 字段。

### 38.3 分类优先级

#### CONFIGURATION

高确定性证据：

- 明确缺少必填环境变量、API Key、路由或环境选择。
- Pydantic/配置校验消息明确指向配置字段。
- 401/403 同时存在 credential、permission、authorization 等明确配置证据。
- 启动阶段出现已知配置异常类型。

不能只因为 HTTP 401/403 就直接判配置；缺少凭据证据时进入 UNKNOWN。

输出：

```text
owner_domain = configuration
confidence = high|medium
```

#### FRAMEWORK_DEFECT

高确定性证据：

- traceback/assert_location 明确位于 `quality/` 的 collector、plugin、aggregator、classifier、storage。
- 完整性代码明确为框架内部不变量破坏。
- 公共框架 API 出现确定性的 TypeError、AttributeError、模型构建或序列化错误，并且不是服务端响应导致。

不能把所有 `common/` 路径异常都判框架缺陷；请求超时、连接失败仍按环境或瞬态处理。

输出：

```text
owner_domain = framework
confidence = high|medium
```

#### ENVIRONMENT

高确定性证据：

- DNS/NameResolutionError。
- ConnectionRefused、HostUnreachable、NetworkUnreachable。
- ProxyError、SSL handshake/certificate、端口或依赖服务不可达。
- 资源不足、磁盘不可写、环境服务未启动，并且证据不指向质量代码自身。

输出：

```text
owner_domain = environment
confidence = high|medium
```

#### TEST_DEFECT

只接受明确测试侧证据：

- fixture 参数构造错误或测试数据 schema 明确不合法。
- 测试资源文件路径、清理逻辑或本地测试辅助函数确定性错误。
- 异常位置明确在业务测试代码，且错误与 API 响应无关，例如本地 KeyError、IndexError、测试数据转换错误。

普通 AssertionError、服务端错误后触发的测试断言不能直接判 TEST_DEFECT。

输出：

```text
owner_domain = test
confidence = high|medium
```

#### TRANSIENT

高确定性证据：

- HTTP 429 或明确 rate limit 错误。
- `retryable=true` 且错误为 ReadTimeout、ConnectTimeout、ConnectionReset 等瞬态类型。
- 服务明确返回可恢复、稍后重试语义。

DNS、持续连接拒绝等基础环境问题优先归 ENVIRONMENT。

输出：

```text
owner_domain = environment
confidence = high|medium
```

当前 OwnerDomain 没有 transient 枚举，因此 TRANSIENT 暂归 environment 责任域；后续如需独立责任域必须升级契约。

#### PRODUCT_DEFECT

只在存在明确产品或契约证据时匹配：

- API 已返回确定响应，JUnit 断言明确表达状态码、字段、类型或契约不符合预期。
- 同 invocation 存在唯一可关联的失败 RequestMetric，且不是配置、环境或瞬态规则。
- 明确业务失败终态或稳定错误响应触发断言。

只有 `AssertionError`、只有 HTTP 500、或多个请求都可能导致失败时，不直接判 PRODUCT_DEFECT。

输出：

```text
owner_domain = product
confidence = high|medium
```

#### UNKNOWN

以下情况统一进入 UNKNOWN：

- 普通 AssertionError 无法区分产品和测试预期。
- JUnit 证据缺失或无法唯一关联。
- 多类规则同时命中且优先级证据不足。
- 只有模糊错误文本，没有稳定异常类型、位置或请求证据。
- classifier 自身安全降级。

输出：

```text
owner_domain = unknown
confidence = low
```
