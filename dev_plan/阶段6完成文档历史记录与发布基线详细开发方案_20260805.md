# 阶段6完成文档、历史记录与发布基线详细开发方案

> 生成日期：2026-08-05
>
> 上位方案：`dev_plan/离线框架能力分类用例与黄金路径详细执行方案_20260805.md`
>
> 当前三层状态：本地验收`PASS`；实际Jenkins`BLOCKED`；整体发布`BLOCKED`

## 1. 需求复述与阶段目标

阶段6把阶段0～5已经形成的本地事实收敛为可学习、可复现、可审计、可发布判断的仓库说明，不再扩展协议、增加用例或修改核心实现。

```text
代码与SCM
→ 可执行命令
→ 机器证据
→ README与规范
→ 最终历史
→ 发布状态
```

阶段6必须回答：

1. README能否从阶段2的7条基线更新为当前23条真实用例；
2. 用例规范能否固化分类、并发和黄金路径的职责；
3. 文档命令能否从仓库根目录在全新PowerShell中执行；
4. 本地、Jenkins和发布三层状态能否分别记录；
5. 阶段4核心文件是否进入Jenkins可Checkout的提交；
6. 最终历史能否完整记录文件、数量、证据和未纳入能力；
7. 4项完整回归失败存在时，能否保持发布阻塞；
8. 外部门禁解除后，能否重新取证并提升发布状态；
9. 能否不触碰用户已有`.gitignore`修改和其他工作树文件。

## 2. 第一性原理与TOC判断

### 2.1 问题本质

文档的最小可信单位是：

```text
准确事实 + 可执行命令 + 可追溯证据 + 明确状态边界
```

只把“7条”改为“23条”仍然不够。如果两个阶段4核心文件没有进入SCM，其他开发者和Jenkins Checkout后仍无法得到完整实现，文档就会把本机事实误写成仓库事实。

### 2.2 TOC首要约束：SCM可见性

```text
阶段4文件未跟踪
→ Jenkins deleteDir()
→ checkout scm
→ 文件消失
→ 阶段4目标无法收集
→ Jenkins验收无效
→ 整体发布阻塞
```

控制规则：

- SCM尚未包含阶段4文件：更新本地能力事实，但明确标记Jenkins和发布阻塞；
- SCM已经包含阶段4文件：才允许触发实际Jenkins；
- 不得在Jenkins Workspace手工复制本机文件绕过Checkout。

### 2.3 三层状态必须分离

1. **本地验收状态**：23条用例和本地质量链已经通过；
2. **外部门禁状态**：实际Jenkins当前为`BLOCKED`；
3. **整体发布状态**：完整`tests`仍有4项失败，当前为`BLOCKED`。

禁止从“本地23 passed”推导“Jenkins通过”或“框架回归全绿”。

### 2.4 文档漂移因果链

```text
文档仍称只有7条用例
→ 开发者误判Polling、Context、Capture、并发未实现
→ 重复实现或扩张黄金路径
→ 职责重叠和维护成本增加
```

README负责“如何学习和运行”，规范负责“以后新增能力放在哪里”，两者必须同步。

### 2.5 回归债务不能被重定义

当前完整回归：

```text
686 collected / 682 passed / 4 failed
```

阶段6不得通过修改文档、允许列表、`.gitignore`、fixture或测试基线制造发布通过。完整回归只有零失败才是发布`PASS`。

### 2.6 决策顺序

```text
保护工作树
→ 冻结代码、SCM和证据
→ 判断三层状态
→ 更新README
→ 更新FRAMEWORK_TEST_SPEC
→ 复验文档命令
→ 生成最终历史
→ 重新判断发布门禁
```

## 3. 当前已验证实施基线

### 3.1 文件与用例

`module/offline_framework_example`当前有15个文件、7个测试文件和23条业务用例：

| 分类文件 | 用例数 | 主要职责 |
|---|---:|---|
| `test_request_pipeline.py` | 3 | Request、Middleware、Header和payload不变性 |
| `test_retry.py` | 4 | GET、Retry-After、幂等和非幂等POST |
| `test_polling.py` | 4 | success、failure、unknown和deadline |
| `test_context_cleanup.py` | 4 | 提取、转换、LIFO cleanup和错误归并 |
| `test_capture_assertions.py` | 4 | Capture、上限、Schema路径和脱敏诊断 |
| `test_concurrency_context.py` | 3 | ContextVar、Session和Header隔离 |
| `test_full_framework_flow.py` | 1 | 稳定成功黄金路径 |
| **合计** | **23** | 6组分类加1条黄金路径 |

黄金nodeid：

```text
module/offline_framework_example/test_full_framework_flow.py::TestFullFrameworkFlow::test_offline_async_media_flow
```

### 3.2 阶段5本地证据

| 门禁 | 当前事实 |
|---|---|
| Runner收集 | `23 total / 23 parallel / 0 serial` |
| Quality关闭 | `23 passed / 0 failed / 0 errors`，Quality文件为0 |
| 完整Quality链 | `23 passed`，P0、Semantic完整 |
| 完整模块Metrics | `degraded`，唯一原因为`usage_incomplete` |
| 外部URL模板 | `0` |
| 黄金路径关系 | `4 operations / 7 groups / 1 polling / 8 events` |
| 黄金路径Metrics | `aggregated`，`media_count=1` |
| Retry/Polling | `503 → 200`；`pending → pending → success` |
| Flaky | `OBSERVING → OBSERVING → STABLE → STABLE` |
| Pipeline Summary | 本地模拟成功，Retry挽救产生`WARN` |
| Smoke收集 | `40 total / 15 parallel / 25 serial` |

运行产物已隔离在临时目录，`reports/execution-result.json`已恢复原Hash。阶段6不得依赖临时目录仍然存在。

### 3.3 SCM与Jenkins

15个模块文件中13个被Git跟踪，以下文件未跟踪：

```text
module/offline_framework_example/test_concurrency_context.py
module/offline_framework_example/test_full_framework_flow.py
```

阶段5方案和验收记录当前也未跟踪。实际Jenkins未触发，因为Pipeline会先`deleteDir()`再`checkout scm`，且当前环境没有Jenkins Build上下文或`jenkins-cli`。

### 3.4 完整回归

```text
collected = 686
passed = 682
failed = 4
errors = 0
exit code = 1
```

失败集合：

```text
tests/quality/test_repository_test_boundaries.py::test_tests_directory_is_not_controlled_by_an_allowlist
tests/quality/test_repository_test_boundaries.py::test_new_test_file_is_not_ignored_by_default
tests/quality/test_repository_test_boundaries.py::test_current_test_modules_are_not_ignored
tests/quality/test_repository_test_boundaries.py::test_quality_fixture_worktree_bytes_use_lf
```

当前`.gitignore`是用户已有修改文件并仍包含`/tests/*`。阶段6只记录，不修复这4项独立仓库边界债务。

### 3.5 文档漂移

README仍包含：当前7条、Polling/Context/Capture/并发/黄金路径尚未实现、离线分类7项和框架测试686 passed等陈旧事实。

规范仍包含：业务分类只有7项、后续能力尚未实现、当前7并发/0串行等陈旧事实。

### 3.6 工作树保护

阶段6开始前已有`.gitignore`修改、阶段4未跟踪测试、阶段5方案、`code_history`和其他无关未跟踪文件。不得执行`git clean`、`git reset --hard`、批量还原、移动或删除。

## 4. 状态收敛规则

### 4.1 阶段6完成与总任务完成分离

阶段6文档收敛可在准确记录阻塞的前提下完成：

```text
阶段6文档基线完成
+ 实际Jenkins BLOCKED
+ 整体发布 BLOCKED
```

总任务只有在阶段4文件进入Jenkins提交、实际Jenkins通过、完整`tests`零失败以及总方案其他条件全部满足时才能标记`COMPLETE`。

### 4.2 SCM状态层级

```text
文件存在 ≠ tracked ≠ HEAD包含 ≠ 远端变更集包含 ≠ Jenkins checkout得到
```

Jenkins门禁至少要求`git cat-file -e HEAD:<path>`成功，且Build Checkout SHA与证据一致。

### 4.3 code_history与提交分离

阶段6必须写最终`code_history`，但不自动执行`git add`或`git commit`。是否把历史记录纳入提交由用户明确决定。

### 4.4 离线语义边界

只声明“业务接口测试零外部请求”。现有Jenkins依赖准备可能访问pip/npm镜像，不能宣称整个构建物理断网。

### 4.5 数量是快照

23条离线用例和686条框架测试是2026-08-05快照。长期合同是集合守恒、职责单一、黄金路径只保留稳定成功主链，以及新增能力后同步更新证据。

## 5. 阶段边界

### 5.1 阶段内计划修改

执行阶段6时修改：

```text
README.md
FRAMEWORK_TEST_SPEC.md
```

新增：

```text
code_history/20260805_阶段6文档与发布基线验收记录.md
```

本方案文件为：

```text
dev_plan/阶段6完成文档历史记录与发布基线详细开发方案_20260805.md
```

### 5.2 条件性外部动作

以下动作仅在用户明确要求或外部条件满足后执行：

- 将阶段4测试文件加入目标提交；
- 将阶段5、阶段6方案加入目标提交；
- 将`code_history`加入目标提交；
- 触发实际Jenkins Build；
- 记录Jenkins Build URL、编号和归档链接；
- 将整体发布状态从`BLOCKED`提升为`PASS`。

### 5.3 明确不修改

```text
common/
quality/
run_orchestration/
pipeline_reporting/
module/offline_framework_example/*.py
tests/
.gitignore
Jenkinsfile
requirements.txt
pytest.ini
```

不新增测试、Marker、报告类型、Jenkins参数或专用Job。

### 5.4 独立债务边界

4项完整回归失败属于独立仓库边界任务。阶段6可以重新验证债务是否解除，但不得顺手修复或重写基线。

## 6. 证据优先级与状态模型

### 6.1 证据优先级

出现冲突时按以下顺序取值：

1. 阶段6实际执行输出；
2. 同轮JUnit、Runner、Quality、Metrics、Flaky和Pipeline机器产物；
3. 实际Jenkins Build与归档；
4. `code_history/20260805_阶段5Runner与质量链验收记录.md`；
5. 当前代码和配置；
6. README、规范和旧方案中的描述。

### 6.2 三层状态字段

最终历史固定记录：

```text
local_validation_status = PASS | FAIL | BLOCKED
jenkins_validation_status = PASS | FAIL | BLOCKED | NOT_RUN
release_status = PASS | BLOCKED
```

- `PASS`：门禁实际执行且全部满足；
- `FAIL`：实际执行并发现失败；
- `BLOCKED`：前置条件不满足；
- `NOT_RUN`：非强制场景选择未执行；
- 整体发布不使用`NOT_RUN`，强制门禁未完成统一为`BLOCKED`。

### 6.3 发布判定表

| 本地 | Jenkins | 完整回归 | 阶段6文档 | 整体发布 |
|---|---|---|---|---|
| PASS | BLOCKED | 4 failed | PASS | BLOCKED |
| PASS | PASS | 4 failed | PASS | BLOCKED |
| PASS | BLOCKED | 0 failed | PASS | BLOCKED |
| PASS | PASS | 0 failed | PASS | PASS |
| FAIL | 任意 | 任意 | FAIL或BLOCKED | BLOCKED |

### 6.4 快照元数据

最终历史必须记录：验收日期与时区、Python版本、分支和HEAD SHA、工作树状态、tracked/HEAD可见性、实际命令和退出码、JUnit数量、Jenkins字段、完整回归失败nodeid、未纳入能力及原因。

不得记录API Key、邮箱密码、Cookie、Authorization Header或私人目录内容。

## 7. 工作树保护与SCM门禁

### 7.1 执行前保护

```powershell
git status --short
git diff -- .gitignore
git -c core.quotepath=false status --short -- `
  README.md `
  FRAMEWORK_TEST_SPEC.md `
  dev_plan `
  code_history `
  module/offline_framework_example
```

保护清单以阶段6实际执行时的`git status`为准。不得修改当前已知的`.gitignore`、其他业务文件和无关未跟踪目录。

### 7.2 必需文件存在性

```powershell
$requiredModuleFiles = @(
  'module/offline_framework_example/__init__.py',
  'module/offline_framework_example/assertions.py',
  'module/offline_framework_example/conftest.py',
  'module/offline_framework_example/decorators.py',
  'module/offline_framework_example/offline_service.py',
  'module/offline_framework_example/request.py',
  'module/offline_framework_example/response_schemas.py',
  'module/offline_framework_example/task.py',
  'module/offline_framework_example/test_capture_assertions.py',
  'module/offline_framework_example/test_concurrency_context.py',
  'module/offline_framework_example/test_context_cleanup.py',
  'module/offline_framework_example/test_full_framework_flow.py',
  'module/offline_framework_example/test_polling.py',
  'module/offline_framework_example/test_request_pipeline.py',
  'module/offline_framework_example/test_retry.py'
)

$missingFiles = $requiredModuleFiles | Where-Object {
  -not (Test-Path -LiteralPath $_ -PathType Leaf)
}
if ($missingFiles) {
  throw ('Offline module files missing: {0}' -f ($missingFiles -join ', '))
}
```

### 7.3 tracked检查

```powershell
$trackedFiles = @(git -c core.quotepath=false ls-files -- module/offline_framework_example)
$untrackedRequiredFiles = $requiredModuleFiles | Where-Object {
  $trackedFiles -notcontains $_
}
$untrackedRequiredFiles
```

当前预期输出两个阶段4测试文件。阶段6不得自动执行`git add`。

### 7.4 HEAD可见性

用户完成目标提交后执行：

```powershell
$jenkinsRequiredFiles = @(
  'module/offline_framework_example/test_concurrency_context.py',
  'module/offline_framework_example/test_full_framework_flow.py'
)

$missingFromHead = foreach ($path in $jenkinsRequiredFiles) {
  $objectName = 'HEAD:' + $path
  git cat-file -e $objectName 2>$null
  if ($LASTEXITCODE -ne 0) { $path }
}

if ($missingFromHead) {
  throw ('Jenkins checkout blocked: {0}' -f ($missingFromHead -join ', '))
}
```

### 7.5 本机与HEAD一致性

```powershell
git diff --exit-code HEAD -- `
  module/offline_framework_example/test_concurrency_context.py `
  module/offline_framework_example/test_full_framework_flow.py
```

有差异时必须先由用户决定提交版本，再继续Jenkins。

### 7.6 SCM结论格式

```text
SCM visibility = PASS
```

或：

```text
SCM visibility = BLOCKED
missing from HEAD = [...]
Jenkins execution = not triggered
```

禁止把tracked成功但HEAD缺失写成`PASS`。

## 8. README详细更新设计

### 8.1 当前状态

将当前7条描述更新为日期化快照：

```text
截至2026-08-05，离线服务包含9个确定性场景和18项基础设施门禁；
离线业务模块包含23条用例，覆盖6组能力分类和1条黄金路径；
当前Runner计划为23条并发、0条串行。
```

同段说明23是当前快照，不是永久合同；本地门禁已通过；Jenkins和整体发布状态读取日期化验收表，不在顶部写无日期的永久完成结论。

### 8.2 架构链路

替换能力尚未实现的陈述，更新为：

```text
OfflineService确定性协议
→ 四件套业务封装
→ Request/Middleware/Retry分类
→ Polling分类
→ TestContext与cleanup分类
→ Capture与Assertions分类
→ ContextVar/Session/Header并发隔离分类
→ 稳定成功黄金路径
→ Runner / Allure / 可选Quality消费同一事实
```

强调离线模块复用公共BaseRequest、BaseTask、BaseAssertions、Retry、Polling、Capture和Runtime Hooks，不复制框架实现。

### 8.3 学习顺序

在“两层离线回归与故障模拟”后新增学习入口：

1. `offline_service.py`：9个服务场景和确定性状态；
2. `conftest.py`：随机loopback端口、代理隔离和资源回收；
3. `request.py`、`task.py`、`assertions.py`、`decorators.py`：四件套最小接入；
4. `test_request_pipeline.py`：Request和Middleware；
5. `test_retry.py`：重试资格与挽救；
6. `test_polling.py`：状态机终态；
7. `test_context_cleanup.py`：上下文提取和LIFO清理；
8. `test_capture_assertions.py`：Artifact边界和契约诊断；
9. `test_concurrency_context.py`：ContextVar和Session所有权；
10. `test_full_framework_flow.py`：最后学习能力组合，不从黄金路径反推全部异常语义。

### 8.4 分类与黄金路径职责

| 类型 | 回答的问题 | 不承担的职责 |
|---|---|---|
| 分类用例 | 哪一项能力或边界失效 | 不证明全部能力组合 |
| 并发分类 | Context、Session和请求级状态是否隔离 | 不验证全部业务异常 |
| 黄金路径 | 稳定成功主链能否组合成立 | 不容纳互斥failure/unknown/timeout |
| Runner/Quality验收 | 同一执行事实能否被可信消费 | 不替代业务断言 |

### 8.5 Quality关闭命令

README必须提供从仓库根目录执行的独立PowerShell示例，并使用唯一系统临时目录：

```powershell
$python = (Resolve-Path .\.venv\Scripts\python.exe).Path
$evidenceRoot = Join-Path `
  ([System.IO.Path]::GetTempPath()) `
  ('offline-example-disabled-' + [guid]::NewGuid().ToString('N'))
$junitPath = Join-Path $evidenceRoot 'offline-disabled.xml'
$allurePath = Join-Path $evidenceRoot 'allure'
New-Item -ItemType Directory -Path $evidenceRoot -Force | Out-Null

$env:QUALITY_ENABLE = '0'
$env:GENERATE_ALLURE_REPORT = 'FALSE'
$env:GENERATE_HISTORY_REPORT = 'FALSE'
try {
  & $python run_master.py module/offline_framework_example `
    -n 2 `
    --junitxml=$junitPath `
    --alluredir=$allurePath `
    -q
  if ($LASTEXITCODE -ne 0) { throw 'Offline example failed.' }
}
finally {
  @(
    'QUALITY_ENABLE',
    'GENERATE_ALLURE_REPORT',
    'GENERATE_HISTORY_REPORT'
  ) | ForEach-Object {
    Remove-Item ('Env:' + $_) -ErrorAction SilentlyContinue
  }
}
```

预期事实：23条通过、Quality文件为0、JUnit和Allure位于`$evidenceRoot`。

### 8.6 Quality、Semantic和Metrics开启命令

README提供独立示例，不复用上一代码块环境：

```powershell
$python = (Resolve-Path .\.venv\Scripts\python.exe).Path
$runId = 'offline-doc-' + [guid]::NewGuid().ToString('N')
$evidenceRoot = Join-Path ([System.IO.Path]::GetTempPath()) $runId
$qualityRoot = Join-Path $evidenceRoot 'quality'
$junitPath = Join-Path $evidenceRoot 'offline-quality.xml'
$allurePath = Join-Path $evidenceRoot 'allure'
New-Item -ItemType Directory -Path $qualityRoot -Force | Out-Null

$env:QUALITY_ENABLE = '1'
$env:QUALITY_SEMANTIC_ENABLE = '1'
$env:QUALITY_METRICS_ENABLE = '1'
$env:QUALITY_FLAKY_HISTORY_ENABLE = '0'
$env:QUALITY_FLAKY_STATE_ENABLE = '0'
$env:QUALITY_OUTPUT_DIR = $qualityRoot
$env:QUALITY_RUN_ID = $runId
$env:QUALITY_EXECUTION_ID = $runId
$env:USE_CHINA_ENVIRONMENT = 'FALSE'
$env:GENERATE_ALLURE_REPORT = 'FALSE'
$env:GENERATE_HISTORY_REPORT = 'FALSE'
try {
  & $python run_master.py module/offline_framework_example `
    -n 2 `
    --junitxml=$junitPath `
    --alluredir=$allurePath `
    -q
  if ($LASTEXITCODE -ne 0) { throw 'Offline Quality example failed.' }
}
finally {
  @(
    'QUALITY_ENABLE',
    'QUALITY_SEMANTIC_ENABLE',
    'QUALITY_METRICS_ENABLE',
    'QUALITY_FLAKY_HISTORY_ENABLE',
    'QUALITY_FLAKY_STATE_ENABLE',
    'QUALITY_OUTPUT_DIR',
    'QUALITY_RUN_ID',
    'QUALITY_EXECUTION_ID',
    'USE_CHINA_ENVIRONMENT',
    'GENERATE_ALLURE_REPORT',
    'GENERATE_HISTORY_REPORT'
  ) | ForEach-Object {
    Remove-Item ('Env:' + $_) -ErrorAction SilentlyContinue
  }
}
```

阶段6必须在全新PowerShell中实际验证。若框架需要额外合成环境值，以阶段5真实成功命令为准补充，不能放未经执行的示意命令。

### 8.7 黄金路径

README展示稳定nodeid：

```powershell
$goldenNodeId = 'module/offline_framework_example/test_full_framework_flow.py::TestFullFrameworkFlow::test_offline_async_media_flow'
.\.venv\Scripts\python.exe -m pytest $goldenNodeId -q
```

运行级精确证据为：

```text
4 operations / 7 request groups / 1 polling session / 8 request events
```

这些数量是当前快照，业务测试不得直接读取Quality内部模型。

### 8.8 报告产物位置

| 场景 | 位置 |
|---|---|
| 本地文档示例 | 唯一`$evidenceRoot`下的JUnit、Allure和Quality目录 |
| Runner固定摘要 | `reports/execution-result.json` |
| Jenkins JUnit | `reports/unit-tests.xml`、`reports/smoke-tests*.xml` |
| Jenkins Quality | `reports/quality/**` |
| Jenkins Pipeline摘要 | `reports/pipeline-summary.md`及同轮机器/邮件产物 |
| Jenkins Allure | `allure-results/**` |
| Flaky数据库 | Workspace外、Job独占的绝对本地路径 |

不得把阶段5已清理的临时绝对路径写入README。

### 8.9 Flaky多轮

可靠验收要求：

```text
同一黄金nodeid
+ 同一case_id/param_hash/environment/execution profile
+ 独占绝对SQLite数据库
+ 每轮唯一run ID与Quality目录
+ stable_min_samples + 1轮真实执行
```

当前`stable_min_samples=3`，阶段5实际4轮状态为`OBSERVING → OBSERVING → STABLE → STABLE`。长期规则是动态读取阈值后加1轮，不把4轮写成永久合同。

### 8.10 Jenkins参数

| 参数 | 值 |
|---|---|
| `RUN_FRAMEWORK_TESTS` | `false` |
| `RUN_COLLECT_ONLY` | `false` |
| `RUN_REAL_SMOKE` | `true` |
| `GENERATE_PIPELINE_SUMMARY` | `true` |
| `ALWAYS_SEND_REPORT_EMAIL` | `false` |
| `USE_CHINA_ENVIRONMENT` | `FALSE` |
| `SMOKE_TARGET` | `module/offline_framework_example` |
| `TEST_PARALLEL_WORKERS` | `2` |

同时说明：阶段4文件必须进入Checkout提交；业务HTTP只访问loopback；pip/npm准备可能访问镜像；未执行Build时不得写Jenkins已通过。

### 8.11 本地验收快照

| 项目 | 2026-08-05事实 |
|---|---|
| 离线服务合同 | 18项通过 |
| 离线业务模块 | 23项通过，23并发/0串行 |
| Quality/Metrics/Flaky | 本地通过 |
| Smoke collect-only | 40项，15并发/25串行 |
| 完整框架回归 | 686收集、682通过、4失败 |
| 实际Jenkins | BLOCKED，阶段4文件未进入SCM |
| 整体发布 | BLOCKED |

若阶段6执行时状态变化，必须替换为实际结果和日期。

## 9. FRAMEWORK_TEST_SPEC详细更新设计

### 9.1 离线测试三层结构

| 层级 | 位置 | 证明内容 |
|---|---|---|
| 单元级故障模拟 | `tests/`中的FakeResponse等 | 单一分支和错误语义 |
| loopback基础设施门禁 | `tests/test_offline_service.py` | 协议、隔离、并发计数、线程回收和网络边界 |
| 业务分类与黄金路径 | `module/offline_framework_example/test_*.py` | 四件套、请求生命周期、能力分类和稳定组合 |

三层不得互相替代，也不得为离线验证复制公共实现。

### 9.2 分类职责表

| 文件 | 允许职责 | 不应继续加入 |
|---|---|---|
| `test_request_pipeline.py` | Request、Middleware、payload/Header | Retry、Polling终态 |
| `test_retry.py` | 重试资格、等待和挽救 | Polling业务状态 |
| `test_polling.py` | 状态转换、失败、未知、超时 | Context/Capture |
| `test_context_cleanup.py` | 提取、转换、清理顺序和错误归并 | 网络重试 |
| `test_capture_assertions.py` | Artifact边界、Schema和脱敏诊断 | 并发所有权 |
| `test_concurrency_context.py` | ContextVar、Session和请求级隔离 | 黄金业务全链 |
| `test_full_framework_flow.py` | 稳定成功主链 | 互斥异常和全部边界排列 |

### 9.3 分类与黄金路径判断规则

```text
某一能力失效时能否精确定位
→ 分类用例

多个已验证能力在稳定成功主链中能否组合
→ 黄金路径

场景与黄金成功主链互斥
→ 保留在分类用例
```

黄金路径不得继续吸收Polling failure、unknown、timeout、非幂等POST禁止重试、Capture超限、cleanup多错误和Header泄漏反例。

### 9.4 确定性本地服务要求

- 只绑定`127.0.0.1`随机端口；
- 客户端禁止读取系统代理；
- 守卫拒绝合同外目标；
- 状态按服务实例隔离；
- 使用锁、计数器和Event协调，不使用随机等待制造时序；
- fixture负责服务启动和关闭；
- Request、Session、线程和文件在`finally`或teardown回收；
- Retry和Polling使用真实公共实现；
- 资源URL由本轮服务生成并校验loopback；
- 不读取真实`.env`服务URL或密钥完成业务请求。

### 9.5 Quality边界

业务分类只能通过`common.runtime_hooks`、Request `runtime_metadata`、逻辑调用作用域和pytest/Runner公开入口产生中性事实。

禁止业务示例：

- 导入`quality.metrics.*`内部builder；
- 导入`quality.flaky_store.*`内部repository或projection；
- 读取Quality内部数据库表断言业务行为；
- 为Metrics修改业务断言；
- 把Quality fail-open解释为Quality成功。

Quality、Metrics和Flaky只在运行级验收中通过机器产物和公开CLI验证。

### 9.6 新增能力归类算法

1. 优先归入现有单一职责分类；
2. 无法归入时判断是否存在新的独立框架职责；
3. 只有职责独立且具有稳定合同才新增分类文件；
4. 先写分类用例，再决定黄金路径是否增加稳定步骤；
5. 不因黄金路径已存在就把新能力全部塞入；
6. 更新快照、学习顺序和最终历史；
7. 复验Runner集合守恒、Quality事实和完整回归。

### 9.7 当前收集快照

将7并发/0串行更新为：

```text
截至2026-08-05：23项并发、0项串行。
```

保留说明：数量是当前快照，长期合同是最终计划与并发/串行池集合守恒。

### 9.8 本地执行入口

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_offline_service.py -q
.\.venv\Scripts\python.exe run_master.py module/offline_framework_example --collect-only -q
.\.venv\Scripts\python.exe run_master.py module/offline_framework_example -n 2 -q
```

完整Quality、黄金路径和Flaky脚本放README，规范保留简洁入口和语义合同，避免复制两份长脚本。

### 9.9 提交检查清单新增项

```text
[ ] 新能力先归入职责明确的分类文件
[ ] 黄金路径只包含稳定成功主链
[ ] 互斥异常分支留在分类用例
[ ] 离线服务状态按实例隔离且不依赖随机sleep
[ ] 分类用例不导入Quality、Metrics或Flaky内部实现
[ ] 当前离线收集快照和README已同步
[ ] SCM Checkout能够得到新增分类和黄金路径文件
[ ] 实际Jenkins、本地验收和发布状态分别记录
```

## 10. 最终code_history设计

### 10.1 文件名与章节

执行阶段6后新增：

```text
code_history/20260805_阶段6文档与发布基线验收记录.md
```

若实际执行日期变化，使用实际日期。固定章节包括：

1. 本轮目标与边界；
2. 第一性原理与TOC约束；
3. 执行前工作树和SCM状态；
4. 实际新增、修改文件；
5. 23条分类数量表；
6. 黄金路径覆盖和4/7/1/8关系；
7. 文档命令实际结果；
8. Quality关闭和开启结果；
9. Flaky多轮结果；
10. Jenkins参数、Build和归档；
11. 完整回归和Smoke收集；
12. 三层状态结论；
13. 未纳入能力和原因；
14. 静态验证与现场恢复；
15. 剩余债务和责任边界。

### 10.2 变更归属

记录必须区分：本轮计划修改、本轮实际修改、执行前已有未提交文件、运行时临时产物、未触碰保护文件。不得把阶段4或阶段5已有文件写成本轮新增。

### 10.3 数量与黄金路径

记录7个测试文件的`3/4/4/4/4/3/1`分布，总计23，并记录基础设施门禁、Runner分池和Smoke收集数量及对应提交SHA。

黄金路径记录：

```text
创建异步媒体任务
→ 首次Polling请求503后Retry挽救
→ pending
→ pending
→ success
→ 输出Capture与Schema断言
→ 并发审计查询
→ cleanup
```

运行级证据：

```text
4 operations / 7 request groups / 1 polling session / 8 request events
media_count = 1
```

### 10.4 未纳入能力

- SSE/流式响应不在本轮黄金路径；
- 真实模型服务、真实账号和真实计费不在离线范围；
- pip/npm物理断网不由现有Jenkins证明；
- 4项仓库边界失败由独立任务处理；
- 不增加第二套Runner、Quality或报告实现。

### 10.5 阻塞状态写法

若当前阻塞不变，最终记录必须包含：

```text
本地门禁：PASS
实际Jenkins：BLOCKED，阶段4核心文件未进入Jenkins可Checkout提交
完整回归增量门禁：PASS，无集合外新增失败
完整回归发布门禁：BLOCKED，仍有4项失败
整体发布：BLOCKED
```

若阻塞解除，用实际Build和零失败证据替换，不能只删除阻塞文字。

## 11. 文档命令真实性验收

### 11.1 原则

每个进入README的命令必须：从仓库根目录执行、使用全新PowerShell、不依赖前一代码块变量、不读取历史Quality产物、不需要真实Key、验证退出码和产物，并恢复环境变量与共享Runner文件。

### 11.2 `.env`前置

```powershell
if (-not (Test-Path -LiteralPath '.env')) {
  Copy-Item -LiteralPath '.env.example' -Destination '.env'
}
```

若用户已有`.env`不得覆盖。记录必须说明业务请求只访问loopback。

### 11.3 离线服务和收集

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_offline_service.py -q
.\.venv\Scripts\python.exe run_master.py `
  module/offline_framework_example `
  --collect-only -q
```

当前预期：基础设施18项通过；业务收集`23 total / 23 parallel / 0 serial`；黄金nodeid出现一次；collect-only不创建Quality身份或业务请求。

### 11.4 Quality关闭

执行README最终代码块，验收：Runner计划23项、JUnit 23 passed、Allure存在、Quality文件为0、退出码为0、共享Runner文件Hash恢复。

### 11.5 Quality开启

执行README最终代码块，验收：Runner/JUnit 23 passed、P0和Semantic完整、foreign run为0、外部URL模板为0、完整模块Metrics只允许因`usage_incomplete`降级、manifest Hash一致、产物全部位于本轮临时目录。

### 11.6 黄金路径精确验收

验收：

```text
Runner planned = 1
P0 invocations = 1
P0 request events = 8
Semantic operations = 4
Semantic request groups = 7
Semantic polling sessions = 1
Metrics = aggregated
media_count = 1
```

同时验证Retry、Polling和资源清理关系，不只检查数量。

### 11.7 Flaky动态多轮

```powershell
$stableMinSamples = [int](& .\.venv\Scripts\python.exe -c `
  'from quality.flaky import DEFAULT_FLAKY_RULE_CONFIG; print(DEFAULT_FLAKY_RULE_CONFIG.stable_min_samples)')
$rounds = $stableMinSamples + 1
```

每轮使用相同黄金nodeid和独占绝对SQLite数据库，使用新run ID和Quality目录，并保持case ID、param hash、environment和execution profile稳定。

当前规则预期4轮得到`OBSERVING、OBSERVING、STABLE、STABLE`；阈值变化时按实际状态机验收。

### 11.8 Smoke与完整回归

```powershell
.\.venv\Scripts\python.exe run_master.py module/smoke --collect-only -q
.\.venv\Scripts\python.exe -m pytest tests -q
```

Smoke当前快照为40项、15并发、25串行。完整回归分两层判定：失败集合不得新增；发布必须零失败。若仍为当前4项，阶段6文档可完成但整体发布继续`BLOCKED`。

## 12. 实际Jenkins验收与记录

### 12.1 前置条件

只有以下条件全部满足才触发：

- SCM可见性门禁通过；
- Jenkins Checkout提交SHA已知；
- Windows Agent具备Python、npm和Allure插件；
- Job独占Workspace；
- `QUALITY_FLAKY_DB_PATH`为Workspace外绝对本地路径；
- 数据库初始状态已记录；
- 用户明确允许触发外部Build。

### 12.2 参数

```text
RUN_FRAMEWORK_TESTS=false
RUN_COLLECT_ONLY=false
RUN_REAL_SMOKE=true
GENERATE_PIPELINE_SUMMARY=true
ALWAYS_SEND_REPORT_EMAIL=false
USE_CHINA_ENVIRONMENT=FALSE
SMOKE_TARGET=module/offline_framework_example
TEST_PARALLEL_WORKERS=2
```

### 12.3 Build验收

- Checkout提交包含23条离线业务用例；
- Framework Unit Tests和Collect Smoke Cases按参数跳过；
- Real Smoke计划23项并全部通过；
- 业务请求目标均为loopback；
- JUnit入口可访问；
- Allure入口消费同轮结果；
- `reports/quality`为同轮P0、Semantic、Metrics和Flaky事实；
- Pipeline Summary来自同轮来源Hash；
- Retry挽救导致`WARN`时不误判测试失败；
- 归档和邮件不泄漏密钥或本机路径。

### 12.4 记录字段

最终历史只记录Job、Build编号、Build URL、Checkout SHA、参数快照、Build结果、Pipeline结论、JUnit数量、归档可用性和业务外部URL数量。

若未触发，记录阻塞事实，不创建虚构Build编号或链接。

## 13. README与规范一致性审计

### 13.1 陈旧文本归零

```powershell
rg -n `
  '当前 7 条|当前7条|只有 7 项|只有7项|7 项并发|7项并发|尚未实现' `
  README.md FRAMEWORK_TEST_SPEC.md
```

## 14. 验收矩阵

| 验收项 | 方法 | 当前预期 | 阶段6通过标准 |
|---|---|---|---|
| 模块文件 | 文件统计 | 15 | 必需文件齐全 |
| 业务用例 | test函数统计 | 23 | 3/4/4/4/4/3/1一致 |
| 离线服务 | pytest | 18 passed | 零失败、资源回收通过 |
| Runner收集 | collect-only | 23/23/0 | 集合守恒、黄金nodeid一次 |
| Quality关闭 | Runner/JUnit | 23 passed | 无Quality身份和文件 |
| Quality开启 | P0/Semantic/Metrics | 本地PASS | 同轮、Hash可信、无外部URL |
| 黄金路径 | 机器事实 | 4/7/1/8 | 关系、Retry、Polling一致 |
| Flaky | 动态多轮 | O/O/S/S | 真实样本和身份稳定 |
| Smoke收集 | collect-only | 40/15/25 | 不退化 |
| SCM可见性 | tracked/HEAD | 当前BLOCKED | 阶段4文件进入Jenkins提交 |
| 实际Jenkins | 参数化Build | 当前BLOCKED | Build和归档实际通过 |
| 回归增量门禁 | 失败集合 | PASS | 无集合外新增失败 |
| 回归发布门禁 | pytest tests | 当前4 failed | 必须0 failed |
| README | 内容与命令 | 陈旧 | 与本轮事实一致 |
| 规范 | 规则审计 | 陈旧 | 分类和黄金边界明确 |
| code_history | 最终记录 | 待生成 | 三层状态和证据完整 |
| 范围 | git diff | 用户已有改动 | 只含允许文档差异 |

## 15. 风险与控制

### 17.1 本机文件被当成仓库文件

控制：同时检查文件存在、`git ls-files`、`git cat-file HEAD`和实际Build Checkout SHA。

### 17.2 文档完成被写成发布完成

控制：阶段6完成门禁和整体发布门禁分开记录。

### 17.3 无新增失败被写成全绿

控制：同时记录增量门禁和发布门禁，发布必须零失败。

### 17.4 修改基线制造通过

控制：`.gitignore`、`tests/quality`和fixture列入保护范围，需要修改时转独立任务。

### 17.5 README命令依赖当前Shell

控制：每个代码块在全新PowerShell执行，并在`finally`清理环境变量。

### 17.6 历史Quality产物制造假成功

控制：每次使用唯一系统临时目录、唯一run ID和manifest Hash。

### 17.7 共享Runner文件污染

控制：执行前备份bytes和Hash，逐轮取证，最终恢复并比较。

### 17.8 夸大物理断网

控制：只声明业务接口零外部请求；物理断网需预缓存依赖的独立任务。

### 17.9 Flaky旧数据库污染

控制：使用Job独占或新数据库，记录初始样本，动态读取阈值。

### 17.10 把23写成永久合同

控制：数量标记日期快照，长期门禁使用集合守恒和职责规则。

### 17.11 README和规范复制两份长脚本

控制：README保留完整脚本，规范保留简洁入口和语义合同。

### 17.12 code_history泄漏环境信息

控制：只保留相对路径、Hash、Build链接和状态，敏感值脱敏。

### 17.13 自动Git操作破坏现场

控制：不执行`git add`、`git commit`、`git clean`或批量还原。

### 17.14 执行前状态变化

控制：阶段6开始时重新取证，2026-08-05只作为起始快照。

### 17.15 命令语法正确但语义错误

控制：静态解析后真实执行，并核对退出码、数量、run ID、Hash和产物位置。

### 17.16 文档验收推动核心改动

控制：如需修改核心代码、测试或Jenkinsfile，立即停止并输出独立缺口方案。

## 16. 停止条件

出现以下任一情况立即停止，不继续写完成：

1. 模块不再是15个文件或23条用例，且无法确认来源；
2. 黄金nodeid无法收集或出现多次；
3. 阶段4文件内容与阶段5验收版本不一致；
4. 文档命令需要真实Key、真实服务或付费调用；
5. 业务HTTP出现非loopback目标；
6. Quality run ID、execution ID或Hash不一致；
7. 完整回归出现当前4项之外的新失败；
8. 需要修改核心、Quality、Runner、Pipeline或Jenkinsfile；
9. 需要覆盖用户已有`.env`或`.gitignore`；
10. Jenkins Checkout提交无法确认；
11. Jenkins产物来自不同轮次；
12. 无法恢复共享Runner文件或环境变量；
13. `git diff`出现允许范围外的可归因修改；
14. 无法区分本地、Jenkins和发布状态。

停止后输出：

```text
已完成事实
+ 阻塞事实
+ 受影响门禁
+ 不应采取的绕过方式
+ 建议独立任务
```

## 17. 阶段完成门禁

### 17.1 文档与发布基线门禁

- [ ] README不再保留当前7条陈旧事实；
- [ ] README包含23条快照和学习顺序；
- [ ] README包含Quality关闭实际成功命令；
- [ ] README包含Quality/Semantic/Metrics开启命令；
- [ ] README包含黄金nodeid；
- [ ] README包含本地和Jenkins产物位置；
- [ ] README说明Flaky动态多轮；
- [ ] README说明业务接口loopback边界；
- [ ] README准确记录三层状态；
- [ ] 规范更新23条快照；
- [ ] 规范区分分类和黄金路径；
- [ ] 规范固化确定性服务规则；
- [ ] 规范禁止业务示例验证Quality内部实现；
- [ ] 规范包含新增能力归类算法；
- [ ] 最终code_history已生成；
- [ ] 文档命令从根目录实际执行；
- [ ] 环境变量和共享Runner文件已恢复；
- [ ] `git diff --check`通过；
- [ ] 用户保护文件未被触碰。

满足后可标记：

```text
阶段6文档与发布基线收敛完成
```

### 17.2 整体发布门禁

- [ ] 阶段4核心文件进入Jenkins实际Checkout提交；
- [ ] 实际Jenkins Build按冻结参数通过；
- [ ] Jenkins JUnit、Allure、Quality和Summary可访问；
- [ ] 业务请求全部为loopback；
- [ ] 分类用例和黄金路径通过；
- [ ] 黄金路径稳定性证据有效；
- [ ] Quality、Metrics和Flaky可信；
- [ ] 完整`tests`零失败；
- [ ] Smoke收集和分池不退化；
- [ ] 无资源泄漏；
- [ ] 不需要真实密钥；
- [ ] 不存在示例专用重复实现；
- [ ] README、规范和最终历史一致。

只有全部满足才允许写：

```text
离线框架能力分类用例与黄金路径总任务：COMPLETE
```

当前起始状态不满足SCM、实际Jenkins和完整回归三项，预期整体发布仍为`BLOCKED`，除非执行前由独立任务解除。

## 18. 分阶段提交建议

如用户要求提交，建议两个原子提交：

1. `test: 纳入离线并发分类与黄金路径`
   - 只包含阶段4两个测试文件及明确依赖；
   - 用于解除Jenkins Checkout阻塞；
   - 提交前重新运行阶段4和离线模块验收。
2. `docs: 完成离线示例文档与发布基线`
   - `README.md`；
   - `FRAMEWORK_TEST_SPEC.md`；
   - 阶段5、阶段6开发方案；
   - 是否包含`code_history`由用户决定。

不得使用`git add .`或把其他未跟踪文件一起提交。本方案不执行暂存、提交、分支或远端操作。

## 19. 阶段6交付定义

执行本方案后应交付：

```text
1份与23条离线用例一致的README学习入口
+ 1份固化分类/黄金路径/确定性/Quality边界的FRAMEWORK_TEST_SPEC
+ 1组从仓库根目录实际执行成功的文档命令证据
+ 1份SCM与Jenkins可见性判断
+ 1份本地/Jenkins/发布三层状态记录
+ 1份阶段6最终code_history
+ 1个明确的整体COMPLETE或BLOCKED结论
```

当前最可能的结果：

```text
文档和历史完成
+ 本地验收PASS
+ Jenkins因SCM可见性BLOCKED
+ 完整回归因4项失败BLOCKED
+ 整体发布BLOCKED
```

若SCM、Jenkins和仓库边界债务在执行前解除，则按本方案重新取证后提升为整体`COMPLETE`；没有新证据时不得提前提升状态。

匹配结果逐项审查。历史上下文如需保留，必须标为阶段2历史，不能继续作为当前事实。

### 13.2 当前事实出现

```powershell
rg -n `
  '23|test_full_framework_flow|test_concurrency_context|4 operations|7 request groups|实际Jenkins|整体发布' `
  README.md FRAMEWORK_TEST_SPEC.md
```

关键词出现不等于通过，还要检查语义一致。

### 13.3 路径与nodeid

```powershell
Test-Path -LiteralPath 'module/offline_framework_example/test_full_framework_flow.py'
Test-Path -LiteralPath 'module/offline_framework_example/test_concurrency_context.py'
Test-Path -LiteralPath 'tests/test_offline_service.py'
Test-Path -LiteralPath 'run_master.py'
```

黄金nodeid必须通过pytest collect验证，不能只检查文件存在。

### 13.4 一致性字段

README、规范和最终历史中的以下内容必须一致：23条、23并发/0串行、黄金nodeid、4/7/1/8、Flaky动态轮数、Jenkins参数、完整回归结果、Jenkins状态和整体发布状态。

## 20. 实施任务分解

### 20.1 任务6.0：保护工作树并冻结基线

1. 记录`git status --short`；
2. 保存README、规范、`.gitignore`和阶段4文件Hash；
3. 记录分支、HEAD SHA和Python版本；
4. 统计15个文件和23条测试；
5. 记录tracked/HEAD可见性；
6. 不清理任何未跟踪文件。

完成条件：能区分执行前已有文件和阶段6可归因变更。

### 20.2 任务6.1：判断SCM与三层状态

1. 检查阶段4文件是否被跟踪；
2. 检查HEAD是否包含它们；
3. 检查本机内容是否与HEAD一致；
4. 读取阶段5验收记录；
5. 冻结本地、Jenkins和发布状态；
6. 决定是否允许实际Jenkins。

完成条件：状态表有证据，不依赖主观判断。

### 20.3 任务6.2：更新README事实与学习入口

1. 更新当前状态；
2. 更新架构链路；
3. 新增离线学习顺序；
4. 增加分类与黄金路径职责表；
5. 删除当前能力尚未实现的陈旧陈述；
6. 保持README无关章节不变。

完成条件：新开发者仅阅读README即可按正确顺序理解23条用例。

### 20.4 任务6.3：写入命令与产物说明

1. 写Quality关闭命令；
2. 写Quality/Semantic/Metrics开启命令；
3. 写黄金nodeid；
4. 写Flaky多轮规则；
5. 写本地和Jenkins产物路径；
6. 写Jenkins参数和网络边界；
7. 所有命令使用根目录相对入口。

完成条件：每个代码块在全新PowerShell中实际执行成功。

### 20.5 任务6.4：更新FRAMEWORK_TEST_SPEC

1. 更新23条快照；
2. 新增三层离线测试职责；
3. 新增分类文件职责表；
4. 固化黄金路径边界；
5. 固化确定性服务要求；
6. 固化Quality内部隔离；
7. 更新能力归类算法和检查清单。

完成条件：规范能指导后续新增能力，而不只描述当前实现。

### 20.6 任务6.5：执行文档命令

依次执行离线服务、collect-only、Quality关闭、Quality开启、黄金路径、Flaky多轮、Smoke收集和完整回归，最后恢复环境与共享Runner文件。

完成条件：README命令和预期事实来自本轮执行。

### 20.7 任务6.6：条件执行实际Jenkins

SCM门禁通过且用户允许时使用冻结参数执行；否则记录`BLOCKED`且不触发。

完成条件：实际Build证据或明确阻塞原因二选一，不允许模拟替代。

### 20.8 任务6.7：生成最终code_history

写实际文件变更、23条分布、黄金路径、运行门禁、三层状态、未纳入能力、现场恢复和静态校验。

完成条件：记录可独立回答做了什么、证据是什么、为什么尚未发布。

### 20.9 任务6.8：一致性与范围审查

搜索陈旧7条描述，比较三份文档状态，验证代码围栏、UTF-8无BOM、LF、最终换行和`git diff --check`，确认没有生产或测试代码差异。

完成条件：差异只包含允许文档和历史文件。

## 21. 权威命令索引

### 21.1 工作树与范围

```powershell
git status --short
git diff --check
git diff -- README.md FRAMEWORK_TEST_SPEC.md
```

### 21.2 文件和测试统计

```powershell
Get-ChildItem -LiteralPath module/offline_framework_example -File
Get-ChildItem -LiteralPath module/offline_framework_example -Filter 'test_*.py' |
  ForEach-Object {
    Select-String -LiteralPath $_.FullName -Pattern '^\s*def test_'
  }
```

### 21.3 SCM可见性

```powershell
git -c core.quotepath=false ls-files -- module/offline_framework_example
git cat-file -e HEAD:module/offline_framework_example/test_concurrency_context.py
git cat-file -e HEAD:module/offline_framework_example/test_full_framework_flow.py
```

### 21.4 运行入口

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_offline_service.py -q
.\.venv\Scripts\python.exe run_master.py module/offline_framework_example --collect-only -q
.\.venv\Scripts\python.exe run_master.py module/offline_framework_example -n 2 -q
.\.venv\Scripts\python.exe -m pytest `
  'module/offline_framework_example/test_full_framework_flow.py::TestFullFrameworkFlow::test_offline_async_media_flow' `
  -q
.\.venv\Scripts\python.exe run_master.py module/smoke --collect-only -q
.\.venv\Scripts\python.exe -m pytest tests -q
```

### 21.5 陈旧文档检查

```powershell
rg -n `
  '当前 7 条|当前7条|只有 7 项|只有7项|7 项并发|7项并发|尚未实现' `
  README.md FRAMEWORK_TEST_SPEC.md
```
