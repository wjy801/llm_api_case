# 接口测试框架能力增强路线图

## 1. 目标

本路线图关注测试框架自身的代码能力，但所有框架能力都应服务于当前业务自动化建设。业务自动化处于起步阶段时，优先解决已经出现的复用、稳定性和定位问题，不为尚未出现的场景提前建设完整平台。

建设目标：

- 提高框架的可扩展性，新增能力时尽量不修改核心请求流程。
- 提高执行稳定性，统一治理重试、轮询、并发和不稳定用例。
- 提高通用性，使公共能力可以支持后续增加的不同接口类型。
- 提高可测试性，使框架核心能力不依赖真实接口也能完成回归。
- 形成能够在面试中完整讲解的框架设计与工程实践。

## 2. 当前已有能力

当前框架已经具备：

- 基于 `pytest + requests` 的代码式接口测试。
- 请求、断言、业务任务和装饰器分层。
- OpenAI Chat Completions、Responses 和 Anthropic Messages 协议测试。
- 同步接口、异步任务轮询和 SSE 流式响应测试。
- pytest-xdist 用例级并发执行。
- 请求和响应日志、cURL 生成及 Allure 附件。
- 模型结果及前置媒体资源下载。
- 账户余额、调用计费和并发计费校验。
- Allure HTML 报告及历史报告管理。
- 框架核心模块单元测试。

下一阶段采用“业务覆盖与框架能力同步演进”的方式：先通过真实用例验证需求，再将重复且稳定的能力沉淀到框架。

## 3. 功能建设优先级

| 优先级 | 框架能力 | 核心价值 | 建议投入 |
| --- | --- | --- | --- |
| P0 | 请求中间件机制 | 建立统一扩展点，承载日志、脱敏、重试、追踪和指标 | 最高 |
| P0 | 配置校验与安全保护 | 避免配置缺失、密钥泄露和环境误用 | 高 |
| P1 | 基础契约断言 | 使用 JSON Schema 统一校验核心响应结构 | 中高 |
| P1 | 重试策略与轮询状态机 | 提升异常场景下的稳定性和语义准确性 | 高 |
| P1 | 测试上下文与变量传递 | 支持复杂接口链路和并发隔离 | 中高 |
| P2 | 轻量 Mock 与故障模拟 | 对框架关键分支进行稳定、离线的单元测试 | 中 |
| P2 | Flaky 治理插件 | 区分真实缺陷与瞬态失败 | 中 |
| CI 后 | 指标聚合与质量门禁 | 有稳定执行数据后建立自动质量判定 | 按条件启动 |

## 4. P0：请求中间件机制

### 4.1 建设原因

当前请求日志、媒体资源处理等逻辑直接进入请求主流程。后续继续加入重试、脱敏、指标和链路追踪，会导致 `BaseRequest` 职责不断膨胀。

建议将请求生命周期抽象为可插拔中间件。

### 4.2 生命周期设计

```python
class RequestMiddleware:
    def before_request(self, context):
        pass

    def after_response(self, context, response):
        pass

    def on_exception(self, context, error):
        pass
```

`RequestContext` 建议包含：

- HTTP method、URL、headers、params 和 body。
- 请求开始时间、当前重试次数和 trace id。
- 用例 nodeid、worker id 和环境信息。
- 可供中间件共享数据的 attributes 字典。

### 4.3 内置中间件

- 第一批实现 `RedactionMiddleware`：对请求头、查询参数和请求体递归脱敏。
- 第一批实现 `LoggingMiddleware`：生成请求、响应、异常及 cURL 日志。
- 在重试策略落地时接入 `RetryMiddleware`。
- `AuthMiddleware`、`TraceMiddleware` 和 `MetricsMiddleware` 只保留扩展位置，出现真实需求后再实现。

### 4.4 关键要求

- 支持配置中间件顺序。
- 一个中间件失败时能够明确定位异常来源。
- 每次请求使用独立上下文，避免 xdist 和线程并发下数据串扰。
- 保持现有 `get/post/put/patch/delete` 调用方式兼容。
- 中间件本身必须可以通过 Mock 独立测试。
- 第一版不实现动态发现、热插拔和复杂依赖注入，使用显式列表注册即可。

### 4.5 验收标准

- 新增指标或追踪能力时无需修改 `BaseRequest.request()` 主流程。
- Authorization、Cookie、API Key 等信息不会出现在 Allure 明文附件中。
- 中间件顺序、异常传播和并发隔离均有单元测试。

## 5. P0：配置校验与安全保护

### 5.1 建设目标

在用例执行前完成必要配置校验，避免运行过程中才因 URL、密钥或超时配置错误而失败；同时防止敏感信息泄露及测试误发往错误环境。

### 5.2 第一版能力

- 校验 base URL、API Key、timeout 等必填项和基本类型。
- 明确环境变量与默认值的优先级。
- 缺失配置时输出具体变量名称，不抛出模糊的 `AttributeError`。
- 日志、异常、cURL 和 Allure 附件共用同一套脱敏规则。
- 为高成本或具有破坏性的用例保留显式开关。

第一版可以继续使用 dataclass，不强制引入 Pydantic、多配置文件合并或远程配置中心。

### 5.3 验收标准

- 配置错误在测试开始前被发现。
- 敏感信息不会出现在控制台和测试报告中。
- 配置校验逻辑具有独立单元测试。

## 6. P1：基础契约断言

### 6.1 建设目标

在核心业务接口中使用 JSON Schema 统一校验响应结构，减少重复的字段类型和必填项断言。

### 6.2 基础 API

```python
assertions.assert_schema(response, schema)
assertions.assert_headers(response, rules)
assertions.assert_response_time(response, max_seconds=2)
assertions.assert_json_contains(response, expected)
assertions.assert_json_types(response, rules)
```

第一版只要求实现 `assert_schema`；其余断言根据真实用例中的重复需求逐步增加。

### 6.3 建议能力

- 使用 JSON Schema 校验字段类型、必填项、枚举值和数据范围。
- 输出精确差异：JSONPath、期望类型、实际类型和实际值。
- 优先覆盖稳定的成功响应和标准错误响应。

### 6.4 后续扩展

当业务已经维护稳定、可信的 OpenAPI 文档后，再考虑自动加载 Schema、缓存以及 Schemathesis/Hypothesis 用例生成。可生成：

- 边界值。
- 缺失必填字段。
- 非法枚举值。
- 错误字段类型。
- 超长字符串和空集合。

自动生成用例失败后，需要保存能够稳定复现的最小请求数据。

### 6.5 验收标准

- 校验失败信息能够直接定位到具体字段。
- Schema 校验逻辑具有单元测试。
- 至少为成功响应和标准错误响应各建立一套 Schema。

## 7. P1：重试策略与轮询状态机

### 7.1 重试策略

建议抽象 `RetryPolicy`：

```python
RetryPolicy(
    max_attempts=3,
    retry_statuses={429, 500, 502, 503, 504},
    backoff="exponential",
    jitter=True,
    respect_retry_after=True,
)
```

必须明确以下规则：

- 连接失败、连接超时、429 和指定 5xx 才允许自动重试。
- GET、HEAD 等幂等方法可以默认重试。
- POST 只有显式声明幂等键或业务允许时才能重试。
- 支持指数退避、随机抖动和 `Retry-After`。
- 每次重试原因、等待时间和响应结果写入 Allure。
- 同时限制最大次数与最大总耗时。
- 自动重试默认关闭，由具体接口或用例显式启用，避免掩盖真实服务问题。

### 7.2 轮询状态机

```python
PollingPolicy(
    status_json_path="$.status",
    pending={"queued", "running"},
    success={"succeeded"},
    failure={"failed", "cancelled"},
)
```

建议支持：

- 明确区分等待、成功、失败和未知状态。
- 超时异常携带最后状态及最后响应。
- 记录状态迁移序列与每个状态持续时间。
- 可以配置结果 JSONPath 和错误 JSONPath。
- 未知状态默认失败或按策略处理，不能直接视为成功。

### 7.3 验收标准

- POST 不会在没有幂等保证时自动重试。
- 能正确解析数字和 HTTP 日期格式的 `Retry-After`。
- 429、5xx、超时、失败状态和未知状态均有测试覆盖。

## 8. P2：轻量 Mock 与故障模拟

### 8.1 建设目标

优先使用 `responses`、`requests-mock` 或 pytest `monkeypatch` 模拟请求结果，使重试、轮询、日志和流式解析的关键分支能够脱离真实环境回归。第一阶段不自研独立 Mock Server。

### 8.2 故障场景

- 连接失败与读取超时。
- 固定延迟和随机延迟。
- 429、500、502、503、504。
- 非法 JSON 和字段类型错误。
- SSE 中途断流、非法 chunk 和缺失 `[DONE]`。
- 轮询状态按 `queued -> running -> succeeded` 迁移。
- 轮询进入 failed、cancelled 或未知状态。
- 返回重复响应、乱序响应或缺少请求 ID。

### 8.3 验收标准

- CI 中无需访问真实模型接口即可测试框架核心能力。
- 同一个故障场景可以稳定重复执行。
- 能验证重试次数、退避时间、日志内容和最终异常类型。
- 只有在需要验证真实 Socket、SSE 断流或 HTTP 协议行为时，再引入本地 Mock Server。

## 9. P1：测试上下文与变量传递

### 9.1 建议 API

```python
context.extract("task_id", response, json_path="$.task_id")
context.extract("request_id", response, header="x-oneapi-request-id")

task_id = context.get("task_id")
```

### 9.2 建议能力

- 从 JSONPath、Header、Cookie 和正则表达式中提取变量。
- 第一版只支持用例级作用域，避免跨用例污染。
- 在 xdist、线程并发和异步任务下保证上下文隔离。
- 支持用例结束后的资源清理回调。
- 变量不存在或类型不匹配时给出明确错误。

类级、会话级共享变量以及持久化上下文应在出现明确业务需求后再增加。

### 9.3 注意事项

坚持代码式用例，不需要为了变量传递引入 YAML 或 Excel 驱动。上下文应当帮助组合复杂接口链路，而不是形成新的隐式 DSL。

## 10. CI 接入后：指标聚合与质量门禁

### 10.1 启动条件

该能力在测试稳定接入 CI 后再实现。开始建设前至少满足：

- CI 中已经存在持续、可重复的接口测试任务。
- 已积累足够的历史执行数据，可以建立合理基线。
- 执行环境与并发配置相对稳定，指标具有可比性。
- 团队已经明确指标超限后的处理责任和流程。

### 10.2 建议采集指标

- 请求总数、成功率及状态码分布。
- 平均耗时、P50、P95 和 P99。
- 重试次数和重试率。
- SSE 首 Token 延迟、总耗时和 chunk 间隔。
- 异步任务排队时间、执行时间和总完成时间。

### 10.3 建议门禁

```powershell
pytest --max-p95 2.0 --min-success-rate 99 --max-retry-rate 5
```

超出门禁时应：

- 使测试任务返回失败状态。
- 输出超限接口、实际值与阈值。
- 生成可供 CI 读取的 JSON 汇总。
- 在 Allure 中附加本次执行指标。

### 10.4 定位

这不是专业压测平台的替代品，而是面向接口回归的轻量性能基线与质量门禁。

## 11. P2：Flaky 治理插件

Flaky 治理能力保留，但分阶段建设：接入 CI 前先统一失败分类和结果记录；接入 CI 并积累历史数据后，再实现失败率、连续失败次数和排行榜。

建议实现：

- 对失败进行指纹分类：网络超时、429、服务端 5xx、断言失败、代码异常等。
- 只允许瞬态错误进入自动重试。
- 保存首次失败和重试结果，不能用最终通过掩盖首次失败。
- 支持 `flaky` 和 `quarantine` marker。
- CI 接入后统计历史失败率、重试通过率和连续失败次数。
- CI 接入后输出不稳定用例排行榜及建议处理方式。

不建议直接对所有失败统一重跑，这会隐藏真实缺陷并增加执行成本。

## 12. 目录演进原则

当前不进行 `core/plugins/adapters` 式整体目录重构。新能力优先放入现有 `common`、`util` 和 `tests`，只有单个文件职责过多或出现两个以上稳定实现时再拆分目录。

迁移应保持现有 `BaseRequest`、`BaseAssertions` 和用例调用方式兼容，保证每一步都可运行、可测试、可回滚。

## 13. 推荐实施顺序

### 第一阶段：安全与扩展基础

1. 补充配置启动校验与高风险用例保护。
2. 建立请求中间件接口和单次请求上下文。
3. 将现有日志功能迁移为 `LoggingMiddleware`。
4. 实现全链路 `RedactionMiddleware`。
5. 为中间件顺序、异常和并发隔离补齐单元测试。

阶段成果：敏感信息不泄露、错误配置尽早失败，并形成后续能力可复用的请求扩展点。

### 第二阶段：契约与接口链路

1. 建立最小 JSON Schema 断言。
2. 为核心成功响应和标准错误响应建立契约样例。
3. 建立测试上下文的用例级存储、提取和清理能力。
4. 验证测试上下文在线程与 xdist 并发下相互隔离。

阶段成果：核心响应结构可以复用校验，复杂接口链路可以安全地传递必要变量。

### 第三阶段：稳定性

1. 实现默认关闭、由用例显式启用的 `RetryPolicy`。
2. 将现有轮询升级为 `PollingPolicy` 状态机。
3. 使用 Mock 库模拟超时、429、5xx 和轮询状态迁移。
4. 对请求、重试、轮询和 SSE 关键分支进行离线回归。

阶段成果：框架稳定性能力可以被自动验证，而不是依赖真实环境观察。

### 第四阶段：Flaky 基础治理

1. 建立失败类型和失败指纹模型。
2. 保留首次失败证据，只对明确的瞬态错误重试。
3. 增加 `flaky`、`quarantine` marker 及隔离规则。
4. 输出本次执行的不稳定用例和重试结果。

阶段成果：能够区分产品缺陷、框架问题和环境瞬态失败，不用无差别重跑掩盖问题。

### 第五阶段：接入 CI 后的质量门禁

1. 在固定执行环境中积累稳定的历史数据。
2. 聚合成功率、耗时分位数、重试率等指标。
3. 根据历史基线设置质量门禁，而不是预先硬编码阈值。
4. 增加 Flaky 历史失败率、连续失败次数和趋势输出。

阶段成果：基于持续数据形成执行、观测、判断和治理闭环。

## 14. 秋招前最小可交付版本

如果时间有限，建议优先完成：

1. 请求中间件机制，并落地日志与全链路脱敏中间件。
2. 可显式启用的重试策略与语义明确的轮询状态机。
3. 用例级测试上下文与变量提取，并保证并发隔离。
4. Flaky 失败分类、首次失败证据保留与隔离标记。

基础 JSON Schema 断言可以与业务用例同步补充，但暂不要求 OpenAPI 自动加载和自动生成用例。

这些能力能够集中体现：

- 面向扩展的框架设计能力。
- 对请求生命周期、并发隔离和扩展边界的理解。
- 对测试稳定性、失败诊断和治理的理解。
- 单元测试、兼容迁移和工程化交付能力。

## 15. 不建议优先投入的方向

秋招准备时间有限时，不建议优先开发：

- Web 用例管理后台。
- YAML 或 Excel 用例平台。
- 大量与具体模型绑定的新增用例。
- 仅封装第三方库、没有自定义治理逻辑的失败重跑。
- 与当前项目场景无关的数据库、Redis 等连接器堆砌。
- pytest 扩展注册中心和按业务维度持续增加的命令行参数。
- 业务 OpenAPI 尚不稳定时建设自动用例生成平台。
- 在普通 Mock 库足够时自研独立 Mock Server。

这些功能开发量较大，但对当前阶段的业务覆盖和框架稳定性提升有限。
