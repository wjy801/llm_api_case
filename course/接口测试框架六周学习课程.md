# 接口测试框架源码架构与证据链六周课程

**课程副标题：基于现有项目实现的调用链、状态、资源所有权与机器证据学习。**

## 1. 课程定位

本文档是当前仓库接口测试框架课程的**唯一课程合同**。课程共6周、30个学习日，面向具备基础接口自动化经验的中级测试开发工程师，学习对象仅限仓库中已经存在并通过公开入口使用的实现。

课程目标不是重新开发一套框架，而是建立可复述、可核验的实现模型：

1. 从公开入口定位到真实实现文件。
2. 沿调用链解释对象、状态、数据和资源如何流转。
3. 使用已有离线测试、collect-only和机器产物验证当前实现合同。
4. 区分业务执行事实、Runner事实、Quality事实和Reporting视图。
5. 解释依赖方向、资源所有权、安全边界和产物职责。

### 明确边界

- **只学习现有实现，不要求新增业务功能或框架能力。**
- **不做故障注入，不故意改坏代码、Manifest、JSONL、数据库或报告文件。**
- 不要求重新实现Request、Retry、Polling、Runner、Quality或Reporting。
- 不把修复仓库已知缺陷作为结业门禁。
- 不执行未经授权的真实Smoke、付费接口或共享业务状态用例。
- 不直接修改Jenkins使用的持久化Flaky数据库。

### 目标学员与建议前置能力

- 能独立使用Python虚拟环境并阅读类型标注、上下文管理器和基础并发代码。
- 能使用pytest运行测试、选择nodeid，并解释fixture与setup/call/teardown基础生命周期。
- 能使用requests或同类HTTP客户端区分请求、响应、传输异常和业务断言失败。
- 能阅读JSON、JSONL、JUnit XML和基础SQLite结构。
- 对ContextVar、状态机、Hash、Manifest或Jenkins不熟悉时可以学习，但必须先通过准备度检查并按周门禁推进。

### Day 0准备度检查

Day 0不是新增学习日，而是进入课程前的准备度门禁。学员应完成以下六项检查：

1. 使用nodeid运行一个指定pytest用例，并说明选择范围。
2. 解释一个fixture的创建、使用和清理时机。
3. 区分HTTP业务失败、传输异常和断言失败。
4. 阅读一段包含类型标注和上下文管理器的Python代码。
5. 区分JSON对象、JSONL记录流和JUnit XML测试结果。
6. 说明线程共享对象可能产生的数据竞争或资源污染。

准备度不足不阻止阅读第1～3周，但进入第4周前必须补齐对应基础能力。

---

## 2. 第一性原理学习模型

复杂框架学习的本质不是记住文件名，而是回答七个稳定问题：

```text
问题由谁接收
→ 数据经过哪些对象
→ 状态由谁维护
→ 副作用由谁拥有
→ 结果由什么证据证明
→ 哪些层不能互相替代
→ 为什么选择当前方案，何时应换方案
```

当前项目的主链路为：

```text
module业务用例
→ common请求、可靠性、上下文和中性观察接口
→ run_master.py / run_orchestration执行编排
→ quality事实、语义、指标和Flaky状态
→ pipeline_reporting / Jenkins报告与发布
```

必须保持的核心依赖方向：

```text
module -> common
quality -> common.runtime_hooks
run_master -> run_orchestration
common -X-> quality
```

每天使用同一学习闭环：

```text
限定一个实现问题
→ 找到公开入口
→ 沿真实调用顺序阅读
→ 标注对象、状态、资源与副作用
→ 运行已有离线验证
→ 把测试或产物映射回实现节点
→ 对比一个替代方案与不适用条件
→ 输出图表并迁移到相邻现有场景
```

---

## 3. TOC学习策略

当前学习约束是抽象模型固化速度，而不是技术内容不足：

```text
第4～6周抽象概念连续输入
→ 前一模型尚未固化就进入下一证据层
→ 术语、关系和证据同时累积
→ 学员依赖模板完成图表
→ 能复述当前实现但难以独立迁移
```

当前更直接的TOC约束已经转为：

```text
每日成果输入过多
→ 时间被表格、重复制图和记录占用
→ 源码追踪与测试证据核对时间下降
→ 学员重新依赖模板完成课程
→ 看似产出完整，但无法独立解释真实实现
```

课程按实现所有权拆成30天，每天只建立一个主要模型，最多包含一个强相关辅助模型。每课只要求一个主成果，图、表、状态说明和设计取舍均作为同一成果的章节；页面中的其他输入只作为可选草稿。每周最后一天在本周已有主成果中完成一次综合更新，不新增四份独立提交物。

| 环节 | 建议时间 |
| --- | --- |
| 实现问题、公开入口与文件地图 | 15～20分钟 |
| 源码调用链、对象、状态与所有权追踪 | 60～80分钟 |
| 现有测试或机器产物验证 | 25～35分钟 |
| 设计选择与适用边界 | 10～15分钟 |
| 主成果增量与证据位置记录 | 10～20分钟 |
| 自测与退出门禁 | 5～10分钟 |

普通日总预算为2～3小时。Day5、10、15、20和25的30～45分钟复盘包含在当天2.5～3小时内，不再叠加到日课之外；Day29与Day30安排3～4小时，并分别包含材料整合和最终复盘答辩。

---

## 4. 统一术语与关系索引

术语表用于定位对象关系，不用于孤立名词记忆。每个日课只要求掌握本周首次出现的术语，高级术语首次使用时统一回到本节核对所属层、所有权和证据。

### 4.1 业务与用例层

| 术语 | 所有权与上下游 | 不等同于 | 首次学习日 | 主要证据 |
| --- | --- | --- | --- | --- |
| Test | 选择场景、调用Task并完成业务断言 | Task实现本身 | Day2 | pytest nodeid、Allure步骤 |
| Task | 编排一个业务能力调用 | HTTP传输层 | Day2 | 模块Task测试 |
| Request | 负责HTTP调用与可靠性协作 | 业务通过条件 | Day2 | loopback请求测试 |
| Assertions | 解释业务Response语义 | Request状态码检查 | Day2 | 断言专项测试 |
| fixture | 创建并清理测试依赖 | 全局单例 | Day4 | pytest生命周期 |
| nodeid | pytest测试对象的稳定执行标识 | Quality execution_id | Day1 | collect-only输出 |

### 4.2 公共能力层

| 术语 | 所有权与上下游 | 不等同于 | 首次学习日 | 主要证据 |
| --- | --- | --- | --- | --- |
| RequestContext | 持有单次attempt输入、响应与诊断属性 | TestContext | Day6 | BaseRequest测试 |
| Middleware | 在请求前后附加诊断或捕获副作用 | Retry决策器 | Day7 | Middleware顺序测试 |
| Capture | 捕获输入输出资源并管理下载任务；取消请求不等于线程已经结束 | Artifact可信消费或同步收口证明 | Day8 | Capture与下载测试、done_event |
| Retry | 管理单次业务动作的可恢复重试 | Polling状态循环 | Day9 | Retry决策测试 |
| Polling | 管理任务状态与统一deadline | Retry等待 | Day10 | Polling分类测试 |
| SSE | 管理流式事件消费与连接生命周期 | 普通一次性Response | Day11 | Streaming测试 |
| TestContext | 按“提取/缺失处理 → transform → expected_type校验 → 写入”保存跨步骤数据，并按LIFO清理资源 | RequestContext | Day12 | Context提取与清理测试 |
| ContextVar | 保存当前执行上下文并支持显式传播 | 任意线程全局变量 | Day13 | 并发隔离测试 |
| Runtime Hooks | common发出的中性观察能力面 | Quality实现 | Day18 | Runtime Hooks专项测试 |
| Lease | 把开始与结束事件绑定到同一Provider | 数据库租约 | Day18 | Provider切换测试 |

### 4.3 Runner与执行层

| 术语 | 所有权与上下游 | 不等同于 | 首次学习日 | 主要证据 |
| --- | --- | --- | --- | --- |
| authoritative collection | 一次权威收集形成唯一nodeid集合 | 各执行池独立收集 | Day15 | collect-only摘要 |
| Execution Plan | 保存目标、分池和计划事实 | 实际测试结果 | Day15 | Runner计划模型 |
| Pool Result | 保存单个执行池状态与原始退出码 | 最终进程退出码 | Day16 | execution-result.json |
| execution_id | 标识一次执行池或执行单元 | Quality父run_id | Day16 | execution-result与P0引用 |
| final_exit_code | Runner根据池结果计算的最终退出码 | 报告页面结论 | Day16 | Runner退出码测试 |

### 4.4 Quality层

| 术语 | 所有权与上下游 | 不等同于 | 首次学习日 | 主要证据 |
| --- | --- | --- | --- | --- |
| run_id | 标识一轮Quality事实与派生产物 | pytest nodeid | Day19 | Quality run.json与Manifest |
| P0事实 | worker运行期产生的基础Case与Request事实 | Metrics指标 | Day20 | worker JSONL分片 |
| submission | worker声明本轮实际提交的分片集合 | 最终Manifest | Day21 | submission清单 |
| Manifest | 声明身份、版本、提交状态、完整性状态与目标产物Hash；是否可消费仍由消费者准入策略决定 | 文件存在性或发布者真实性证明 | Day21 | 各级Manifest与消费者校验 |
| Semantic | 当前实现只以P0 run_id、status=complete和request-metrics Hash作为来源门禁，再与Semantic分片建立Case、Operation、Request Group等关系 | 完整性可信P0；当前未检查P0版本和integrity_status | Day22 | Semantic JSONL与Manifest |
| Metrics | 同时消费满足准入条件的P0与Semantic关系，在来源与分母可解释后派生指标 | 原始事实 | Day23 | run-metrics.json |
| Flaky identity | 从满足Flaky导入准入条件的P0 Case、Failure和完整性事实独立建立跨run稳定身份，不依赖Semantic或Metrics | 单次execution_id | Day24 | identity与import测试 |
| epoch | 可比较历史的连续阶段 | 单次Observation | Day24 | Flaky repository记录 |
| detected_state | 自动状态域中的检测/评估基线；部分人工裁决和恢复完成会重新锚定或同步它 | 永远不受人工操作影响的纯机器列 | Day25 | projection与override测试 |
| current_state | 叠加自动检测、人工裁决和QUARANTINED/RECOVERING治理阶段后的有效状态 | 单纯的人工状态列 | Day25 | governance审计记录 |

### 4.5 Reporting与CI层

| 术语 | 所有权与上下游 | 不等同于 | 首次学习日 | 主要证据 |
| --- | --- | --- | --- | --- |
| Source | Reporting读取的一类上游事实及其状态 | 最终报告 | Day27 | Source加载测试 |
| PipelineReport | 汇总上游事实后形成的单一结论对象 | Markdown文本 | Day27 | Reporting模型测试 |
| Markdown视图 | 面向人的PipelineReport渲染 | 新的事实计算 | Day27 | summary.md |
| 机器JSON | 面向机器的同一PipelineReport渲染 | Quality Manifest | Day27 | summary.json |
| Jenkins阶段状态 | CI编排、归档与通知结果 | Python报告语义 | Day28 | Jenkinsfile阶段 |

---

## 5. 学习规则、设计边界与评分

- 默认只执行`tests/`和`module/offline_framework_example/`中的确定性离线内容。
- `module/smoke`默认只执行collect-only，不调用真实业务接口。
- 阅读代码时沿调用链进入下一层，不按目录从头到尾背诵。
- 现有测试、页面推演器和机器产物用于验证实现，不用于人为制造异常。
- Flaky课程只使用CLI帮助、已有专项测试、测试夹具或临时SQLite数据库。
- Day30操作`reports/`前必须先备份需要保留的本地产物，完成后恢复。
- 不把`.env`、API Key、真实响应、数据库或账号数据写入学习成果。
- 测试数量是日期化快照，用于识别变化，不是永远不变的业务合同。
- `README.md`是架构基线，`FRAMEWORK_TEST_SPEC.md`是用例与质量语义基线。

### 5.1 每课设计选择与适用边界

30个日课都必须回答一个关键设计选择：当前方案是什么、替代方案是什么、为什么在本仓库采用当前方案、什么条件下当前方案不再适用。对于事实读取类课程，可以比较“由当前层负责”与“交给相邻层负责”的后果，不强行扩展新的技术产品。

### 5.2 每周复盘产出

Day5、10、15、20和25末尾预留30～45分钟，但复盘时间已经包含在当天总预算内。复盘不新增独立成果：学员只需在本周已有主成果中完成一次综合更新，至少包含一处关系修正、一处相邻场景迁移和必要的术语关系变化。Day30在同一答辩包中完成第6周及全课程关系复盘。

30个日课各保留一条强制成果证据记录，用于填写成果路径，或记录口头验收时间、追问与一个可核对事实。其他页面输入统一标记为可选草稿并默认折叠；空白不会阻断门禁。文件成果与口头记录二选一，不要求重复填写两份证明。

### 5.3 六维评分量表

| 维度 | 0分 | 1分 | 2分 |
| --- | --- | --- | --- |
| 源码定位 | 无法找到入口或实现 | 可按材料找到 | 可从测试或公开入口独立追踪 |
| 调用链与状态 | 只能复述名词 | 能说明主路径 | 能解释分支、终态和终止责任 |
| 所有权 | 状态和资源归属混乱 | 能列出主要所有者 | 能解释转移、清理和禁止越权 |
| 证据映射 | 只记录测试通过 | 能指出一个测试或产物 | 能说明证据证明什么及不能证明什么 |
| 设计边界 | 无法说明选择原因 | 能复述当前设计理由 | 能比较替代方案和不适用条件 |
| 迁移能力 | 只能填写原模板 | 能在提示下迁移 | 能独立映射到相邻现有场景 |

每周总分12分，通过线为9分；“所有权”和“证据映射”不得为0分。任一0分项必须完成错误订正后再进入下一周。日课复选框只记录进度，不能替代成果证据和周评分。

截至2026-08-07的工作树观察值：离线课程模块23项、Polling分类4项、Smoke collect-only 40项；`tests/`收集686项，执行结果为682 passed、4 failed。该快照只用于说明当前环境，不替代重新执行后的本轮事实。

---

## 6. 六周总览

| 周次 | 学习主题 | 核心目录 | 周目标 |
| --- | --- | --- | --- |
| 第1周 | 项目边界与业务用例分层 | `module/offline_framework_example/`、`common/base_task.py`、`common/base_assertions.py` | 建立完整框架地图，解释规范业务用例如何组合公共能力 |
| 第2周 | 请求主干与可靠性实现 | `common/base_request.py`、Middleware、Capture、Retry、Polling | 解释请求从输入到终态的实现链路和预算归属 |
| 第3周 | 流式、上下文、并发与收集 | `common/streaming.py`、`common/test_context.py`、`run_master.py` | 解释资源所有权、上下文隔离和权威执行计划 |
| 第4周 | Runner、Allure与Quality接入 | `run_orchestration/`、Allure生命周期、Runtime Hooks | 解释执行事实如何产生以及Quality如何可选接入 |
| 第5周 | Artifact基础准入、Quality归并、语义、指标与Flaky | `quality/`、Manifest消费者 | 区分提交完成、完整性状态和消费者准入，再解释P0如何分叉形成关系、指标和历史状态 |
| 第6周 | Artifact高级边界、Reporting、Jenkins与综合串联 | `util/artifact_io.py`、`pipeline_reporting/`、`Jenkinsfile` | 比较跨消费者策略与Hash/签名边界，解释报告视图、CI编排和同轮证据链 |

### 独立课程入口约定

HTML版总纲中的30个学习日是独立日课的固定入口。日课统一位于`course/分阶段课程/`，文件名由六周生成器中的连续映射确定。总纲只显示文件是否已经生成，不把历史四周课程继续作为第二份课程合同。

---

# 第1周：建立项目实现地图

## 本周目标

从框架使用者视角理解一条离线业务用例如何组合Test、Task、Request和Assertions，并建立业务层、公共层、编排层、质量层和报告层的总体边界。

## Day 1：项目边界与执行主线

- **实现问题**：一个测试目标如何从稳定入口进入业务用例、公共能力和执行编排。
- **核心入口**：`README.md`、`FRAMEWORK_TEST_SPEC.md`、`run_master.py`。
- **离线验证**：对`module/offline_framework_example`执行collect-only，观察入口提前返回和分池摘要。
- **术语使用**：学习统一术语表的所属层、首次学习日和“不等同于”字段，本日只要求掌握首周术语。
- **可核验产出**：五层架构图、稳定入口表、collect-only提前返回位置和准备度检查记录。

## Day 2：Module用例最小闭环与模块四件套

- **用例编写问题**：Test、Task、Request、Assertions如何完成一次规范业务调用，并保持输入、动作、传输、断言和资源责任清晰。
- **核心入口**：`FRAMEWORK_TEST_SPEC.md`与`module/video_model/`、`module/image_model/`中的标准业务用例。
- **事实边界**：可以读取黄金用例中与标准Module重合的Test、Task、Request、Assertions、fixture和清理部分；service只作为HTTP/SSE边界对端，不展开内部状态、线程、路由和故障注入实现。
- **可核验产出**：四件套完整调用链、输入输出表和资源所有权表。

## Day 3：模块标准结构与任务能力

- **实现问题**：模块目录如何分配公开用例、任务编排和请求能力。
- **核心入口**：`common/base_task.py`、模块Task与模块公开导出。
- **离线验证**：运行现有模块结构与Task相关测试。
- **可核验产出**：模块目录职责表、Task能力路由图和禁止越层清单。

## Day 4：测试生命周期断言与报告步骤

- **实现问题**：pytest的setup、call、teardown如何与断言及Allure步骤对应。
- **核心入口**：fixtures、`common/base_assertions.py`、模块断言和报告步骤。
- **离线验证**：运行已有断言与报告相关离线测试并查看原始Allure结果。
- **可核验产出**：pytest生命周期与报告步骤对照表、断言职责边界。

## Day 5：Module用例分类与设计边界

- **用例编写问题**：正向、契约、错误边界、鉴权隔离、资源清理、异步状态和串行场景如何分类。
- **核心入口**：`FRAMEWORK_TEST_SPEC.md`、真实`module/`测试与现有业务用例设计文档。
- **事实边界**：组合流程只提供组合证据，不作为逐行教学主线，也不替代分类用例。
- **可核验产出**：场景分类图、测试文件职责表、断言与清理矩阵和第1周完整调用链。
- **周复盘30～45分钟**：连接Module边界、四件套、标准目录、生命周期和用例分类，说明每类变化的主要所有者。

## 第1周门禁

- [ ] 已从一个离线测试定位到Task、Request和Assertions。
- [ ] 已提交Test到Assertions的完整调用链图。
- [ ] 能解释业务层为什么不能直接承担Runner或Quality职责。
- [ ] 能区分公开入口、内部实现和机器证据。
- [ ] 未运行未经授权的真实接口。
- [ ] 已按六维量表完成第1周评分，总分至少9分且所有权、证据映射均不为0分。

---

# 第2周：理解请求主干与可靠性实现

## 本周目标

把一次HTTP调用拆成请求上下文、中间件、资源捕获、Retry和Polling五个相互配合但所有权不同的实现模型。

## Day 6：基础请求与请求上下文

- **实现问题**：一次HTTP attempt的输入、配置、响应和诊断字段由谁持有。
- **核心入口**：`common/base_request.py`与RequestContext。
- **离线验证**：运行BaseRequest相关离线测试，按实际顺序标记八阶段主线。
- **可核验产出**：字段归属表、八阶段请求图和Response关闭责任说明。

## Day 7：请求中间件脱敏与诊断日志

- **实现问题**：真实请求参数如何与脱敏诊断副本并行存在而不互相污染。
- **核心入口**：Middleware、Redaction和Logging实现。
- **离线验证**：运行已有中间件、日志和脱敏测试，核对日志不暴露敏感字段。
- **可核验产出**：双轨数据图、中间件顺序表和日志安全边界。

## Day 8：资源捕获下载与原子落盘

- **实现问题**：请求输入输出资源如何捕获并安全落盘，以及取消请求、worker观察取消、Response/文件关闭、临时文件清理、done_event和线程结束分别能证明什么。
- **核心入口**：Media/Capture、downloads和`.part → 校验 → replace`落盘链。
- **离线验证**：现有`tests/test_capture_downloads.py`只有4项测试，分别证明关闭Capture不启动线程、输出下载失败仍返回原Response、累计字节超限后目录为空，以及输入/输出命名规则一致；取消观察、`done_event`、Response关闭、Content-Length预检、成功replace时机和线程结束只能做源码追踪，不能宣称已被该测试文件覆盖。
- **可核验产出**：输入/输出Capture触发链、五阶段资源状态表、四项测试证据/未覆盖合同对照表，以及当前实现无法同步证明完全收口的边界说明。

本日不读取`util/artifact_io.py`，不学习JSON/JSONL读取、Hash或Manifest。安全落盘不等于可信消费；基础Artifact准入模型在Day21建立，跨消费者和签名边界在Day26深化。

## Day 9：请求重试策略

- **实现问题**：Retry如何区分可恢复条件、幂等边界、等待预算和最终异常。
- **核心入口**：`common/retry.py`、`common/retry_executor.py`。
- **离线验证**：运行现有Retry测试并使用已有推演器核对决策分支。
- **可核验产出**：Retry决策表、幂等边界表和等待预算账本。

## Day 10：轮询状态机与总截止时间

- **实现问题**：Polling如何用统一deadline管理多次请求、等待和四类终态。
- **核心入口**：`common/polling.py`、`common/base_request.py`中的协作入口。
- **离线验证**：运行Polling分类测试，核对状态迁移和总预算扣减。
- **可核验产出**：四状态迁移图、deadline账本和Retry/Polling预算关系图。
- **周复盘30～45分钟**：整合RequestContext、Middleware、Capture、Retry和Polling，订正至少一个预算或资源所有权误解。

## 第2周门禁

- [ ] 已提交Request、Retry和Polling预算关系图。
- [ ] 能解释真实参数与诊断副本为什么必须分离。
- [ ] 能区分取消请求、worker结束、Response/文件关闭、临时文件清理和线程完全结束，并说明当前实现没有`join()`保证。
- [ ] 能区分单次attempt预算、Retry等待和Polling总deadline。
- [ ] Day8没有提前把Artifact原语解释成完整信任模型。
- [ ] 已按六维量表完成第2周评分，总分至少9分且所有权、证据映射均不为0分。

---

# 第3周：理解流式、资源、并发和执行计划

## 本周目标

理解长响应资源生命周期、测试上下文清理、ContextVar传播、CLI参数边界和权威收集分池之间的关系。

## Day 11：流式响应与资源生命周期

- **实现问题**：SSE响应如何完成Header验证、事件消费、终态识别和连接关闭。
- **核心入口**：`common/streaming.py`及现有流式测试。
- **离线验证**：运行流式响应专项测试并标记消费与close路径。
- **可核验产出**：SSE生命周期图、终态表和资源关闭责任表。

## Day 12：测试上下文提取与资源清理

- **实现问题**：TestContext如何按“提取与缺失/默认处理 → `transform`转换 → `expected_type`校验 → 写入Context”的顺序保存数据，并按LIFO清理资源。
- **核心入口**：`common/test_context.py`和cleanup测试。
- **离线验证**：运行已有上下文提取与清理测试，验证`expected_type`只做`isinstance`校验而不承担转换。
- **可核验产出**：四阶段数据写入链、transform/expected_type职责表和LIFO清理所有权图。

## Day 13：上下文传播与并发隔离

- **实现问题**：ContextVar如何跨执行器传播，并保持线程、Session和Header隔离。
- **核心入口**：上下文执行器、并发分类测试和实例化边界。
- **离线验证**：运行已有并发隔离测试并记录每个对象的作用域。
- **可核验产出**：上下文传播图、隔离矩阵和共享状态判断标准。

## Day 14：稳定入口与命令行参数

- **实现问题**：用户参数如何从`run_master.py`稳定入口进入内部Runner配置。
- **核心入口**：`run_master.py`、CLI解析与配置对象。
- **离线验证**：运行CLI帮助和已有参数解析测试，不执行真实Smoke。
- **可核验产出**：参数路由表、公开入口与内部模块边界图。

## Day 15：权威收集与并串行调度

- **实现问题**：一次权威收集如何形成唯一nodeid集合并守恒地拆成并发池与串行池。
- **核心入口**：收集器、分类标记和分池逻辑。
- **离线验证**：对离线模块和Smoke执行collect-only，核对集合互斥、并集与nodeid唯一。
- **可核验产出**：集合守恒证明、并串行判定表和资源所有权矩阵。
- **周复盘30～45分钟**：连接SSE、TestContext、ContextVar、CLI参数和权威收集，把并发隔离判断迁移到另一个现有测试分类。

## 第3周门禁

- [ ] 已提交资源所有权与并串行分类矩阵。
- [ ] 能解释SSE消费结束与Response关闭不是同一个动作。
- [ ] 能解释TestContext清理顺序和ContextVar传播边界。
- [ ] 能证明parallel与serial集合互斥且并集等于权威收集集合。
- [ ] 所有实践仅使用离线测试或collect-only。
- [ ] 已按六维量表完成第3周评分，总分至少9分且所有权、证据映射均不为0分。

---

# 第4周：理解Runner、Allure与Quality接入

## 本周目标

先建立Runner与Allure的执行事实，再理解common如何通过中性Runtime Hooks发出事件，最后学习Quality生命周期如何安装Provider并消费事件。

## Day 16：执行器状态与退出码

- **实现问题**：Runner如何从执行计划、池结果和原始退出码计算最终进程退出码。
- **核心入口**：`run_orchestration/`中的计划、执行和结果模型。
- **离线验证**：运行现有Runner测试并读取`reports/execution-result.json`结构。
- **可核验产出**：阶段状态机、池结果表和最终退出码决策表。

## Day 17：测试报告生命周期与机器产物

- **实现问题**：Allure原始结果如何清理、隔离、合并、生成HTML并恢复history。
- **核心入口**：Allure生命周期与`execution-result.json`。
- **离线验证**：运行已有Allure生命周期测试，区分JUnit、Allure raw和HTML职责。
- **可核验产出**：单一生命周期图、机器产物职责矩阵和历史恢复链。

## Day 18：运行时观察接口

- **实现问题**：公共层如何通过Protocol、Noop、Provider和Lease发出中性事件而不依赖Quality。
- **核心入口**：`common/runtime_hooks/`。
- **离线验证**：运行Runtime Hooks专项测试，核对关闭时Noop、开启时Provider租约和Provider切换边界。
- **可核验产出**：Protocol/Provider/Lease对象关系图、事件边界表和依赖方向证明。

## Day 19：可选质量能力与执行阶段

- **实现问题**：Runner如何在Quality关闭、collect-only和正式开启时安装观察Provider、建立运行身份并管理产物阶段。
- **核心入口**：`run_orchestration/quality_lifecycle.py`和插件入口。
- **离线验证**：运行已有Quality生命周期测试，对比disabled、collect-only、enabled三条路径。
- **可核验产出**：Hooks安装位置、三路径生命周期图、开关与副作用对照表。

## Day 20：质量适配与运行事实采集

- **实现问题**：Quality适配器如何把HTTP、Retry、Polling和SSE事件映射为运行事实。
- **核心入口**：`quality/runtime_adapter.py`与Collector。
- **离线验证**：运行适配器与采集器已有测试，核对事件到事实字段的映射。
- **可核验产出**：事件映射表、Case/Request事实边界和旁路失败原则。
- **周复盘30～45分钟**：整合Runner、Allure、Runtime Hooks、Quality生命周期和Adapter，订正“报告等于执行事实”或“Hooks等于Quality”的混淆。

## 第4周门禁

- [ ] 已提交Runner、Allure与Quality生命周期对照图。
- [ ] 能从池级退出码解释Runner最终退出码。
- [ ] 能区分JUnit、Allure、execution-result和Quality事实。
- [ ] 能解释common为什么只依赖中性Runtime Hooks。
- [ ] 能说明Day18只定义并发出中性事件、Day19管理Quality接入、Day20才消费事件并形成质量事实。
- [ ] 已按六维量表完成第4周评分，总分至少9分且所有权、证据映射均不为0分。

---

# 第5周：理解Quality归并、语义、指标与Flaky

## 本周目标

先建立Artifact基础准入模型，再理解worker事实如何完成提交、表达完整性并形成可由具体消费者判定是否接纳的P0。P0事实形成后分成两条硬依赖支线，但每条支线仍要执行自己的消费者准入策略：一条建立Semantic并派生Metrics，另一条直接导入Flaky历史并推进State与Governance；课程顺序不等于数据依赖顺序。

```text
Manifest最小解析与身份/版本/status/integrity校验
→ 目标产物bytes与Manifest Hash核对
→ 当前消费者准入判定
→ P0 → Semantic → Metrics
     └→ Flaky Import → State → Governance
```

## Day 21：质量事实归并与完整性

- **实现问题**：Manifest与目标产物如何形成基础准入链，以及worker分片如何通过提交清单和Aggregator完成P0写入；`status=complete`为什么只表示写入事务完成而不等于完整性通过或发布者可信。
- **核心入口**：`quality/aggregator.py`及分片Manifest模型。
- **离线验证**：运行P0归并专项测试并核对分片、提交、最终文件、`status`、`integrity_status`和`output_hashes`关系。
- **可核验产出**：Manifest基础准入链、归并链、`complete/degraded/failed`消费矩阵和缺失值表达规则。
- **关系锚点**：读取Day20 worker事实；本日新增submission、Manifest基础准入模型和P0归并；向Day22与Day24输出“已完成提交、等待各消费者按身份/版本/完整性/Hash策略判定”的基础事实。

## Day 22：语义关系与所有权完整性

- **实现问题**：Case、Operation、Request Group和Polling如何形成可追踪的语义关系，以及当前Semantic为何只能把P0表述为“run_id、status和目标Hash通过”，不能称为“完整性可信P0”。
- **核心入口**：Semantic模型、Collector与Aggregator。
- **离线验证**：运行Semantic专项测试并对照`quality/semantic_aggregator.py::_load_p0_evidence`，确认它检查run_id、status=complete和request-metrics Hash，但不检查P0版本或integrity_status。
- **可核验产出**：语义实体关系图、所有权完整性表、当前P0门禁/未检查项对照表和关系终态说明。
- **关系锚点**：读取Day21已完成提交且通过当前有限门禁的P0事实与Semantic worker分片；本日新增语义关系；只向Day23 Metrics输出可追踪的Case、Operation与Request Group关系，不成为Flaky Import前置，也不把该P0升级表述为完整性可信。

## Day 23：指标来源验证与解释

- **实现问题**：Metrics如何验证来源、分母、缺失值和聚合状态后再输出指标。
- **核心入口**：`quality/metrics/`。
- **离线验证**：运行Metrics专项测试并对照Manifest与`run-metrics.json`。
- **可核验产出**：来源信任链、分母说明和原始事实/逻辑事实/派生指标三层表。
- **关系锚点**：读取P0与Semantic；本日新增来源验证、分母和派生指标；不反向改写原始事实，也不是Day24 Flaky Import的前序输入。

## Day 24：不稳定用例身份周期与导入

- **实现问题**：可比较身份、epoch、Observation和导入幂等如何建立跨run历史。
- **核心入口**：Flaky models、importer和repository公开边界。
- **离线验证**：查看CLI帮助并运行已有导入与identity测试，数据库只使用测试夹具或临时路径。
- **可核验产出**：identity/epoch/Observation关系图和幂等导入链。
- **关系锚点**：从Day21形成并通过Flaky导入准入条件的P0 run、Case、Failure和完整性事实直接读取；不依赖Semantic或Metrics；本日新增可比较身份、epoch和Observation，并向Day25输出可治理历史。

## Day 25：不稳定用例状态与治理

- **实现问题**：检测基线与有效治理状态如何按不同转换规则耦合，公开CLI如何更新两列并审计治理信息。
- **核心入口**：projection、governance和Flaky CLI。
- **离线验证**：运行已有状态机与治理测试，不连接Jenkins持久化数据库。
- **可核验产出**：detected_state/current_state转换矩阵、命令与审计职责表，明确哪些动作同时改两列、哪些治理动作主要只改current_state。
- **关系锚点**：读取Day24身份与历史样本；本日新增检测基线、有效状态、人工裁决、治理阶段和审计；不替代单轮P0事实。
- **周复盘30～45分钟**：在本周主成果中补全两条分支，明确`P0 → Semantic → Metrics`与`P0 → Flaky Import → State → Governance`共享已完成提交的P0，但分别执行自己的准入策略且彼此不串行依赖；订正至少一个Manifest、identity或双状态混淆。

## 第5周门禁

- [ ] 已在同一主成果中画出P0分叉到Semantic/Metrics与Flaky/State/Governance的证据关系。
- [ ] 能区分`status=complete`、`integrity_status`、Hash匹配、真实性和消费者准入策略。
- [ ] 能区分原始事实、语义关系和派生指标。
- [ ] 能解释Flaky可比较身份、epoch、检测基线、有效状态及不同人工动作对两列的更新规则。
- [ ] Flaky实践只使用帮助、已有测试或隔离数据库。
- [ ] 已按六维量表完成第5周评分，总分至少9分且所有权、证据映射均不为0分。

---

# 第6周：深化产物信任、报告、CI和完整实现链

## 本周目标

在Day21基础准入模型之上比较不同消费者的接纳策略，区分Hash完整性与签名/可信分发渠道提供的真实性边界，再完成Pipeline Reporting、Jenkins编排与现有离线场景跨层追踪。

## Day 26：Artifact读取Hash与Manifest信任

- **实现问题**：不同消费者如何复用Day21基础准入模型，并把Manifest链和目标产物链分开；Hash为什么只能证明内容一致，不能独立证明Manifest发布者真实性。
- **核心入口**：`util/artifact_io.py`、Metrics与Flaky消费者。
- **离线验证**：运行现有Artifact、Metrics和Flaky信任测试，分别核对Manifest最小解析与字段准入、目标产物bytes Hash、Schema/领域解析顺序和消费者差异。
- **可核验产出**：`Manifest bytes → 最小解析 → 身份/版本/status/integrity → 外部签名或可信渠道边界`与`目标bytes → Hash核对 → Schema/领域解析`双链、跨消费者矩阵和领域错误职责表。
- **Day29材料**：形成Artifact信任边界卡片，后续直接纳入跨层证据说明。

## Day 27：流水线报告事实与多视图

- **实现问题**：Source、PipelineReport、Markdown、机器JSON和邮件如何共享同一结论对象。
- **核心入口**：`pipeline_reporting/`。
- **离线验证**：运行Reporting专项测试和CLI帮助，核对事实优先级与NOT_RUN/NO_DATA差异。
- **可核验产出**：来源优先级表、一个Report到多视图关系图。
- **Day29材料**：完成Runner、Quality与Reporting机器产物职责矩阵初稿。

## Day 28：持续集成编排与发布边界

- **实现问题**：Jenkins如何负责编排、阶段状态、归档和通知，而不复制Python报告语义。
- **核心入口**：根目录`Jenkinsfile`与迁移模板。
- **离线验证**：静态阅读Pipeline阶段和参数；不触发真实Smoke或修改Jenkins持久状态。
- **可核验产出**：阶段编排图、Python与Jenkins职责边界、发布产物清单。
- **Day29材料**：完成Jenkins发布边界和一个关键设计取舍说明。

## Day 29：离线场景跨层实现追踪

- **建议学习**：3～4小时。
- **实现问题**：一个已有离线场景如何从Test贯穿Request、Runner、Quality到Pipeline Summary。
- **核心入口**：学员选择的现有离线nodeid及其真实调用链。
- **离线验证**：只运行所选现有场景和必要的已有专项测试，不修改框架代码。
- **主成果**：整合Day26～28累积材料，提交一份“跨层实现档案”；完整调用链、对象与状态所有权、资源生命周期、机器产物职责和关键设计取舍作为档案章节，不拆成五份独立成果。

## Day 30：结业答辩与同轮证据验收

- **建议学习**：3～4小时。
- **实现问题**：如何证明Runner、Quality和Pipeline Summary来自同一轮离线执行，而不是历史产物拼接。
- **核心入口**：`run_master.py`、Quality环境开关、`pipeline_reporting` CLI及本轮`reports/`证据。
- **学习边界**：不新增功能、不制造失败；Flaky默认关闭，需要学习Flaky时只能使用临时数据库。
- **脚本自动检查范围**：必需文件与Allure raw存在、四个Quality run_id一致、Runner非NOT_RUN stage_id与P0 expected_execution_ids均为`serial-pool`、P0/Semantic/Metrics提交状态、Reporting质量阶段与run_id warning、核心文件时间和JUnit数量。
- **人工答辩范围**：Runner target、planned nodeids、final/原始退出码、P0 integrity、Semantic/Metrics上游引用，以及Markdown/JSON关键结论一致性；这些不能宣称由内嵌脚本自动验证。
- **答辩范围**：解释Day29调用链、各层所有权、同轮身份、事实优先级和安全边界。
- **总复盘**：在同一“同轮证据答辩包”中更新第6周及全课程关系、错误订正和六维最终评分，不再新增独立复盘文件。

### Day30同轮证据流程

最终验收必须在一次连续会话中完成以下步骤：

```text
记录执行日期与工作树状态
→ 备份需要保留的reports本地产物
→ 清理本轮可再生产物
→ 创建唯一run_id
→ 显式开启QUALITY_ENABLE、QUALITY_SEMANTIC_ENABLE和QUALITY_METRICS_ENABLE
→ 指定reports/quality与同一个QUALITY_RUN_ID
→ 初始化本轮pipeline阶段状态
→ 运行module/offline_framework_example并生成JUnit、Allure和execution-result
→ 按本轮Runner结果更新real_smoke阶段状态
→ 在环境开关仍属于本轮时显式调用pipeline_reporting generate
→ 同时生成pipeline-summary.md和pipeline-summary.json
→ 核对run_id、execution_id、Manifest、文件时间与Reporting质量来源状态
→ 保存验收记录后恢复原有本地产物
```

禁止使用以下方式替代同轮证据：

- 直接读取上一次`reports/quality`。
- Runner结束后恢复旧`execution-result.json`再生成摘要。
- 未开启Quality却把目录中残留文件解释为本轮质量事实。
- 只生成Markdown而不生成机器JSON，导致无法核对共享Report结论。
- 用“测试全绿”替代run_id、Manifest、来源状态和时间一致性检查。

### Day30同轮证据答辩包

只提交一份答辩包，包内包含以下核对章节：

- 本轮唯一`run_id`、执行日期、工作树状态和完整命令记录。
- 本轮JUnit、Allure raw、`execution-result.json`和Quality核心Manifest位置。
- 同轮`pipeline-summary.md`与`pipeline-summary.json`。
- Quality `run.json`、归并Manifest、Semantic Manifest和Metrics Manifest的身份核对表。
- Reporting中Quality来源状态与本轮开关、文件身份的对应说明。
- 当前测试总数、通过数、失败数及失败归属；不要求用“全部通过”掩盖已知基线。

## 第6周门禁

- [ ] 已提交完整实现链与同轮证据矩阵。
- [ ] 能区分Artifact机械校验与领域消费者错误翻译。
- [ ] 能解释PipelineReport如何生成Markdown、机器JSON和邮件视图。
- [ ] 能解释Jenkins只负责编排和发布，不复制Python报告结论。
- [ ] Day30证据具有同一run_id、同轮Manifest和可核对的生成时间。
- [ ] 已按六维量表完成最终评分，总分至少9分且所有权、证据映射均不为0分。

---

# 7. 知识点覆盖清单

## 业务与用例层

- [ ] 模块四件套与标准目录结构。
- [ ] BaseTask、断言和pytest生命周期。
- [ ] 离线服务级、能力级和分类级测试。

## 请求与可靠性层

- [ ] BaseRequest、RequestContext和中间件顺序。
- [ ] 脱敏日志、Capture、下载和资源关闭。
- [ ] Retry决策、Polling总deadline和SSE生命周期。
- [ ] TestContext、ContextVar与并发隔离。

## 执行编排层

- [ ] 稳定入口、CLI、权威收集与集合守恒。
- [ ] Runner状态机、池结果与退出码。
- [ ] Allure单一生命周期和execution-result机器事实。

## 质量治理层

- [ ] Quality可选加载与Runtime Hooks中性接口。
- [ ] 运行事实采集、P0归并与完整性。
- [ ] Semantic关系、Metrics来源和Flaky双状态。
- [ ] Artifact信任边界与领域错误翻译。

## 报告与CI层

- [ ] Pipeline Reporting来源优先级与多视图。
- [ ] Jenkins阶段、归档和通知边界。
- [ ] 同轮Runner、Quality和Pipeline Summary证据闭环。

---

# 8. 每日学习笔记模板

```text
# 今日主题

## 1. 本课要解释的实现问题与不展开范围
## 2. 前序模型：本课读取什么
## 3. 公开入口
## 4. 真实调用链
## 5. 对象、状态与资源所有权
## 6. 生命周期、终态与副作用
## 7. 现有测试或机器证据
## 8. 设计选择、替代方案与不适用条件
## 9. 主成果增量：把图、表、状态和边界写入同一份材料
## 10. 可选草稿：迁移、订正与仍无法解释的问题
## 11. 成果位置或口头验收记录
## 12. 周末日六维评分
```

---

# 9. 结业能力评估

结业不要求开发新框架功能。学员需要选择一个已有离线场景并完成实现答辩：

1. 从测试入口准确定位到Task、Request和Assertions。
2. 解释实际经过的Middleware、Retry、Polling或SSE分支。
3. 说明Context、Session、Response、文件和数据库的所有权。
4. 解释Runner收集、分池、执行和退出码事实。
5. 解释Quality开启时Runtime Hooks、Adapter、分片、归并、Semantic和Metrics链路。
6. 区分JUnit、Allure、Quality和Pipeline Summary分别回答的问题。
7. 提供Day30同轮Quality与Reporting机器证据。

---

# 10. 课程使用建议

- 按周推进，不建议跳过周门禁直接进入Quality或Reporting。
- 每天先确定一个实现问题，再打开3～6个核心文件，避免无边界遍历目录。
- 命令执行后必须把输出映射回对象、状态或产物节点，不能只记录“运行成功”。
- 页面中的数量与状态以本轮实际输出为准，历史快照只用于比较。
- 当仓库并行更新时，以当前`README.md`、`FRAMEWORK_TEST_SPEC.md`和公开接口为准，记录日期与工作树状态。
- 旧四周总纲仅提供迁移链接，不再承担课程内容合同。
